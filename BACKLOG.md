# AittaDB Backlog

This file records uncommitted implementation ideas. An item here is not a current capability, release commitment, delivery schedule, or stable API contract. Stable `BACKLOG-NNN` identifiers must not be renumbered or reused.

`PLAN.md` is the auditable list of accepted implementation work. `ROADMAP.md` describes public product direction. Before work starts on a backlog idea, define the integrated contract, implementation, tests, documentation, failure behavior, and acceptance evidence as a new unchecked `PLAN.md` task. The backlog item remains unchecked until the idea is either promoted through that process or explicitly retired with a documented replacement.

A backup or synchronization feature must operate only on the authenticated user's data within the authorized OAuth client namespace. It must never export, import, mirror, or reveal AittaDB authentication tables, other users' or clients' data, physical R2 keys, bindings, environment variables, signing material, hosted secrets, or deployment metadata. ChatGPT Sites D1/R2 capacity and operational limits remain provider-controlled and numerically undocumented.

Downstream OAuth Apps means third-party clients registered with AittaDB and the public OAuth/OIDC grants they use. It is distinct from AittaDB's own ChatGPT Sites browser session. OAuth Apps must remain disabled by default until the focused backlog items below are promoted through `PLAN.md`, completed, and accepted against one exact deployment.

Events review (2026-08-11): `BACKLOG-004` is the only backlog item that directly names persistent Events. It remains here because opt-in record/file live synchronization is a separate downstream primitive that can consume Events only after the core contracts promoted from `ROADMAP-009` through `ROADMAP-015` are complete. `BACKLOG-003` describes backup change history, and `BACKLOG-006` describes internal audit records; neither is the public Events feature.

- [ ] BACKLOG-001: Define namespace-scoped point-in-time backup export for an authenticated user's authorized application records and files, including a versioned manifest, bounded D1/R2 snapshot-consistency semantics, integrity metadata, caller-controlled encryption, resumable transfer, retention and expiry rules, and explicit exclusion of AittaDB internal state and secrets.
- [ ] BACKLOG-002: Define verified backup restore into the same authorized user/client namespace, including manifest and content-integrity checks, ownership proof, dry-run reporting, idempotent retries, collision policy, partial-failure recovery, quota and rate enforcement, and no ability to choose or overwrite another namespace.
- [ ] BACKLOG-003: Define incremental backups after a verified checkpoint, including opaque durable cursors, ordered changes, tombstones for deletions, cursor expiry and retention, bounded batches, idempotent replay, and behavior when history is no longer available.
- [ ] BACKLOG-004: Define opt-in live synchronization for authorized records and files, including resumable ordered delivery, idempotency keys, backpressure, rate and quota controls, connection recovery, an ordinary polling fallback, and integration with persistent Events only after the Events contracts exist.
- [ ] BACKLOG-005: Define live-sync conflict and deletion semantics, including resource versions or entity tags, concurrent update detection, deterministic conflict reporting, explicit resolution choices, tombstone lifecycle, and protection against silent destructive overwrite.
- [ ] BACKLOG-006: Define privacy-preserving backup and synchronization administration, including user-visible status, bounded audit events, recovery drills, restore verification, failure alerts, key-loss guidance, provider-limit failures, and redaction of identities, credentials, object keys, and secret material.
- [ ] BACKLOG-007: Define the release gate for enabling downstream OAuth Apps, including an exact supported-surface inventory, secure configuration default, migration compatibility, complete OpenAPI/discovery behavior, zero-high dependency audit, and explicit hosted acceptance evidence.
- [ ] BACKLOG-008: Complete OAuth-client list/create acceptance, including public and confidential types, exact redirects/origins/scopes, reload-safe creation, and client-bound one-time secret display.
- [ ] BACKLOG-009: Complete OAuth-client enable/disable acceptance, including state-dependent HTML/hypermedia controls, disabled-client rejection, idempotent reloads, and redacted audit evidence.
- [ ] BACKLOG-010: Complete confidential-secret rotation and client-grant revocation acceptance, including old-secret rejection, one-time new-secret display, refresh-family revocation, replay safety, and redacted audits.
- [ ] BACKLOG-011: Complete Device Grant browser-state behavior, including entry, pending decision, terminal approved/denied/used/expired representations, same-origin continuation, and no terminal mutation actions or reflected codes.
- [ ] BACKLOG-012: Remove readable Device Grant user codes from runtime repository contracts, retain hash-only durable lookup, prove invalid and terminal responses and logs contain no code, and verify the deployed migration scrub.
- [ ] BACKLOG-013: Prove authorization requests transition atomically from pending to one approved or denied terminal state across explicit consent and remembered-consent continuation, with no duplicate authorization code or redirect manipulation.
- [ ] BACKLOG-014: Prove authorization-code consumption is atomic under production-shaped and hosted concurrent exchange, yielding exactly one token response and terminal `invalid_grant` losers.
- [ ] BACKLOG-015: Prove approved Device Grant consumption is atomic under production-shaped and hosted concurrent polling, yielding credentials at most once and terminal replay behavior.
- [ ] BACKLOG-016: Prove refresh-token rotation is atomic under concurrency, issues no two usable successors, detects predecessor reuse, and revokes the associated token family deterministically.
- [ ] BACKLOG-017: Complete access-JWT revocation by `jti`, owning-client binding, omitted/wrong hint handling, and immediate rejection by every supported token consumer.
- [ ] BACKLOG-018: Complete opaque refresh-token revocation with owning-client binding, omitted/wrong hint handling, family invalidation, and non-disclosing success behavior.
- [ ] BACKLOG-019: Complete bounded OAuth state cleanup and endpoint-specific rate-limit recovery, including refresh-family creation/cleanup races, memory/D1 parity, supporting query plans, and hosted D1 evidence.
- [ ] BACKLOG-020: Complete downstream OAuth token-endpoint CORS and active-client enforcement before authorization-code, device-code, or refresh-token consumption, including foreign-origin non-consumption and disabled-client behavior.
- [ ] BACKLOG-021: Complete a two-client hosted OAuth interoperability matrix against one exact deployment without recording client secrets, grant credentials, user identity, or complete redirect URLs.
- [ ] BACKLOG-022: Move bounded canonical bearer-request admission ahead of request-body buffering where a cheap non-disclosing pre-authentication and per-source rate gate can reject abusive storage, Events, and OAuth traffic without weakening stream limits, browser pre-body origin checks, client-bound CORS, or protocol authentication; include deterministic ordering, D1-failure behavior, memory-amplification tests, and operational telemetry before promotion to PLAN.
