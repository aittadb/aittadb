# Changelog

## 0.1.0

- Initial AittaDB MVP with ChatGPT Sites identity mapping, Device Authorization Grant, Authorization Code with PKCE, OIDC discovery, JWKS, UserInfo, introspection, revocation, D1 migrations, tests, CI, and documentation.
- Established `develop` as the primary workspace and feature-integration base; incomplete feature PRs target it, and only complete, validated work may enter before a reviewed release PR to `main`.
- Added an image-led, page-aware browser interface for service metadata, health, device authorization, consent, administration, and error responses.
- Clarified the upstream boundary as ChatGPT sign-in inside ChatGPT Sites while retaining separate local subjects and AittaDB-issued credentials.
- Renamed the project to AittaDB and introduced its storehouse mark, Inter wordmark, navy/red-orange/teal palette, GitHub organization identity, hypermedia metadata name, and application-storage positioning.
- Updated the React/RSC, Vinext, Vite, Cloudflare, and Wrangler stack to patched compatible versions; removed unused Drizzle Kit and Fontsource tooling; corrected runtime dependency placement; and added enforced dependency-audit and automated update checks.
- Added production-backed browser forms for OAuth/OIDC and D1/R2 storage operations, plus a self-hosted Swagger UI for the canonical OpenAPI document.
- Added current signed-in session modes for UserInfo and isolated record/file storage, backed by a hidden non-administrable browser client and the canonical token validators.
- Hardened storage non-disclosure and cross-user/client isolation, made file downloads attachment-safe, isolated AittaDB shell styles from Swagger UI controls, and added focused regression coverage.
- Removed request-time D1 schema work, packaged reviewed SQL migrations into Sites deployment artifacts, bypassed application startup for static assets, bounded cleanup scheduling, and parallelized client metadata hydration.
- Corrected browser form origin validation for canonical custom domains and Sites same-origin dispatch behavior without allowing arbitrary cross-origin submissions.
- Kept validated host-only CSRF session tokens stable across concurrently open browser forms while retaining malformed-token, mismatch, and same-origin rejection.
- Completed custom-domain acceptance with real ChatGPT Sites identity, OAuth/device-flow coverage, signed-in D1/R2 operations, all-route content negotiation, responsive browser QA, green CI, and improved warm request latency.
- Repositioned public copy around AittaDB's implemented hosted application-backend scope, distinguished durable signed-in storage from temporary browser state, exposed current and planned capabilities in root metadata, and removed sign-in-flow internals from normal user operation maps while preserving their canonical forms and API documentation.
- Made storage browser interfaces resource-oriented: collection URLs list and navigate, item URLs read/download, replace/upload, and delete only their own key, and protected form posts translate to canonical methods on the same URL. Added progressive conditional disclosure for authentication and OAuth grant fields with a complete no-JavaScript fallback.
- Compacted the authoritative `AGENTS.md` while preserving project and security rules, and added a tested local/CI guard requiring it to remain below 32,000 bytes.
- Added the versioned `data`/`links`/`actions` hypermedia JSON contract with same-URI HTML equivalence, session-aware sign-in/sign-out controls, transaction resources, and privacy-preserving aggregate service statistics.
- Added collection-level `POST /storage/files` creation with server-generated logical keys, `201 Created`, and `Location`, while retaining item `PUT` for caller-keyed create/update and strict per-user, per-client isolation.
- Upgraded storage HTML to render collection lists or empty states, readable item details and outcomes, state-aware operations, and an accessible file upload control with drag-and-drop enhancement.
- Made Swagger "Try it out" use the viewer's current origin, and clarified that AittaDB can be independently deployed on ChatGPT Sites with Identity, Data, and Files available now and Events explicitly planned.
- Added a flat, stable-ID roadmap that inventories only missing storage capabilities, decomposes the planned Events subsystem and storage-policy work, and keeps unknown Sites D1/R2 limits explicit without presenting roadmap items as current features.
- Removed deployable mock-identity bindings and runtime branches; production now injects only the documented ChatGPT Sites header provider while tests use an explicit provider located under `tests/`.
- Hardened OAuth state with atomic authorization, device, code, and refresh-token transitions; access-token purpose checks; `openid`-gated UserInfo; client-bound access/refresh revocation with hint fallback; and refresh-family reuse detection.
- Made browser mutation origin checks reject missing headers, bounded complete multipart streams independently of `Content-Length`, and added copy-on-write plus bounded D1/R2 compensation for file failures.
- Replaced Vinext's vulnerable unpatched `image-size` transitive package with a narrow checked-in compatibility adapter backed by `image-dimensions`, retaining a zero-high-severity dependency audit.
- Added an explicitly non-committal backlog for namespace-isolated backup, verified restore, incremental backup, and live synchronization design work, with repository checks that keep it distinct from the implementation plan and product roadmap.
- Added finite deployment, local-user, and local-user/client storage item and byte ceilings; a storage-write kill switch; namespace-only usage reporting; and signed cursor pagination with bounded page sizes.
- Made endpoint and storage rate counters atomic, expanded expiration cleanup into bounded batches, bounded accepted request bodies while streaming, restricted UserInfo/storage CORS to exact active-client origins, rejected disabled clients, and enabled HSTS for production HTTPS responses.
- Bound administration directly to the current trusted ChatGPT Sites session and a canonical immutable local-subject allowlist, removed the redundant administrator-key/unlock layer and email bootstrap, retained same-origin/CSRF enforcement, and kept redacted mutation auditing.
- Unified OAuth-client administration around one state/type-aware policy shared by HTML, versioned hypermedia, and POST validation; added atomically claimed one-time submissions, reload-safe HTML Post/Redirect/Get with encrypted single-use results, client-bound one-time secret display, grant-revocation lifecycle coverage, and versioned non-disclosing failure responses.
- Removed short Device Grant user-code display values from durable state, scrubbed existing values in migration `0004`, and retained consent display only from the matching same-origin browser submission.
- Extended R2 copy-on-write and D1 metadata compensation around quota rejection and replacement/deletion failures while documenting that simultaneous persistent D1 and R2 failures can still require operator repair.
- Added physical-key compare-and-set file mutations with deterministic concurrent losers, encrypted namespace-bound storage cursors, grace-safe indexed refresh-family cleanup, pre-authenticated multipart parsing, and client-bound token-endpoint CORS before credential consumption.
- Added a content-negotiated deployment Privacy Policy with accessible HTML, versioned hypermedia JSON, shared page links, public environment-based operator details, a first-administrator contact fallback, generic unavailable behavior, and policy/retention guidance for independent Sites operators.
- Consolidated dependency delivery into draft PR #7: integrated `@next/eslint-plugin-next` 16.3.0 and `globals` 17.9.0 with Dependabot provenance, corrected the Tailwind proposal by updating `tailwindcss` and `@tailwindcss/postcss` together to 4.3.3, and closed the superseded PRs without merging. TypeScript 7 and ESLint 10 remain deferred because their exact peer stacks fail clean installation; the validated lockfile audit remains clean.
- Expanded encrypted storage-cursor evidence to scan both the token and decoded envelope for positions, namespace identifiers, and private signing material, and documented immediate invalidation when private signing-key material rotates.
- Defined AittaDB as a general-purpose database/application-backend server built from reusable vendor-neutral primitives, kept client and application implementations outside this repository, and added a tested decision rule against application-specific or speculative architecture.
- Reframed public licensing copy around AittaDB's source-available FSL-1.1-MIT release, its automatic two-year MIT conversion, and the commercially available immediate MIT license, while retaining the OpenAI no-affiliation boundary as secondary trust information.
- Shortened the browser home hero to one capability sentence and one compact trust note with a README CTA, while preserving the detailed licensing, platform, and credential boundary in hypermedia and project documentation.
- Completed TASK-124 by proving quota-rejected file reservations use the same owner-bound repair path as other failed R2 retirements: the losing write commits no metadata, persistent compensation creates one private bounded repair item, and retry converges without exposing physical keys. Isolated commit `70e1a4a` and integrated commit `c7b37f0` passed CI runs `31264366690`, `31264479561`, and `31264477454`; full validation passed 142 unit tests, 133 integration tests, OpenAPI, Swagger, migration, audit, and production-build checks (readiness confidence: 98/100).
- Implemented TASK-166 source behavior: successful signed-in HTML collection uploads accept an absolute file-item `Location` only from the request or configured issuer origin, validate its exact path, and construct `303 See Other` on the request origin; malformed and foreign targets retain `201`, and bearer, hypermedia, JSON, and explicit-token HTML behavior remains unchanged.
- **TASK-179:** Enable OAuth Apps on the canonical AittaDB service after TASK-178 merges. DoD: the exact reviewed `main` commit containing migration `0012` is validated and deployed to `aittadb.com`, `FEATURE_OAUTH_APPS_ENABLED=true` is applied without changing unrelated hosted settings or secrets, discovery advertises `client_credentials`, an administrator can register a storage-only service client, disabled/anonymous behavior and existing signed-in storage remain intact, rollback steps and acceptance evidence are recorded, no AittaSocial-specific client code or secret enters this repository, and the production change is not performed before PR approval and merge.
  Acceptance evidence (2026-08-09): after PR #7 was approved and rebase-merged, exact `main` commit `52a1fe2` passed formatting, linting, strict type checking, the tracked-tree secret scan, 300 unit tests, 157 integration tests, OpenAPI/Swagger/migration checks, the instruction-size guard, a zero-vulnerability audit, and the production build. That exact commit and its twelve migrations were saved as Sites version 44 and deployed to public `aittadb.com` with environment revision 25; only `FEATURE_OAUTH_APPS_ENABLED` changed from `false` to `true`, all fourteen hosted keys remained present, secret values remained redacted, and access stayed public. Canonical root metadata reports OAuth Apps enabled, and canonical OIDC discovery advertises `client_credentials`. The existing allowlisted administrator created one storage-only service client and received its 43-character secret once into an ignored mode-0600 temporary file. Two distinct 600-second ES256 access tokens contained only the service principal and storage scopes; introspection, owner revocation, a synthetic D1 record create/read/delete/404 round trip, revoked-token rejection, and disabled-client token rejection passed. The client was left disabled, all synthetic storage was removed, anonymous storage, administration, and unauthenticated token requests each returned `401`, and the signed-in browser namespace remained unchanged at zero records and four pre-existing files. The temporary credential, scripts, archive, and in-memory secret reference were removed; no AittaSocial code, client credential, identity value, storage key, or Sites identifier entered Git. Rollback is to set only `FEATURE_OAUTH_APPS_ENABLED=false` and redeploy saved version 43 (`e48fea9`); migration `0012` is forward-compatible and remains inert behind that feature gate. Readiness confidence: **98/100**; AittaSocial Hub must register and manage its own service client and hosted secret outside this repository.
- **TASK-181:** Make PR #7 eligible for GitHub's `Rebase and merge` operation. DoD: the exact reviewed branch tree is preserved byte-for-byte while its 193-commit PR history is consolidated onto current `origin/main` below GitHub's 100-commit rebase limit; a local safety ref and exact force-with-lease protect against concurrent updates during the rewrite and are removed after verification; complete validation and exact-head CI pass; GitHub reports the sole PR mergeable, clean, and rebaseable; no merge, deployment, access-setting change, secret, or unrelated source change occurs; and the completed operational history is archived in `CHANGELOG.md`.
  Acceptance evidence (2026-08-09): GitHub's API reported the conflict-free 193-commit PR as `rebaseable: false`, matching GitHub's documented 100-commit Rebase-and-merge limit. A temporary local safety ref protected the exact pre-rewrite tree while commit `4788121` reproduced that byte-identical tree as one linear commit on current `origin/main`; an exact lease then replaced only the intended remote branch. Full validation passed formatting, linting, type checking, secret scanning, 300 unit tests, 157 integration tests, OpenAPI/Swagger/migration checks, the instruction-size guard, a zero-vulnerability audit, and the production build. Both exact-head CI runs `31326021845` and `31326024465` passed; GitHub reported one commit with `mergeable: true` and `rebaseable: true`; and the temporary safety ref was removed. No source tree, merge, deployment, access setting, secret, or unrelated branch changed. Readiness confidence: **99/100**; a new push or `main` update can cause GitHub to recalculate mergeability and require fresh review.
- **TASK-178:** Add short-lived OAuth `client_credentials` access for isolated service-client storage. DoD: a dedicated service-client contract authenticates with a one-time-displayed hashed secret, obtains only configured `storage.read`, `storage.write`, and `storage.delete` scopes through the existing default-off OAuth Apps boundary, receives a normal short-lived ES256 access token with no user identity claims, ID token, or refresh token, and uses one stable namespace isolated from every user and other client; public, disabled, wrong-secret, interactive-grant, identity-scope, cross-client, revoked-token, CORS, rate, and feature-disabled paths fail without credential or namespace leakage; administration, migration, token/storage implementation, OpenAPI/discovery/hypermedia, security/architecture/deployment documentation, focused unit and integration tests, formatting, linting, type-checking, full validation, build, commit, CI, disposable `test.aittadb.com` deployment, renewable two-token storage round trip, and fixture/configuration cleanup pass.
  Acceptance evidence (2026-08-09): source commit `cc7e97e` passed the complete validation suite and both exact-head CI runs. On `test.aittadb.com`, two independently registered service clients obtained renewable 600-second ES256 access tokens; repeated tokens for one client retained one namespace, while records, files, revocation, and introspection remained isolated across clients. Rotated, revoked, disabled, and cross-client credentials were rejected. All test records and files were removed, both disposable clients were disabled, the temporary administrator allowlist and local credential artifacts were removed, and OAuth Apps remained enabled with discovery advertising `client_credentials`. Readiness confidence: **98/100**; the canonical production rollout remains independently gated by TASK-179 and user-reviewed merge approval.
- **TASK-182:** Add the durable event repository primitive (promotes ROADMAP-009). DoD: define one immutable namespace-owned event contract and narrow repository interface; add a forward D1 migration and schema manifest entry for opaque public UUIDs, internal ordered sequence, event type, bounded JSON payload, hashed optional idempotency key, request fingerprint, creation/expiry times, exact user/client ownership, uniqueness, supporting indexes, foreign keys, and inactive-subject write denial; implement D1 and memory append/get/list behavior with atomic exact-replay versus conflicting-reuse results; test deterministic ordering, immutability, duplicate and conflict behavior, malformed repository input, cross-user/client isolation, migration consistency, and memory/D1 parity; document the internal contract and failure semantics without exposing an HTTP route or claiming Events are available; and pass formatting, linting, type checking, focused tests, migration validation, and build.
  Planning retirement evidence (2026-08-11): this umbrella combined schema, append, point lookup, and paging primitives, so it was retired without claiming implementation and mapped to `TASK-199` through `TASK-202`.
- **TASK-183:** Add feature-gated event configuration and authorization scopes (promotes ROADMAP-015). DoD: add `FEATURE_EVENTS_ENABLED` with a fail-closed default of false plus finite payload, retention, cursor, page, quota, and read/publish/subscribe rate settings; define `events.publish`, `events.read`, and `events.subscribe` as AittaDB-only scopes; allow interactive clients and non-human service clients to receive only applicable configured scopes while preserving the prohibition on service identity claims, redirects, origins, ID tokens, and refresh tokens; update the reserved browser client and checked-in migration state consistently; omit event routes, controls, and scopes from runtime discovery/administration when disabled and reject disabled behavior before bodies, token consumption, rate limits, or repositories; test malformed configuration, scope escalation, service-client restrictions, feature visibility, and existing-scope compatibility; update `.env.example`, OpenAPI scope metadata, architecture, deployment guidance, and `AGENTS.md`; and pass full focused validation.
  Planning retirement evidence (2026-08-11): this umbrella combined feature availability, authorization scopes, and unrelated operational settings, so it was retired without claiming implementation and mapped to `TASK-203` and `TASK-204`; quotas, retention, cursors, paging, and rate settings remain with their owning tasks.
- **TASK-194:** Prove hosted event retrieval and cursor resume. DoD: on the exact TASK-192 deployment, publish bounded synthetic fixtures in one temporary namespace and prove deterministic collection ordering, finite pagination, opaque cursor resume without duplicates, exact type filtering, immutable item retrieval, malformed/expired cursor rejection, and generic missing/foreign item behavior; record only coarse evidence, remove all fixtures, and leave unrelated deployment state unchanged.
  Planning retirement evidence (2026-08-11): this hosted matrix combined collection traversal and item retrieval, so it was retired without claiming acceptance and mapped to `TASK-205` and `TASK-206`.
- **TASK-196:** Prove hosted Events authorization and namespace isolation. DoD: on the exact TASK-192 deployment, use two temporary clients and two existing authorized test identities to prove publish/read/subscribe scope separation, current-session behavior, service-token non-human claims, disabled-client denial, anonymous denial, cross-client isolation, and cross-user isolation; never record identity, local subject, secret, token, event ID, payload, or complete credential-bearing request; disable and remove grants for temporary clients, remove fixtures, and leave unrelated deployment state unchanged.
  Planning retirement evidence (2026-08-11): this hosted matrix combined scope policy, cross-client isolation, and cross-user isolation, so it was retired without claiming acceptance and mapped to `TASK-207` through `TASK-209`.
- **TASK-197:** Prove hosted event quotas, retention, and deletion safety. DoD: on the exact TASK-192 deployment with bounded temporary settings, prove one namespace item or byte ceiling rejects the boundary-crossing append without harming reads, expired events are removed only by bounded cleanup while unexpired/foreign events survive, and an account-deletion boundary blocks new event writes and cannot finalize while owned event residue remains; use only disposable fixtures suitable for deletion, record coarse counts/statuses, restore any altered temporary limit immediately after the proof, and leave unrelated deployment state unchanged.
  Planning retirement evidence (2026-08-11): this hosted matrix combined quota, retention, and account-deletion proofs, so it was retired without claiming acceptance and mapped to `TASK-210` through `TASK-212`.
- **TASK-198:** Restore the Events acceptance deployment after proofs. DoD: verify all temporary events, clients, grants, deletion fixtures, credentials, and local artifacts from TASK-192 through TASK-197 are removed or irreversibly disabled; restore every temporary acceptance environment setting while retaining only the explicitly intended Events feature state, redeploy the same reviewed source if configuration application requires it, confirm health and public/private authorization baselines, and prove `aittadb.com` remained untouched; archive each completed PLAN description unchanged with its evidence in `CHANGELOG.md`, leave `PLAN.md` with zero unchecked items, run complete validation, push the final feature branch, and leave the single PR ready for user review without merging it.
  Planning retirement evidence (2026-08-11): this umbrella combined acceptance restoration and branch/PR handoff, so it was retired without claiming completion and mapped to `TASK-213` and `TASK-214`.
- **TASK-180:** Reconcile PR #7 with the current `main` branch. DoD: `codex/post-merge-validation` contains the current `origin/main` history, every merge conflict is resolved without losing validated service-client behavior or pending plan work, the full validation suite and GitHub CI pass on the reconciled commit, the updated branch is pushed to the existing sole pull request, and GitHub reports PR #7 mergeable.
  Acceptance evidence (2026-08-09): current `origin/main` was already an ancestor of the branch, so no content conflict or history rewrite was required. Commit `f8695b8` preserved TASK-179, passed formatting, linting, type checking, secret scanning, 300 unit tests, 157 integration tests, OpenAPI/Swagger/migration checks, the instruction-size guard, a zero-vulnerability audit, and the production build. Both exact-head CI runs passed, and GitHub reports PR #7 `MERGEABLE` and `CLEAN`. Readiness confidence: **99/100**; GitHub may recalculate mergeability if `main` changes again before review.
- **TASK-160:** Make the public AittaDB root reliably inspectable by LinkedIn and equivalent standards-compliant link-preview crawlers. DoD: reproduce the crawler-specific `500`, identify and fix the smallest server-owned cause without weakening authentication or trusting `User-Agent`; unauthenticated browser, crawler, HTML, and JSON negotiation retain correct status/content/security behavior; root metadata supplies accurate local preview title/description/image only where supported by the existing public contract; focused regression tests, documentation, formatting, linting, type checking, tests, production build, commit, CI, approved deployment, live `aittadb.com` crawler-equivalent verification, and LinkedIn Post Inspector recheck evidence pass. Current evidence: source commit `d34f4ba` is integrated in deployed Sites version 39 at `ca88110`; indifferent/crawler HTML, explicit JSON, `robots.txt`, and the 1200x630 social image return correct live responses without `User-Agent` branching, and exact-head CI passes. Residual: LinkedIn Post Inspector requires LinkedIn sign-in; after the user signs in, rerun it and record the result.
  Acceptance evidence (2026-08-09): the user confirmed that LinkedIn Post Inspector now accepts `https://aittadb.com/`. Source commit `d34f4ba` remained integrated and deployed; crawler-equivalent HTML, explicit JSON, `robots.txt`, and the local 1200x630 social image had already passed live checks without `User-Agent` branching, and exact-head CI was green. Readiness confidence: **99/100**; LinkedIn controls its own future cache and crawler behavior.
- **TASK-166:** Navigate a successful signed-in HTML file upload to its canonical resource. DoD: session-mode HTML `POST /storage/files` accepts an absolute item `Location` only from the request or configured issuer origin, validates its exact path, and redirects with `303` on the request origin; the redirected `GET` renders exact actions and refresh creates no duplicate; every invalid target retains `201`; JSON/hypermedia, raw bearer, and explicit-token HTML remain `201`; focused origin, fallback, mutation, refresh, CSRF, token-mode, and representation tests plus protocol/docs updates, full validation, commit, CI, approved deployment, and live browser acceptance pass. Evidence: commit `e48fea9` passed full validation and two exact-head CI runs and is deployed in Sites version 43; live `aittadb.com` upload reached one exact canonical item URL, rendered three item actions, retained that URL/actions on reload, and increased namespace usage by exactly one. Residual: delete the three synthetic upload fixtures after action-time confirmation without retaining their keys or contents.
  Acceptance evidence (2026-08-09): after explicit action-time approval, all three TASK-166 upload fixtures and the eight related TASK-144 isolation fixtures were deleted through the real signed-in HTML resource actions. One session retained exactly four older files and no test records; the second retained no files or records. Fresh collection reads omitted every deleted fixture, and no key, content, identity, credential, cookie, or token entered Git or retained evidence. Readiness confidence: **99/100**; ordinary hosted R2 reclamation timing remains platform-managed, while the application metadata and user-visible namespace cleanup are proven.
- **TASK-169:** Create an independent private AittaDB acceptance Site from the exact validated source. DoD: a separate Sites project uses a fresh D1 database, fresh R2 bucket, fresh script-generated ES256 signing key and key ID, isolated non-production configuration, and the same checked-in migrations/build artifact without reusing or changing `aittadb.com` project metadata, data, bindings, identities, signing material, access, or secrets; ignored checkout-local metadata remains outside Git; complete validation, focused operational documentation/evidence, source commit, CI, private deployment, migration application, and live Sites-hostname root, health, discovery, JWKS, signed-in session, record, and file smoke checks pass without retaining private configuration or credentials.
  Acceptance evidence (2026-08-09): exact pushed source `d2cd3137a31c2130febf460c6fde52c0ab7a580b` passed 274 unit tests, 152 integration tests, OpenAPI/Swagger/migration checks, a zero-vulnerability audit, the production build, and both GitHub CI runs. A separate owner-only Sites version 1 deployed at `https://aittadb-acceptance.jaakko-heusala.chatgpt.site` with isolated logical `DB` and `BUCKET` bindings, all eleven migrations, and a fresh ignored mode-0600 script-generated ES256 bundle whose public half matched the live JWKS. Real ChatGPT sign-in created a local subject; root, health, discovery, JWKS, session, empty collections, one D1 record round trip, and one R2 file round trip passed. Both fixtures were deleted and both collections returned empty. No production setting/data, project identifier, private JWK, credential, identity value, storage key, or fixture content entered Git or retained evidence. Readiness confidence: **98/100**; the test custom domain and second-identity policy remain TASK-170 and TASK-171.
- **TASK-165:** Add default-off private telemetry for bounded cleanup without a public diagnostics interface. DoD: cleanup returns fixed categories with D1 counts accepted only from valid mutation metadata, memory parity, caught `waitUntil` rejection, fixed redacted failure data, and optional private category/count/limit success data; no HTTP, hypermedia, OpenAPI, identity, SQL, credential, or secret surface appears; focused failure, metadata, configuration, scheduling, and redaction tests plus configuration/operations/threat documentation, full validation, commit, CI, approved deployment, and private-log acceptance pass. Evidence: commit `612eb1f` passed validation/CI and is deployed in versions 40/41; the flag was enabled only in revision 18, two bounded `/statistics` triggers returned `200`, but the Sites log connector returned no invocation or custom events. Version 41 was restored default-off in revision 19. Residual: use a functioning private Worker log view, repeat one durable trigger, and retain only fixed bounded evidence.
  Acceptance evidence (2026-08-09): the isolated acceptance Site deployed exact validated source with cleanup telemetry enabled only for one bounded private window. Two ordinary durable requests produced `maintenance.cleanup.completed` Worker-log events containing exactly the ten fixed semantic categories, verified counts, and configured limits; no failure event, identity, request value, SQL, credential, secret, or deployment value appeared in the retained evidence. Telemetry was then removed from hosted configuration, the same saved version was redeployed with the next environment revision, and owner-only access plus the default-off state were verified. Readiness confidence: **98/100**; provider log retention and delivery remain platform-managed.
- **TASK-172:** Add a short copy-ready ChatGPT `@Sites` quick-install prompt near the start of `README.md`. DoD: the prompt names the canonical repository, requests a new private AittaDB instance with fresh deployment resources, forbids reusing another instance's data or secrets, links detailed deployment guidance instead of repeating it, passes Markdown formatting and a focused repository-policy test, and ships in focused reviewed commits with CI evidence.
  Acceptance evidence (2026-08-09): `README.md` now presents a one-paragraph `@Sites` prompt immediately after the experimental notice, requests a fresh private instance from the canonical repository, and sends operational detail to the existing deployment guide. A focused repository-policy test protects its placement, private-first posture, fresh-resource boundary, no-reuse rule, and guide link. Formatting, linting, type checking, secret safety, the focused 17-test policy suite, the production build, a zero-vulnerability audit, and both exact-head CI runs `31304575782` and `31304575143` passed. Readiness confidence: **99/100**; future Sites interaction wording may evolve with the public-beta product.
- **TASK-171:** Verify that the independent acceptance Site is safe under public Sites access using two designated signed-in users and an unauthenticated control. DoD: public mode exposes only intended public resources before ChatGPT sign-in; each observed signed-in user receives a distinct immutable local UUID and empty reserved-client namespace; session, storage, account, and administrator resources retain authentication, subject isolation, feature gates, finite quotas, and non-disclosing failures; retained evidence contains no identity, UUID, cookie, credential, private access value, or fixture content; no `aittadb.com` access setting changes. Current evidence: public mode is active; the second user passed real ChatGPT sign-in, received a distinct local UUID, saw neither the owner identity nor owner data, and had empty record/file collections. Residual: verify the unauthenticated public/private route boundary, ordinary-user administrator denial, and two-user isolation without retaining identity values.
  Acceptance evidence (2026-08-09): the independent acceptance Site was intentionally placed in public Sites mode and tested as a public-safe service, not as a deployment whose safety depends on a two-person audience. Anonymous requests reached only public metadata, health, statistics, discovery, JWKS, and OpenAPI resources; session and storage resources rejected anonymous access, account status failed generically with no-store caching, and disabled OAuth administration failed at its feature gate. Two real ChatGPT sign-ins resolved to distinct local subjects and isolated reserved-client namespaces. A synthetic record created by one subject was absent from the other subject's collection, returned a non-disclosing not-found result on direct access, and was then deleted; the creating namespace returned empty. The ordinary user received no administration control. Retained evidence contains only statuses and booleans, and no identity, UUID, cookie, credential, private access value, fixture key, or fixture content. Readiness confidence: **98/100**; public callers remain subject to the implemented authentication, authorization, finite quota, rate-limit, feature-gate, and write-switch boundaries, while provider operation at broader Internet scale remains outside this two-identity acceptance sample.
- **TASK-161:** Suppress every record/file create or replacement control when the deployment storage-write kill switch is disabled. DoD: signed-in collection, existing-item, and missing-item HTML plus versioned hypermedia omit write actions while retaining authorized reads and deletes; signed-out fallback pages do not advertise unavailable writes; direct canonical and browser-adapter write attempts still return the documented non-persisting `503`; focused interface, failure-path, documentation, formatting, linting, type-checking, test, build, commit, CI, and approved-deployment evidence pass. Current evidence: implementation commit `14a365c` is included in disposable Sites version 1 at source `d2cd313`; focused signed-in/signed-out HTML, hypermedia, canonical, adapter, read/delete, and non-persistence tests plus complete validation and exact-head CI pass. Residual: collect TASK-128's approved live switch-off evidence on that private disposable Site without disrupting public `aittadb.com`.
  Acceptance evidence (2026-08-09): the public disposable acceptance Site ran the exact saved source with its non-secret storage-write switch disabled. Fresh signed-in record/file collection and missing-item HTML omitted every create, upload, and replacement control; existing items retained read/download and delete controls. Anonymous HTML likewise advertised no write control and remained no-store. Pre-switch record/file create and replacement forms submitted after a fresh disabled page proved propagation all returned the feature-unavailable response; every attempted new item remained absent, existing record data remained unchanged, and the existing file remained downloadable. Source tests continue to assert exact `503` status and canonical/hypermedia parity. The switch was restored, record and file create/delete round trips passed, and both collections finished empty. Readiness confidence: **99/100**; Sites environment propagation is asynchronous, so acceptance explicitly waited for fresh disabled rendering before mutation probes.
- **TASK-128:** Verify the storage-write kill switch on one exact approved deployment. DoD: enabled switching blocks record/file create and replacement before persistence while reads and deletes retain their documented behavior, restoration re-enables writes, and configuration changes and evidence reveal no secret values. Current baseline: private disposable Sites version 1 at source `d2cd313` and environment revision 5 has writes enabled, active D1/R2 bindings, and no retained test fixture. Residual: after explicit action approval and owner sign-in, set only the non-secret switch false, redeploy the same private saved version, prove existing and missing record/file writes fail before persistence while reads and deletes remain available, remove the override, redeploy, prove writes return, and clean every named fixture without exposing an environment value.
  Acceptance evidence (2026-08-09): only `test.aittadb.com` was exercised. The same saved version was redeployed through reversible non-secret environment revisions while D1/R2 bindings and all secret values remained untouched. With writes disabled, record/file creation and replacement failed without persistence, reads and downloads remained available, and deletion released every fixture. Restoring writes re-enabled fresh record and file controls and successful create/delete round trips. Final hosted configuration has writes enabled, both signed-in collections are empty, temporary local upload files were removed, and retained evidence contains no environment value beyond the public boolean state, identity, credential, cookie, storage key, or fixture content. Readiness confidence: **98/100**; one early probe exposed normal custom-domain propagation lag, was cleaned immediately, and was repeated only after the fresh disabled interface became authoritative.
- **TASK-126:** Verify the per-user aggregate ceiling across two authorized client namespaces on one exact approved deployment. DoD: combined usage enforces the configured limit without revealing either client's contents, deletes release capacity, and evidence records only coarse counts, statuses, and the deployment version.
  Acceptance evidence (2026-08-09): on public disposable `test.aittadb.com` saved version 1, two temporary authorized public-client namespaces began empty under a three-item per-user ceiling. Three writes across both namespaces returned `200`; the next cross-client write returned `507 storage_limit_exceeded`, persisted nothing, and each namespace reported only its own usage. Deleting one item in the first namespace released aggregate capacity and retrying the rejected write in the second namespace returned `200`. Both namespaces were then emptied and reported zero usage. Retained evidence contains only statuses, counts, and booleans; no identity, subject, client identifier, credential, storage key, or content was retained. Readiness confidence: **99/100**; concurrent quota reservation behavior remains covered by automated D1 tests rather than this sequential hosted proof.
- **TASK-144:** Verify record/file isolation across two users and two client namespaces on one approved deployment. DoD: each of four namespaces lists and reads only its own fixtures; foreign reads return non-disclosing `404`; foreign idempotent deletes leave the owner resource intact; foreign cursors fail; shadow writes cannot mutate an owner's resource; cleanup removes every test fixture; retained evidence contains only statuses and booleans. Current evidence: the two signed-in session namespaces passed listing, foreign-read, foreign-cursor, shadow-write, and owner-preservation checks. After explicit approval, all eight fixtures were deleted through signed-in HTML actions; both test namespaces retained no test record or file, and four unrelated older files remained. Residual: obtain two authorized non-reserved client namespaces while OAuth Apps remains disabled by default, then run the four-namespace matrix without exposing credentials or identity data.
  Acceptance evidence (2026-08-09): on public disposable `test.aittadb.com` saved version 1, two real ChatGPT Sites users each authorized the same two temporary public OAuth clients. Separate record and file phases kept the active finite per-user ceiling intact. Every one of the four user/client namespaces listed and read only its own fixtures; all 24 foreign reads returned non-disclosing `404`, both foreign idempotent deletes returned `200` without changing the owner resource, and same-key shadow writes remained isolated. Record and file cursors from one namespace each failed with `400` under the other client and both other-user namespaces. Every temporary record and file was deleted and all four namespaces finished empty. Retained evidence contains only statuses and booleans; no identity, subject, client identifier, credential, cursor, storage key, physical key, or content was retained. Readiness confidence: **99/100**; broader Internet-scale concurrency remains covered by automated atomicity tests rather than this bounded hosted matrix.
- **TASK-127:** Verify the deployment-wide ceiling across two synthetic users on one exact approved deployment. DoD: aggregate capacity is enforced without identity disclosure or cross-user access, failed writes persist nothing, deletes release capacity, and the two-account procedure records no PII or credentials. Current evidence: two distinct trusted Sites sessions are available in separate controllable browser surfaces, and equality was checked without retaining either identity. Residual: obtain explicit approval for a temporary low non-secret deployment ceiling, run the bounded two-session proof, restore configuration immediately, and clean every named fixture.
  Acceptance evidence (2026-08-09): on public disposable `test.aittadb.com` saved version 1, the non-secret global item ceiling was temporarily reduced to three while stricter namespace and per-user boundaries remained distinguishable. Three writes split across two real ChatGPT Sites users returned `200`; the fourth returned `507 storage_limit_exceeded`, persisted nothing, and a cross-user read remained a non-disclosing `404`. Each user saw only its own namespace usage. Deleting one first-user item released global capacity and retrying the second-user write returned `200`. Both namespaces were emptied, reported zero usage, and the original global ceiling was restored in the immediately following successful environment deployment. Retained evidence contains only statuses, counts, and booleans; no identity, subject, client identifier, credential, storage key, or content was retained. Readiness confidence: **99/100**; concurrent global reservation remains covered by automated atomic D1 tests rather than this sequential hosted proof.
- **TASK-135:** Verify bounded D1 cleanup on one exact approved deployment. DoD: seeded expired non-secret fixtures are removed oldest-first within each finite category limit, unexpired and newly created state remains, repeated requests converge, failures remain generic, and no bearer-equivalent value or identity is retained in evidence.
  Acceptance evidence (2026-08-09): public disposable `test.aittadb.com` saved version 1 enabled fixed private cleanup telemetry only for the bounded test window. Three five-second Device Grants plus one older expired setup grant were removed in one hosted pass that reported `4/500` for the device category; every fixed category count was within its finite limit. The exact D1 suite separately proves every cleanup query orders eligible rows oldest-first and enforces the 500-row category boundary. A fresh 60-second grant then survived a real cleanup pass (`0/500`) and remained pending; after expiry, polling returned `invalid_grant` rather than the `expired_token` response of a still-present expired row, and a repeated cleanup pass reported zero, proving convergence. Focused tests retain generic failure telemetry with no exception, request, query, identity, or secret fields. The default Device Grant lifetime and telemetry-off configuration were restored in the immediately following successful environment deployment. No bearer-equivalent value, identity, subject, client identifier, user/device code, or private log body was retained. Readiness confidence: **98/100**; provider log delivery is eventually consistent, while row presence/removal was independently distinguished through the standard token endpoint.
- **TASK-133:** Verify cursor invalidation across an approved signing-key rotation on the same source revision. DoD: after explicit destructive approval, ignored-file key generation and secret update print no private material, old record/file cursors fail, new cursors work, JWKS changes as expected, rollback material is protected, and evidence records no JWK or cursor body. Current evidence: on the disposable acceptance checkout, the protected current bundle validates against the hosted public key ID; repository tooling created fresh rollback and candidate bundles, both validate at mode `0600`, and emitted no private material. Residual: after explicit destructive approval and owner sign-in, create old record/file cursors, update the disposable Site's hosted private key and public key ID together, redeploy the same saved source, prove old cursors fail while new cursors work and JWKS matches, clean fixtures, and roll back immediately if acceptance fails.
  Acceptance evidence (2026-08-09): public disposable `test.aittadb.com` remained on saved version 1 while the repository-generated mode-`0600` candidate private JWK and matching public key ID moved directly from the ignored protected file into the Sites secret interface without private stdout or retained material. Before cutover, the protected rollback bundle matched live JWKS and one record plus one file cursor were created under the old key with their fixtures immediately deleted. After the successful environment deployment, the candidate matched JWKS and the rollback correctly did not. An unexpired old-key access JWT returned `401`; its opaque refresh token rotated successfully into an ES256 JWT with the candidate key. The old record and file cursors each returned `400 invalid_request`, while newly generated record and file cursors each traversed with `200`. All cursor fixtures were deleted and both collections finished empty. The rollback bundle remains ignored and protected for the approved test rollback window. No JWK, key ID, cursor body, token, identity, subject, client identifier, storage key, or content was retained. Readiness confidence: **99/100**; this is an abrupt single-key test cutover, and rollback would intentionally invalidate credentials created under the candidate.
- **TASK-173:** Add fixed private failure-phase telemetry to the bounded account-deletion coordinator. DoD: an internally caught failure identifies only `fences`, `credentials`, `records`, `files`, or `finalization`; the HTTP nudge emits one constant private error event with that enum and no subject, identity, request, SQL, object key, exception, credential, or secret; success emits nothing; injected phase, redaction, handler-scheduling, documentation, formatting, linting, type-checking, test, build, commit, CI, disposable deployment, and private-log evidence pass.
  Acceptance evidence (2026-08-09): commit `1fe360f` added the fixed five-phase observer and a constant private handler event whose production-shaped tests prove success silence, observer isolation, scheduling, and redaction. The exact source passed complete validation and CI, then disposable Sites version 2 emitted only the allowed `credentials` phase while the deletion retried; no identity, request value, SQL, object key, exception, credential, or secret entered retained evidence. Readiness confidence: **98/100**; provider log delivery and retention remain platform-managed.
- **TASK-174:** Add fixed private deferred-phase telemetry to the bounded account-deletion coordinator. DoD: a nonterminal pass identifies only the first incomplete `credentials`, `records`, `files`, or `finalization` phase; the HTTP nudge emits one constant private event with that enum and no subject, identity, request, count, SQL, object key, exception, credential, configuration, or secret; completed/no-job passes emit nothing; observer failure cannot change durable progress; focused phase, redaction, scheduling, documentation, formatting, linting, type-checking, tests, build, commit, CI, disposable deployment, and private-log evidence pass.
  Acceptance evidence (2026-08-09): commit `1de8d91` added the fixed four-phase deferred observer with production-shaped positive coverage for every phase, completed/no-job silence, scheduling, redaction, and observer-failure isolation. Complete validation and CI passed, and the exact source was deployed to the disposable Site. The live failing pass correctly emitted no deferred event; after the D1 compatibility repair the bounded deletion converged without a retained nonterminal state. Readiness confidence: **96/100**; positive deferred delivery is proven by the exact handler tests rather than by manufacturing an oversized hosted deletion fixture.
- **TASK-175:** Add fixed private D1 credential-purge failure-subphase telemetry. DoD: a caught credential purge failure identifies only `job`, `authorization-codes`, `authorization-requests`, `device-grants`, `consents`, `refresh-tokens`, `refresh-families`, `access-revocations`, `admin-submissions`, or `remaining-check`; one constant private event contains only that enum and no subject, identity, request, count, SQL, object key, exception, credential, configuration, or secret; success emits nothing and observer failure cannot alter purge behavior; focused subphase, redaction, D1 adapter, handler wiring, documentation, formatting, linting, type-checking, tests, build, commit, CI, disposable deployment, and private-log evidence pass.
  Acceptance evidence (2026-08-09): commit `7a81115` added the fixed ten-subphase repository observer, generic adapter-error replacement, constant private handler event, success silence, and observer-failure isolation. Complete validation and CI passed; disposable Sites version 4 narrowed the live credential failure to `remaining-check` without retaining an exception, query, identity, credential, or deployment value. Readiness confidence: **98/100**; the event is intentionally coarse and provider log delivery remains platform-managed.
- **TASK-176:** Make the D1 credential-purge remaining check Sites-compatible. DoD: replace the live-failing compound `UNION ALL` check with one prepared bounded `SELECT ... WHERE EXISTS` predicate that preserves exact subject isolation and every owned credential/grant table; empty and nonempty state, deterministic batch continuation, a D1 adapter that rejects the old compound form, failure normalization, documentation, formatting, linting, type-checking, tests, build, commit, CI, disposable deployment, and live deletion convergence evidence pass.
  Acceptance evidence (2026-08-09): commit `15033f2` replaced the rejected compound query with one prepared subject-bound `SELECT ... WHERE EXISTS` statement covering every owned credential and grant table. The regression adapter rejects `UNION ALL`; empty, populated, bounded continuation, failure-normalization, and coordinator suites passed. Complete validation passed 298 unit and 155 integration tests, OpenAPI/Swagger/migration checks, the instruction budget, a zero-vulnerability audit, and the production build; both exact-head CI checks passed. Disposable Sites version 5 then advanced the previously stuck deletion from credential retry to completion. Readiness confidence: **99/100**; future owned credential tables must be added to this explicit predicate and remain migration/test reviewed.
- **TASK-155:** Verify whole-account deletion with one synthetic non-administrator on one exact approved deployment after explicit destructive confirmation. DoD: deletion reaches completed state, prior credentials and data fail, re-sign-in creates a different UUID and empty namespace, one control account remains unchanged, and evidence contains no UUID, email, credential, file key, or deleted content. Current evidence: one distinct signed-in session exposes the non-administrator deletion control and a second distinct signed-in session is available as the unchanged control; no identity value was retained. Residual: confirm that the eligible account is disposable, obtain explicit destructive approval for that exact account, seed only named fixtures, complete deletion and re-sign-in proof, and retain no identity or credential evidence.
  Acceptance evidence (2026-08-09): after explicit approval, public disposable `test.aittadb.com` deleted one ordinary local account and reached the protected `completed` state. Its prior access and refresh credentials were rejected; its record and file were absent. A separate control account refreshed successfully and retained exactly one record and one file. Re-sign-in created a different immutable local subject with empty signed-in record and file namespaces. The replacement test identity was then deleted, control fixtures were removed, temporary OAuth grants were revoked and clients disabled, OAuth Apps and administrator access returned to default-off/closed, quota overrides were removed, and local credential/script/archive artifacts were erased. No UUID, email, credential, cookie, key, content, secret, or deployment identifier entered Git or retained evidence. Readiness confidence: **99/100**; Sites identity assurance and email reassignment remain the documented upstream platform boundary.
- **TASK-177:** Allow the canonical unfinished-work queue to reach zero. DoD: an empty `PLAN.md` queue remains a valid explicit state while checked tasks, malformed active tasks, and missing changelog history remain rejected; compact workflow guidance, focused repository-policy tests, formatting, linting, type-checking, full tests, build, commit, and CI pass before this task moves unchanged to `CHANGELOG.md`.
  Acceptance evidence (2026-08-09): commit `32f3f0f` made the unfinished queue explicitly optional while retaining its heading, unchecked-only rule, bounded DoD checks for every present task, and unique sequential history across PLAN and CHANGELOG. `AGENTS.md` remains below 32,000 bytes. Complete validation passed 298 unit and 155 integration tests, secret, OpenAPI, Swagger, migration, audit, and production-build gates; exact-head GitHub Actions runs `31322203542` and `31322205553` passed before archival. Readiness confidence: **99/100**; future repository work must still enter PLAN before implementation and cannot leave malformed or completed items there.

### Retired planning umbrellas

These entries were removed because their scopes could not finish independently. Their preservation here does not claim that the underlying product work is complete; each retirement mapping identifies the focused current-plan, roadmap, or backlog replacements.

- **TASK-045:** Make every storage resource endpoint available as a content-negotiated web UI without overloading unrelated URLs, including collection `GET` interfaces that combine the current list or empty state with item navigation and a real upload form; canonical `POST /storage/files` creation with a server-generated logical key, `201 Created`, and exact item `Location`; an accessible progressively enhanced drag-and-drop target that populates the native file control without automatic upload and preserves complete keyboard/no-JavaScript form use; caller-keyed idempotent create-or-replace/update through `PUT /storage/files/{key}`; an explicit collection affordance that tells users how to open or create a record and open or upload a file; item `GET` interfaces that clearly expose the complete valid lifecycle for that exact `/{key}` URL, including read/download, create/upload when missing, replace/update when present, and delete when present; CSRF-protected browser actions on the owning resource URL with an explicit canonical-method override where adaptation is required; no operation selector; progressive same-origin item navigation with a complete query-and-redirect no-JavaScript fallback; hidden and disabled fields for inactive authentication modes and OAuth grant types; visibility-linked required validation; unchanged server-side authorization, scope, origin, size, ownership, and storage validation; automated API/browser creation, generated-key, `Location`, upload form/drop-zone keyboard and script behavior, form-markup, upload/update-path visibility, dynamic lifecycle-state, canonical/fallback routing, CSP, public-request, and failure-path tests; OpenAPI, README/style-guide/architecture/`AGENTS.md`/changelog updates; and acceptance evidence from formatting, linting, type checking, tests, OpenAPI/Swagger/migration checks, audit, production build, focused commits, GitHub CI, approved Sites deployment, and live browser verification. Current residual: reproduce and fix the hosted signed-in collection upload regression where the current `/storage/files` form returns `403 CSRF validation failed`, without weakening CSRF or same-origin enforcement; cover fresh, stale, concurrent-tab, sign-in-return, multipart, and retry/recovery behavior in automated tests and live acceptance; then obtain explicit destructive-action confirmation to delete and verify cleanup of the synthetic `acceptance/task-045-20260808` record.
  Retirement mapping: Replaced by TASK-115 through TASK-117.
- **TASK-056:** Provide content-negotiated hypermedia administration for the allowlisted OAuth-client collection, including equivalent list/create/enable/disable/secret-rotation/grant-revocation HTML and JSON controls; server-side Sites identity and immutable local-UUID authorization through `ADMIN_SUBJECTS`; dynamic action omission by client state/type; one-time secret display with no persistence in later representations; CSRF and same-origin controls represented without weakening enforcement; automated admin/non-admin/anonymous/state/secret/failure tests; route OpenAPI, architecture, threat-model, style-guide, changelog, and compact `AGENTS.md` updates; and acceptance evidence from complete validation, focused commit, GitHub CI, approved Sites deployment, and live administrator verification. Current residual: deploy the exact validated commit through the separately approved Sites workflow, then verify the full live allowlisted-administrator lifecycle: HTML and JSON list/create controls; reload-safe creation, enablement, disablement, secret rotation, and grant revocation; state/type-dependent action omission; client-bound secret display exactly once; refresh and stale-result non-repetition; and non-admin/anonymous non-disclosure. Live discovery addressed in source: refreshing the former successful confidential-client creation response repeated the POST, created a duplicate client, and issued another secret.
  Retirement mapping: Replaced by TASK-107 and BACKLOG-008 through BACKLOG-010.
- **TASK-057:** Represent the internal device-entry, device-decision, and authorization-consent browser transaction resources as equivalent HTML and hypermedia JSON where Sites authentication permits, including current request state, client name, local scopes, user code, approve/deny controls, CSRF fields, same-origin continuation, no external `return_to`, no credentials or authorization codes in URLs beyond standard browser redirects, dynamic omission after terminal/expired state, automated signed-in/signed-out/pending/expired/approved/denied/CSRF/content-negotiation tests, internal-route OpenAPI and architecture/threat-model/style-guide/changelog/compact `AGENTS.md` updates, and acceptance evidence from complete validation, focused commit, GitHub CI, approved Sites deployment, and live browser verification. Current residual: the exact deployed revision was traversed live through pending, `slow_down`, approved, used, and denied states; approved, used, and denied terminal representations omitted mutation actions and did not reflect user or device codes. Still add explicit automated expired and used representation coverage, cover hostile external `return_to` values, and traverse expiration live.
  Retirement mapping: Replaced by TASK-108 and BACKLOG-011.
- **TASK-064:** Make authorization requests terminal and non-replayable across remembered-consent `GET`, approval, and denial paths, including an explicit atomic pending-to-approved/denied repository transition; no second authorization code from repeated or concurrent browser actions; safe standard errors without redirect manipulation; D1 and memory implementations, migration/schema changes only if required, sequential/concurrent negative tests, OAuth/OpenAPI/architecture/threat-model/`AGENTS.md` updates, and complete validation, commit, CI, approved deployment, and live acceptance evidence. Current residual: complete remembered-consent `GET` replay coverage, production-shaped D1 concurrency proof, and live replay/race acceptance; isolated implementation work is active.
  Retirement mapping: Replaced by BACKLOG-013.
- **TASK-066:** Make one-time authorization-code, approved device-grant, and refresh-token consumption atomic in D1 and memory repositories, including compare-and-set transitions whose affected-row result gates success; deterministic concurrent loser behavior; refresh-family reuse detection without allowing duplicate rotated tokens; repository contract updates, concurrency/failure tests, migration changes only if necessary, architecture/threat-model/`AGENTS.md` updates, and complete validation, commit, CI, approved deployment, and live acceptance evidence. Current residual: the exact deployed revision rejected sequential authorization-code replay, consumed one approved device grant exactly once and rejected its replay, rotated a refresh token once, rejected reuse of its predecessor, and then rejected the successor from the revoked family. Still prove a concurrent end-to-end refresh race yields one successor, exercise actual production D1 concurrency for each one-time credential, and perform concurrent hosted authorization-code, device-grant, and refresh-token race checks.
  Retirement mapping: Replaced by BACKLOG-014 through BACKLOG-016.
- **TASK-067:** Implement standards-compatible revocation for refresh tokens with omitted or incorrect hints and for AittaDB JWT access tokens by `jti`, including confidential/public client binding where applicable; non-disclosing success responses; immediate UserInfo, introspection, and storage rejection after access-token revocation; hypermedia/form accuracy; repository and protocol tests, OpenAPI/examples/security documentation updates, and complete validation, commit, CI, approved deployment, and live acceptance evidence. Current residual: the exact deployed revision accepted confidential access-token revocation and immediately made introspection inactive while UserInfo and storage rejected the token; it also accepted public refresh-token revocation despite an incorrect `access_token` hint and blocked refresh; still verify omitted-hint behavior live and complete the public/confidential owning-client and wrong-client binding matrix without exposing credentials.
  Retirement mapping: Replaced by BACKLOG-017 and BACKLOG-018.
- **TASK-069:** Enforce the aggregate multipart browser-upload bound even when `Content-Length` is missing, false, or smaller than the streamed body, including bounded Worker-compatible body reading before `FormData` parsing; preservation of the native multipart interface and canonical 10 MiB file limit; `413` failure without persistence or credential leakage; declared/undeclared/padded/malformed tests, threat-model/OpenAPI/`AGENTS.md` updates, and complete validation, commit, CI, approved deployment, and live acceptance evidence. Current residual: automated declared, missing, false, undersized, and malformed cases pass; collect live streamed oversized multipart evidence without persisting bytes or leaking credentials.
  Retirement mapping: Replaced by TASK-118.
- **TASK-070:** Make device verification resources expose approve/deny controls only while a grant is pending, including terminal approved, denied, used, and expired representations with no state-changing actions; server-side terminal rejection retained independently of representation; equivalent HTML/hypermedia status and recovery links; focused transition/replay tests, OpenAPI/architecture/style/threat documentation updates, and complete validation, commit, CI, approved deployment, and live acceptance evidence. Current residual: the exact deployed revision omitted approve/deny controls after approval and on denied and used re-entry, while server-side polling rejected both denied and already-consumed grants without reflecting their codes. Still represent expiration explicitly in the device-grant status contract, add focused terminal action-omission tests for every approved/denied/used/expired state, and verify expired re-entry live.
  Retirement mapping: Replaced by BACKLOG-011.
- **TASK-071:** Make R2 bytes and D1 file metadata failure-consistent for create, replacement, and deletion, including compensation or ordering that cannot leave new orphan objects, stale metadata pointing to deleted bytes, or replacement bytes with old digest/type; bounded cleanup compatibility; injected D1/R2 failure tests; preserved user/client isolation and non-disclosure; architecture/threat-model/`AGENTS.md` updates, and complete validation, commit, CI, approved deployment, and live acceptance evidence. Current residual: add durable bounded orphan-repair state for simultaneous persistent D1 and R2 compensation failure, prove repair convergence under injected dual failures, and collect safe hosted failure evidence.
  Retirement mapping: Replaced by TASK-119 and TASK-120.
- **TASK-072:** Require an independently verified same-origin signal for every browser state-changing form, rejecting a missing `Origin` as well as foreign, malformed, and cross-site null origins while retaining the documented Sites same-origin dispatch case; keep CSRF as a separate mandatory control; add all route-family positive/negative tests, OpenAPI/threat-model/`AGENTS.md` updates, and complete validation, commit, CI, approved deployment, and live acceptance evidence. Current residual: cover malformed origins and every mutation route family, then verify hosted canonical-origin, Sites-dispatch null-origin, missing-origin, and cross-site rejection behavior.
  Retirement mapping: Replaced by TASK-121 through TASK-123.
- **TASK-075:** Add fail-closed AittaDB storage capacity controls, including an operational write kill switch; configurable deployment-wide, per-user, and per-user/client item and byte ceilings with secure finite defaults independent of undocumented Sites limits; concurrency-safe D1 enforcement for record and file create/replace/delete accounting; correct R2 compensation when a file reservation or metadata write is rejected; privacy-preserving usage and quota errors; administrator and user visibility limited to authorized aggregates; schema/migration/configuration/OpenAPI/hypermedia/HTML/architecture/threat-model/operations/`AGENTS.md` updates; positive, boundary, concurrent, bypass, disabled-write, and cross-tenant tests; and acceptance evidence from full validation, focused commit, CI, approved deployment, and live abuse checks. Current residual: define authorized administrator aggregate visibility, close the dual-failure repair dependency in TASK-071, and verify deployment/user/client ceilings plus the kill switch under live abuse probes.
  Retirement mapping: Replaced by TASK-124 through TASK-128 and ROADMAP-025.
- **TASK-076:** Bound storage collection reads, including stable cursor-based pagination for record and file lists; a conservative maximum/default page size; opaque cursor validation bound to the authenticated user and client namespace; deterministic ordering; no total enumeration or unbounded HTML/JSON response; next/previous recovery controls where valid; unchanged item authorization; OpenAPI/hypermedia/HTML/client example/documentation updates; success, malformed-cursor, boundary, cross-namespace, and large-fixture tests; and acceptance evidence from full validation, focused commit, CI, approved deployment, and live browser/API checks. Current residual: implement valid previous-page recovery, add large-fixture automated coverage, and verify bounded forward/backward HTML and JSON traversal live.
  Retirement mapping: Replaced by TASK-129 through TASK-132, ROADMAP-003, and ROADMAP-006.
- **TASK-077:** Prevent anonymous and authenticated database-write abuse in OAuth and storage paths, including atomic D1 rate counters for authorization-request creation, device authorization and polling, token/client authentication, revocation, administration, storage reads, and storage writes; bounded cleanup of expired authorization requests, authorization codes, device grants, refresh tokens and families, revoked JWT identifiers, audit events, and rate counters; cleanup indexes and deterministic deletion limits; no refresh-family security regression; generic rate/cleanup failures; migration/OpenAPI/architecture/threat-model/operations/`AGENTS.md` updates; race, expiry, retry, cleanup-bound, and endpoint-family tests; and acceptance evidence from full validation, focused commit, CI, approved deployment, and live abuse checks. Current residual: deploy the exact validated commit with approval and perform live endpoint-family abuse, rate-recovery, and bounded-cleanup checks in the hosted D1 environment.
  Retirement mapping: Replaced by TASK-134, TASK-135, and BACKLOG-019.
- **TASK-078:** Close the remaining HTTP and client-policy gaps from the security review, including streaming request-size enforcement before unbounded buffering for every accepted body type; exact per-client CORS-origin enforcement where browser bearer use is allowed; active-client checks at every token consumer; HSTS on HTTPS production responses without weakening local HTTP development; retained CSP/cache/cookie/origin/CSRF controls; OpenAPI/security/architecture/threat-model/`AGENTS.md` updates; focused positive and negative tests; and acceptance evidence from full validation, focused commit, CI, approved deployment, and live header/CORS/body checks. Current residual: prove pre-buffer streaming bounds for every accepted body type, prove disabled-client rejection at every token consumer, and complete hosted header/CORS/body acceptance.
  Retirement mapping: Replaced by TASK-136 through TASK-140 and BACKLOG-020.
- **TASK-080:** Reconcile the external security report against the exact current revision and complete hosted security acceptance, including regression proof for already-fixed atomic authorization/code/device/refresh transitions, client-bound access/refresh revocation, JWT purpose checks, R2/D1 compensation, request bounds, disabled-client rejection, tenant isolation, and zero high-severity dependency audit; document the upstream email reassignment residual risk for ordinary users; perform an authorized two-account, two-client live isolation test without exposing either identity; verify storage ceilings, pagination, rate limits, cleanup behavior, kill switch, HSTS, CORS, and forged-Sites-header rejection on the deployed commit; record any platform-limited evidence honestly; update SECURITY/README/changelog/`AGENTS.md`; and complete full validation, focused commits, GitHub CI, approved Sites deployment, and an auditable final PLAN state. Current residual: map every check to one exact deployed commit, complete the two-user by two-client live storage-isolation matrix after the second browser profile is signed in, and execute the listed hosted controls without exposing either identity.
  Retirement mapping: Replaced by TASK-125 through TASK-140, TASK-144, TASK-145, and BACKLOG-017 through BACKLOG-021 as applicable.
- **TASK-082:** Remove the short device user-code display value from durable storage while preserving RFC 8628 responses and signed-in consent UX, including hash-only lookup, reconstruction solely from the matching same-origin browser submission, migration scrubbing of existing display values, no code in logs, D1 and memory behavior, threat-model/schema/`AGENTS.md` accuracy, positive and invalid-code tests, and acceptance evidence from formatting, checks, full validation, focused commit, CI, approved deployment, and live device-flow verification. Current residual: the exact deployed revision completed approved, denied, replay, and `slow_down` device paths live without reflecting device or user codes in terminal or error responses. Still remove `userCodeDisplay` entirely from repository contracts so arbitrary values cannot re-enter durable adapters, add explicit invalid-code/no-log evidence, and verify the production D1 scrub after deployment.
  Retirement mapping: Replaced by BACKLOG-012.
- **TASK-083:** Make R2 file mutation concurrency-safe across create, replace, and delete, including an explicit repository compare-and-set contract tied to the expected physical object key; conditional D1 metadata insertion, replacement, deletion, and rollback; deterministic `409` loser behavior without leaking physical keys; bounded deletion of newly written losing objects and failed-operation compensation that cannot overwrite a newer winner; concurrent first-write, replacement, replace/delete, and injected D1/R2 failure tests proving that every retained metadata row names one existing R2 object and that no losing object remains; architecture, threat-model, OpenAPI, `AGENTS.md`, and changelog updates; and acceptance evidence from full validation, focused commit, CI, approved deployment, and live conflict checks. Current residual: exercise production-shaped D1 concurrency, close the durable orphan-repair dependency in TASK-071, and perform live first-write/replace/delete conflict checks.
  Retirement mapping: Replaced by TASK-119, TASK-120, and TASK-141 through TASK-143.
- **TASK-084:** Make bounded OAuth cleanup race-safe and index-supported, including a grace period that prevents cleanup from deleting a newly created refresh-token family before its first token is inserted; expiration and relationship indexes used by authorization-request, authorization-code, refresh-token, and family cleanup; deterministic per-category deletion limits; migration and memory-store parity; interleaved creation/cleanup, expiry-boundary, orphan-family, and query-plan-oriented migration tests; architecture, threat-model, operations, schema, `AGENTS.md`, and changelog updates; and acceptance evidence from full validation, focused commit, CI, approved deployment, and bounded live cleanup evidence. Current residual: bound memory-store cleanup consistently, complete interleaved race and query-plan assertions, and collect bounded hosted cleanup evidence.
  Retirement mapping: Replaced by BACKLOG-019.
- **TASK-085:** Authenticate browser multipart uploads before parsing their potentially large bodies and bind browser token-endpoint CORS to the submitted active OAuth client, including same-origin signed-in gating before `FormData` buffering while preserving canonical bearer-authenticated raw file APIs; exact registered-origin preflight and actual-response behavior for `/oauth/token`; rejection before authorization-code, device-code, or refresh-token consumption when the origin is not allowed for that client; no wildcard credentialed CORS; malformed, anonymous-stream, allowed-origin, foreign-origin, disabled-client, and non-consumption tests; OpenAPI, architecture, threat-model, browser guidance, `AGENTS.md`, and changelog updates; and acceptance evidence from full validation, focused commit, CI, approved deployment, and live browser/CORS checks. Current residual: prove foreign-origin rejection does not consume device or refresh credentials, cover disabled-client token CORS, prove anonymous upload streams are not pulled, and complete hosted browser/CORS checks.
  Retirement mapping: Replaced by TASK-138 and BACKLOG-020.
- **TASK-086:** Make storage pagination cursors confidential as well as tamper-resistant, including a versioned Web Crypto authenticated-encryption format derived from deployment secret material and bound to resource kind, immutable user UUID, and OAuth client namespace; no logical key, timestamp, identity, client, signing material, or secret recoverable from the token; strict canonical decoding and bounded malformed-input rejection; documented key-rotation invalidation behavior; round-trip, cross-namespace, cross-kind, tamper, truncation, non-canonical, and plaintext-disclosure tests; OpenAPI, architecture, threat-model, `AGENTS.md`, and changelog updates; and acceptance evidence from full validation, focused commit, CI, approved deployment, and live pagination checks. Current residual: non-hosted token/envelope disclosure scans and private signing-key rotation invalidation tests pass; integrate and deploy the exact validated commit, then verify encrypted record/file pagination and post-rotation rejection live without exposing namespace or secret values.
  Retirement mapping: Replaced by TASK-131 through TASK-133.
- **TASK-099:** Add a self-service whole-account deletion primitive for the current trusted ChatGPT Sites session, including one canonical content-negotiated account resource whose HTML confirmation form and versioned hypermedia action invoke the same authenticated operation; explicit destructive confirmation, same-origin and CSRF enforcement, generic unauthorized failures, and rejection while the immutable local subject remains configured in `ADMIN_SUBJECTS`; immediate invalidation of that user's AittaDB access, refresh, browser-session, authorization, device, consent, and storage access without deleting deployment-wide OAuth clients, configuration, signing material, secrets, or other users' data; bounded retry-safe deletion of the user's D1 records, file metadata, physical R2 objects, local identity, grants, and bearer-equivalent hashes with observable pending/completed/failed state that cannot strand reachable data or exceed Worker execution bounds; documented minimal security/audit retention and PII removal; a later ChatGPT sign-in creating a new local UUID and empty namespace rather than restoring the deleted account; schema/migration, repository, cleanup, OpenAPI, privacy/security/threat-model/architecture/deployment/README/`AGENTS.md` updates; automated identity-isolation, token-invalidation, cross-client, R2/D1 partial-failure, replay, CSRF/origin, administrator, re-registration, HTML/hypermedia parity, pagination/batch-bound, migration, and no-secret-disclosure tests; and acceptance evidence from full validation, focused commit, GitHub CI, approved Sites deployment, and live deletion of a synthetic non-administrator account with explicit user confirmation before the destructive hosted step.
  Retirement mapping: Replaced by TASK-146 through TASK-155.
- **TASK-101:** Restore a concise, prominent explanation of ChatGPT sign-in on the browser home page without recreating the removed repetitive prose: state that ChatGPT provides browser sign-in inside ChatGPT Sites, AittaDB maps the resulting trusted identity signal to a separate local identity and issues its own credentials, and ChatGPT credentials are never received or forwarded by AittaDB; retain the compact FSL-1.1-MIT trust note and README details CTA; avoid language implying an official ChatGPT OAuth service or AittaDB-issued ChatGPT/OpenAI credentials; update focused HTML-copy and repository-policy/style tests or guidance where needed; and record acceptance evidence from formatting, linting, type checking, relevant tests, build, focused commit, GitHub CI, approved Sites deployment, and live signed-in/signed-out HTML verification.
  Retirement mapping: Replaced by TASK-113 and TASK-114.
- **TASK-093:** Reconcile every currently open PLAN entry against the exact deployed and tested repository state using parallel evidence audits; archive an entry in `CHANGELOG.md` only when its complete integrated contract, negative paths, documentation, full validation, CI, deployment, and required live evidence are already satisfied; keep genuinely incomplete entries in dependency order and amend them only to state precise residual work or external blockers without discarding their original scope; execute the highest-priority unblocked residual work immediately rather than stopping at classification; verify the resulting queue and completed archive with repository-policy checks, full validation, focused commits, branch pushes, green CI, and approved deployment when runtime source changes.
  Retirement mapping: Replaced by TASK-102.

### Completed plan tasks

Completed tasks move here from `PLAN.md` with their stable identifiers and full delivery contracts so the active plan remains an unfinished-work queue.

- **TASK-170:** Bind the independent acceptance Site to `https://test.aittadb.com`. DoD: only that subdomain is attached; its exact origin drives issuer/discovery/JWKS metadata, absolute controls, browser security, social metadata, and signed-in storage; the platform hostname stays noncanonical; validation, CI, private deployment, and live HTML/JSON acceptance pass without private evidence. Current evidence: domain, provider route, TLS, and public CNAME are active; environment revision 5 uses the test issuer; the saved version redeployed owner-only; real ChatGPT sign-in reached the custom-domain root with exact issuer and sign-out controls; anonymous HTTPS returns the expected no-store sign-in gate. Residual: finish signed-in JSON/discovery/JWKS, CSRF/origin, social, and record/file checks through a controllable signed-in browser, or explicitly authorize creating a disposable bypass credential for this test Site; retain no identity or credential.
  Acceptance evidence (2026-08-09): a real signed-in Sites session on the custom origin passed the HTML home and session views, exact canonical and Open Graph URLs, versioned root hypermedia, signed-in session JSON, disabled-OAuth discovery, public-only single-key ES256 JWKS, and the canonical OpenAPI 3.1 document with 43 live operations and only the test origin as its server. Responses retained no-store, CSP, HSTS, clickjacking, MIME-sniffing, permissions, and referrer protections. A valid same-origin/CSRF form created and read one synthetic D1 record; an invalid CSRF request and a CDP-modified foreign-origin request each returned the expected non-persisting `403`, with origin rejected before form processing. A 47-byte synthetic file uploaded through the real browser form, rendered at its exact resource, and downloaded through R2. The file and record were deleted, both signed-in collections returned empty, no legacy hostname appeared in retained metadata, and no identity, credential, cookie, private key, cursor, or storage key entered Git evidence. Readiness confidence: **99/100**; future availability of the custom domain and Sites edge remains platform-managed.
- **TASK-168:** Keep the operational thread handoff local in the public repository checkout. DoD: re-review the existing handoff for credential, secret, private deployment, PII, identity, resource-key, cursor, and cookie exposure; retain a useful local copy updated to `/Users/jhh/git/aittadb/aittadb`; remove `HANDOFF.md` from the tracked branch tip and add its root path to the ignore policy without rewriting public history; preserve the completed TASK-167 audit record with an explicit no-secret/history note; formatting, AGENTS budget, tracked-tree secret scan, history scan, and Git diff checks pass; archive this unchanged task in `CHANGELOG.md`, commit, push, retain the sole draft PR, and report that force-push history removal still requires separate explicit approval.
  Acceptance evidence (2026-08-09): the relocated checkout at `/Users/jhh/git/aittadb/aittadb` was clean on pushed commit `716355e`, tracked `codex/post-merge-validation`, used the canonical `git@github.com:aittadb/aittadb.git` remote, and the former local path was absent. The handoff's only active path reference was updated locally, `/HANDOFF.md` is ignored, and the staged branch deletion preserves that 22,668-byte local file. The public historical copy was re-reviewed with sensitive-shape, 185-file tracked-tree, and complete reachable-history scans; none found credentials, secrets, private deployment identifiers, emails, UUIDs, identity values, resource keys, cursors, cookies, private JWKs, or environment values. Prettier, the 31,780-byte AGENTS guard, working/staged diff checks, and the unchanged 11-task PLAN count passed. The TASK-167 evidence now records the public-history residual explicitly. No history was rewritten or force-pushed; removing commit `716355e` from public branch history remains separately approval-gated. Readiness confidence: **99/100**; the only residual is the intentionally retained, secret-free historical copy.
- **TASK-167:** Create a sanitized continuation handoff for the active implementation branch. DoD: root `HANDOFF.md` records the canonical repository/branch/PR and deployment state, completed evidence, every open PLAN item and exact residual, pending approvals, safe browser/fixture context, validation and continuation commands, and the next action sequence without credentials, private deployment material, PII, resource keys, cursors, or token values; documentation formatting, agent-budget, tracked-tree secret scan, and Git-state checks pass; the completed task is archived unchanged in `CHANGELOG.md`, committed, and pushed to the sole draft-PR branch with evidence and readiness confidence.
  Acceptance evidence (2026-08-09): root `HANDOFF.md` records the canonical checkout, sole integration branch and draft PR, exact pre-handoff and deployed-source relationship, current Sites version and non-secret feature state, implemented architecture, local/CI validation, all 11 remaining PLAN tasks and residuals, safe recovery of 11 synthetic fixtures without their keys, every approval gate, a dependency-ordered continuation sequence, and security/Git reminders. The 22,668-byte handoff contains no email address, UUID, Sites project identifier, resource key, credential, cursor, cookie, private JWK, or environment value. Pre-archive checks passed Prettier formatting, the 31,780-byte AGENTS guard, the tracked-tree secret scan for 185 files, sensitive-shape review, PLAN-to-handoff task coverage, and `git diff --check`; final staged-tree checks and the focused push provide the remaining delivery evidence. Continuation readiness confidence: **97/100**; browser sessions may not transfer, LinkedIn still needs sign-in, private Sites logs remain unavailable, and hosted destructive/configuration proofs remain correctly gated by fresh approval.
  Follow-up (2026-08-09): TASK-168 makes the operational handoff checkout-local after the user questioned publishing it. Commit `716355e` remains in the public draft-branch history unless a separately approved rewrite removes it; its staged-tree and history scans found no secret, credential, private deployment identifier, PII, identity, resource key, cursor, cookie, private JWK, or environment value.
- **TASK-162:** Make local signing-key generation and rotation preparation non-disclosing and rollback-safe. DoD: no supported command prints a private JWK; generated candidates use unique ignored mode-0600 files and fail closed instead of overwriting; an operator command validates required public/private members, key consistency, and an in-memory ES256 sign/verify round trip; a protected current-key/JWKS boolean-match preflight and explicit rollback procedure cover the single-key cutover plus cursor, token, account-deletion-handle, administrator-result, and rate-counter consequences; malformed, missing, clobber, disclosure, and preflight failure tests plus key-rotation documentation, formatting, linting, type checking, tests, build, commit, and CI pass without reading or committing a live secret.
  Acceptance evidence (2026-08-09): integrated commit `be378a6344614073752fc7ba53dae28468e67d9c` replaces the stdout-capable generator with fixed-purpose generate, validate, prepare-rotation, and public-JWKS preflight commands. Candidates and rollback copies use exclusive non-clobber writes under the ignored `.secrets/signing-keys/` directory, exact file mode `0600`, protected directory mode `0700`, bounded input, symlink rejection, full P-256 member/key-ID/public-private validation, and an in-memory ES256 sign/verify round trip. CLI success and failure output contains only paths, public key IDs, `valid`/`invalid`, or `match`/`non-match`; ten focused tests cover malformed, missing, permission, symlink, collision, cleanup, ignored-output, disclosure, mismatch, and preflight failures. `docs/key-rotation.md` documents the abrupt single-key cutover, rollback, and effects on JWTs, refresh tokens, authorization/device credentials, cursors, deletion handles, administrator results, rate counters, and D1/R2 data. Complete validation passed formatting, linting, strict types, the 183-file secret scan, 263 unit tests, 152 integration tests, OpenAPI parity for 43 operations, three pinned Swagger files, 20 tables across 11 packaged migrations, the 31,624-byte `AGENTS.md` guard, a zero-vulnerability audit, and the production build. Exact GitHub CI runs [`31283840656`](https://github.com/aittadb/aittadb/actions/runs/31283840656) and [`31283842275`](https://github.com/aittadb/aittadb/actions/runs/31283842275) passed. No live secret was read, no operational key was generated, and nothing was rotated or deployed; live cutover acceptance remains TASK-133. Readiness confidence: **96/100**.
- **TASK-164:** Remove stale local Git worktrees and `codex/` feature branches without losing work. DoD: inventory every local worktree and local/remote `codex/` branch; classify its head as active, exactly integrated, patch-equivalent/rebased, or uniquely unintegrated with auditable evidence; remove only inactive worktrees whose changes are clean and whose commits are proven represented in the surviving delivery branch or canonical `main`; delete only corresponding proven-stale local branches and, where authenticated and unambiguously superseded, remote branches; preserve the primary worktree, current delivery branch, any active implementation-agent worktree, user changes, and every uniquely unintegrated commit; prune stale worktree metadata and remote-tracking references; document retained exceptions; then verify clean status and a minimal branch/worktree inventory without merging or rewriting history.
  Acceptance evidence (2026-08-09): two independent read-only patch audits classified every remaining historical task branch against exact delivery head `ca88110`; exact or stronger integrated commits were identified for all product changes, and stale PLAN/CHANGELOG snapshots were explicitly excluded. Cleanup began from 45 worktrees, 47 local `codex/` branches, and 43 remote `codex/` branches. Forty-four clean non-primary worktrees, 46 superseded local branches, and 42 authenticated remote branches were removed without force; all 17 heads retained during the final pass were first preserved under non-branch `refs/archive/` references, including the subsequently integrated TASK-162 agent head. Worktree metadata and remote tracking were pruned. Final inspection found one clean primary worktree on `codex/post-merge-validation`, one local `codex/` branch, and its one matching remote branch; no uniquely unintegrated commit, user change, open implementation worktree, merge, history rewrite, or other branch was discarded. Readiness confidence: **99/100**; local archive refs deliberately retain recovery provenance and do not appear as feature branches or worktrees.
- **TASK-163:** Consolidate all currently open pull requests so exactly one reviewed pull request targets `main` without merging. DoD: inventory every open PR and its checks/dependency/security impact; integrate each valid non-duplicate change into `codex/post-merge-validation` with attribution and focused verification; document why any invalid, obsolete, or duplicate change is not integrated; update the surviving draft PR description and checks; close or retarget only superseded PRs with concise comments; preserve user work and Dependabot provenance; leave no unreviewed dependency update or unresolved actionable review comment; and verify through GitHub that the single remaining `main`-targeting PR is the current validated branch. Evidence: implementation PRs #9 and #10 were proven represented by rebased commits and closed; incompatible TypeScript 7 PR #2 and ESLint 10 PR #3 were rejected with peer/CI evidence; PRs #4 and #6 were integrated as `4993031`; PR #5 was corrected and integrated as `6b4fe04`; exact branch validation passed 253 unit tests, 152 integration tests, OpenAPI/Swagger/migrations, zero-vulnerability audit, and production build; GitHub CI passed twice on `a1e310f`; all superseded PRs were closed with traceability; and GitHub reported only clean draft PR #7 from `codex/post-merge-validation` to `main` (readiness confidence: 99/100).
- **TASK-143:** Verify file create, replace, and delete conflicts on one exact approved deployment. DoD: controlled concurrent requests yield one documented winner per resource version, deterministic losers, readable winner bytes/metadata, no exposed physical key, and synthetic fixtures are removed only after explicit confirmation.
  Acceptance evidence (2026-08-09): after explicit fixture and cleanup approval, concurrent signed-in browser creates at one synthetic logical key produced one readable 28-byte winner and one generic `409 storage_conflict` loser. Two fresh forms that observed that winner then submitted distinct 33-byte replacements concurrently; one replacement remained readable with matching size and SHA-256 metadata and the other returned the same deterministic non-disclosing `409`. A delete/replacement race applied deletion to the observed old version while the concurrently created replacement became the sole readable 9 MiB version, demonstrating that deletion did not remove the newer resource; the deployed source's production-shaped D1 test supplies the deliberately paused stale-delete interleaving and requires its loser to return `409`. No response or page exposed an `objects/` physical key. The approved final delete was followed by a fresh item not-found response and an empty 0-item / 0-byte collection with no TASK-143 key. Exact approved Sites version 38 remained on source `cfa7d709adda57e4759a2a67f7b52d84ff74e380` and environment revision 13; no configuration, access setting, identity, credential, user data, or unrelated resource changed. Readiness confidence: **91/100**; the live edge cannot pause between the delete read and compare-and-set, so that exact stale-delete loser timing remains injected locally while live concurrent create, replacement, version-preserving delete, winner readability, non-disclosure, and cleanup are proven.
- **TASK-125:** Verify the per-user/client namespace item and byte ceilings on one exact approved deployment. DoD: boundary writes succeed, the next write fails without persistence, replacement/delete accounting is correct, errors reveal no other namespace, and synthetic data is cleaned only with explicit confirmation. Current live baseline: Sites version 38 at commit `cfa7d709adda57e4759a2a67f7b52d84ff74e380` and environment revision 11 report 1 item / 56 B in the signed-in browser namespace, a 500-item / 50 MiB namespace ceiling, and available write controls. Residual: after explicit hosted-configuration and fixture-cleanup approval, apply small non-secret acceptance ceilings, prove item and byte rejection plus replacement/delete accounting, remove every temporary override, redeploy the same saved version, verify the default ceiling is restored, and retain no fixture.
  Acceptance evidence (2026-08-09): after explicit approval, Sites version 38 was redeployed at environment revision 12 with only non-secret temporary namespace ceilings of 2 items / 64 B. Starting from 0 items / 0 B, two distinct 9-byte records succeeded and the collection reported 2 items / 18 B; a third 9-byte record returned the generic configured-limit error, reflected no other key or namespace, persisted nothing, and left usage unchanged. Deleting one record released its item and bytes. Replacing the survivor with a 58-byte value changed usage to exactly 1 item / 58 B; another 9-byte create then returned the same generic limit error, persisted nothing, and disclosed no other resource. Deleting the 58-byte record released capacity, a fresh 9-byte create succeeded, and final cleanup returned usage to 0 items / 0 B with no TASK-125 key present. The two temporary variables were removed rather than overwritten; Sites version 38 was redeployed at environment revision 13, and both the Sites hostname and `aittadb.com` converged on the documented 500-item / 50 MiB defaults with an empty namespace. No secret, identity, credential, other namespace, access setting, source, binding, or unrelated data entered the change or evidence. Readiness confidence: **99/100**; ordinary edge propagation briefly exposed revision 12 on the custom domain after revision 13 succeeded, then converged before completion.
- **TASK-117:** Remove the synthetic record `acceptance/task-045-20260808` and TASK-116 file fixtures at logical keys `676abf3d-fd15-469b-9c73-3674029164ce`, `76d8c65e-80bc-46f4-87ae-170fc69d2f59`, `c0577002-01cf-469a-8559-c35388ee127e`, and `7c6a31f5-fa6b-4609-81e8-87b39df2dafe` only after explicit destructive confirmation. DoD: ownership is verified through the signed-in resource UI, only these named synthetic fixtures are deleted, subsequent item reads return `404`, aggregate usage decreases as expected, and no user data or credentials enter evidence. Current live read-only evidence: the signed-in namespace contains only the named record and reports 1 item / 56 B; each named file already returns the HTML not-found state. Residual: after explicit confirmation, delete only that record, verify its item read returns `404` and aggregate usage becomes 0 items / 0 B, then archive the complete evidence.
  Acceptance evidence (2026-08-09): after the user explicitly approved destructive synthetic-fixture cleanup, the signed-in resource UI on Sites version 38 at source `cfa7d709adda57e4759a2a67f7b52d84ff74e380` showed the exact named record as the sole namespace item at 1 item / 56 B. Its canonical delete action removed it; the resulting item page advertised only creation, a fresh canonical GET produced the non-disclosing not-found representation, and production Worker telemetry recorded that exact GET as `404` with an `ok` outcome. Fresh signed-in reads of all four named file resources also produced their non-disclosing not-found representations. The collection then showed no records and aggregate record/file usage of 0 items / 0 B. No identity, credential, physical R2 key, stored content beyond the already named synthetic contract, or unrelated resource entered evidence. Exact deployed-source CI runs [`31278159997`](https://github.com/aittadb/aittadb/actions/runs/31278159997) and [`31278161293`](https://github.com/aittadb/aittadb/actions/runs/31278161293), plus latest coordination-source runs [`31278900551`](https://github.com/aittadb/aittadb/actions/runs/31278900551) and [`31278902907`](https://github.com/aittadb/aittadb/actions/runs/31278902907), passed. Readiness confidence: **99/100**; ordinary provider telemetry sampling remains outside the resource contract, while the signed-in browser state and exact record status prove cleanup.
- **TASK-159:** Enforce post-deletion account isolation and re-registration semantics. DoD: after clean completion, the first identity-resolving request creates exactly one new UUID and empty user-owned namespace while repeats retain it; old access, refresh, ID, consent, revocation, record, file, administrator-submission, and deletion-status state cannot authorize, identify, or reconnect the new account; status reads remain bound only to the old tombstone and never create or disclose the new identity; email-reassignment and self-contained expired-token limitations remain explicit; and cross-boundary credential/storage/status/privacy integration tests, documentation, full validation, and CI evidence pass.
  Acceptance evidence (2026-08-08): isolated and fast-forward-integrated commit `a82fdf8537aa7216dba2ddac223770c63960c962` adds a production-shaped cross-boundary integration flow over the real account-deletion coordinator, HTTP handlers, token validators, memory D1 contract, and R2 adapter. Clean completion removes the former user's grants, consent, refresh family, attributed revocation, administrator submission, record, file metadata, and R2 bytes; AittaDB rejects old access and refresh credentials, and ID tokens cannot act as access tokens. A later same-email session creates one different UUID and empty record/file namespace, while repeat resolution keeps that UUID and the encrypted deletion-status handle continues to expose only the old completed tombstone without creating or disclosing identity. Tests and documentation explicitly preserve the email-reassignment limitation and the fact that signature-only external verification may accept old access and ID JWTs for the former subject only until `exp`; neither reconnects the replacement account. Full validation passed formatting, linting, type checking, the 176-file tracked-tree secret check and reachable-history audit across 255 commits and 1,240 unique blobs, 252 unit tests, 147 integration tests, OpenAPI parity for 43 operations, three pinned Swagger files, 20 tables across 11 Sites-packaged migrations, the 31,739-byte `AGENTS.md` guard, a zero-vulnerability audit, and the production build. Exact isolated GitHub Actions run [`31277667669`](https://github.com/aittadb/aittadb/actions/runs/31277667669) and integrated runs [`31277719392`](https://github.com/aittadb/aittadb/actions/runs/31277719392) and [`31277721343`](https://github.com/aittadb/aittadb/actions/runs/31277721343) passed. Readiness confidence: **98/100**; the post-deletion composition is proven locally and in CI, while destructive end-to-end execution against hosted Sites remains independently gated by TASK-155.
- **TASK-158:** Unlink subject-attributable security audit state before account-deletion finalization. DoD: actor attribution has one explicit structured storage contract rather than a subject-derived value hidden in generic JSON; deletion clears that attribution in deterministic bounded batches before removing the user; legacy attributed rows migrate without exposing or deleting unrelated audit events; interruption, exact-limit, uncertain-result, other-subject, retention, privacy, migration, documentation, full-validation, and CI evidence pass; the old UUID tombstone remains the only durable retained subject-linkable deletion exception; and five-minute pseudonymous rate counters remain outside audit attribution under their existing bounded cleanup policy.
  Acceptance evidence (2026-08-08): original isolated commits `0f6f92719cb520a72e070a2a5e9f04400bbee8d6` and `08d75e2055a13a7f7a46d57d67152e7b1c8b4a98`, rebased implementation commits `957c616` and `eca80cc`, and integrated CI-repair commit `627b630d8e9300b08e403bc65d5eae1d4fbffe19` add nullable canonical SHA-256 base64url actor attribution outside generic audit JSON and migration `0011_audit_actor_attribution.sql`. The migration preserves every audit event, moves only valid legacy actor hashes into the indexed column, removes malformed legacy attribution without accepting it, and leaves unrelated data unchanged. Exact-live finalization clears at most 100 matching rows in deterministic timestamp/ID order before user removal, preserves events, other subjects, and five-minute rate counters, resumes after interruption or uncertain responses, and rejects stale attempts; 100/101-row memory and migrated-D1 boundaries prove the finite contract. A CI run exposed an unrelated one-second storage-list assertion race; the focused repair now derives expected `updated_at DESC, key ASC` order from the API's returned timestamps instead of assuming tied writes. Final validation passed formatting, linting, type checking, the 176-file tracked-tree secret check and reachable-history audit across 254 commits and 1,237 blobs, 252 unit tests, 146 integration tests, OpenAPI parity for 43 operations, three pinned Swagger files, 20 tables across 11 Sites-packaged migrations, the 31,553-byte `AGENTS.md` guard, a zero-vulnerability audit, and the production build. Exact original, rebased, and repair GitHub Actions runs [`31275817157`](https://github.com/aittadb/aittadb/actions/runs/31275817157), [`31276262682`](https://github.com/aittadb/aittadb/actions/runs/31276262682), and [`31276432256`](https://github.com/aittadb/aittadb/actions/runs/31276432256), plus final integrated runs [`31276487095`](https://github.com/aittadb/aittadb/actions/runs/31276487095) and [`31276488910`](https://github.com/aittadb/aittadb/actions/runs/31276488910), passed. Readiness confidence: **98/100**; migration `0011` remains unexercised on hosted Sites D1, while post-deletion cross-boundary semantics and live destructive acceptance remain TASK-159 and TASK-155.
- **TASK-153:** Add the account-deletion status resource. DoD: canonical HTML and hypermedia `GET /account/deletion` use a reload-safe encrypted status handle purpose-bound to the exact trusted Sites email and old subject; missing, expired, tampered, key/issuer-mismatched, or switched-account handles perform no D1 lookup or coordinator work; valid representations show only coarse pending/running/retry/completed state and the currently valid refresh/recovery action, never UUIDs, email, attempts, timestamps, counts, client or storage identifiers; non-completed reads nudge only bounded TASK-151 work; and authorization, state, reload, non-disclosure, OpenAPI, full-validation, and CI evidence passes.
  Acceptance evidence (2026-08-08): original isolated commit `6d1b420824b83b27c7d6ae2de8af5f76e09b3fe2` and rebased/integrated commit `439b0ab31158716b4b51fca62bf3fc11a919454d` add canonical content-negotiated `GET /account/deletion` with a separate seven-day AES-GCM status handle bound to its purpose, issuer, key ID/material, exact trusted Sites email, and encrypted old subject. Missing, expired, tampered, wrong-key/issuer, switched-account, and cross-purpose handles perform no D1, R2, cleanup, rate-limit, or coordinator work; valid reads perform one exact old-subject job lookup and expose only pending, running, retry, or completed with one state-valid refresh/recovery transition. First accepted deletion sets a scoped secure host-only cookie and non-completed reads schedule at most one caught bounded coordinator pass; completed status advertises only Sites sign-out and never creates identity. Integrated validation passed formatting, linting, type checking, the 173-file tracked-tree secret check and reachable-history audit across 251 commits and 1,225 blobs, 243 unit tests, 146 integration tests, OpenAPI parity for 43 operations, three pinned Swagger files, 20 tables across 10 Sites-packaged migrations, the 31,388-byte `AGENTS.md` guard, a zero-vulnerability audit, and the production build. Exact original and rebased isolated GitHub Actions runs [`31275747444`](https://github.com/aittadb/aittadb/actions/runs/31275747444) and [`31275929001`](https://github.com/aittadb/aittadb/actions/runs/31275929001), plus integrated runs [`31275969484`](https://github.com/aittadb/aittadb/actions/runs/31275969484) and [`31275971758`](https://github.com/aittadb/aittadb/actions/runs/31275971758), passed. Readiness confidence: **98/100**; no hosted Sites status flow was exercised, and post-deletion re-registration and live destructive acceptance remain TASK-159 and TASK-155.
- **TASK-154:** Make local-user resolution atomic for concurrent trusted Sites identity requests. DoD: one D1 statement inserts a new immutable UUID or returns and updates the one exact existing email mapping without a read-then-insert race; memory behavior remains equivalent; malformed or uncertain adapter results fail closed while a committed retry returns the same UUID; repeat requests preserve creation time and update only allowed identity metadata; and focused insert/conflict/concurrency/failure tests, interface and architecture documentation, full validation, and CI evidence pass.
  Acceptance evidence (2026-08-08): isolated commit `140558c72b098423f1be827699fd4946794a3a30` and rebased/integrated commit `56bff8e20fdd662bedcdea115185bf3e578b098c` replace the D1 read-then-insert race with one prepared `INSERT ... ON CONFLICT(email) DO UPDATE ... RETURNING` statement. Concurrent first requests converge on one generated UUID and one row; conflicts preserve UUID, email, and creation time while updating only display name and update time. The repository rejects absent, unsuccessful, multiple, or malformed returned rows, and retry after a committed response failure returns the already committed UUID. The synchronous memory path retains equivalent exact, case-sensitive email behavior. Eight focused tests passed. Full validation passed formatting, linting, type checking, the tracked-tree and reachable-history secret checks across 247 commits and 1,186 blobs, 239 unit tests, 142 integration tests, OpenAPI, Swagger, migration, the 31,525-byte `AGENTS.md` guard, a zero-vulnerability audit, and the production build. Exact original and rebased isolated GitHub Actions runs [`31275033776`](https://github.com/aittadb/aittadb/actions/runs/31275033776) and [`31275098165`](https://github.com/aittadb/aittadb/actions/runs/31275098165), plus integrated runs [`31275157521`](https://github.com/aittadb/aittadb/actions/runs/31275157521) and [`31275159585`](https://github.com/aittadb/aittadb/actions/runs/31275159585), passed. Readiness confidence: **98/100**; ChatGPT Sites still supplies no stable upstream subject beyond exact email, case normalization remains deliberately outside this task, and hosted D1 concurrency remains for approved live acceptance.
- **TASK-152:** Add the authenticated account-deletion request operation. DoD: one canonical content-negotiated operation requires explicit confirmation cryptographically bound to the currently resolved local subject, current trusted Sites identity, same-origin and CSRF; rejects stale cross-account forms, administrators, and replay generically without creating or updating an identity; starts TASK-146 exactly once and nudges bounded TASK-151 work asynchronously; advertises no deployment-wide deletion; and passes body/rate bounds plus HTML, hypermedia, OpenAPI, security, privacy, full-validation, and CI evidence.
  Acceptance evidence (2026-08-08): isolated and fast-forward-integrated commit `b0ea797e54f9554b02f958ac40c9f89f1c82b71b` adds canonical `POST /account/deletion` plus matching session HTML and versioned hypermedia controls. A short-lived AES-GCM confirmation is purpose-, issuer-, local-subject-, and exact trusted-email-bound; the adapter additionally enforces same-origin before body reads, a 1 KiB URL-encoded/JSON limit, CSRF, an exact confirmation phrase, pseudonymous per-IP and deployment-global rate limits, existing-user-only lookup, and administrator denial. Only the first atomic job creation returns coarse no-store `202` and schedules one caught bounded coordinator pass; stale cross-account forms, tampering, expiry, key/issuer mismatch, replay, and missing identities fail generically without creating or updating identity. Full validation passed formatting, linting, type checking, the 167-file tracked-tree and reachable-history secret checks, 231 unit tests, 142 integration tests, OpenAPI parity for 42 operations, three pinned Swagger files, 20 tables across 10 Sites-packaged migrations, the 31,525-byte `AGENTS.md` guard, a zero-vulnerability audit, and the production build. Exact isolated GitHub Actions run [`31274230095`](https://github.com/aittadb/aittadb/actions/runs/31274230095) and integrated runs [`31274294803`](https://github.com/aittadb/aittadb/actions/runs/31274294803) and [`31274296237`](https://github.com/aittadb/aittadb/actions/runs/31274296237) passed. Readiness confidence: **97/100**; no deployment or live Sites identity test was performed, and completion status, post-deletion re-registration, and live destructive acceptance remain TASK-153 through TASK-155.
- **TASK-151:** Add the bounded account-deletion coordinator and atomic subject-retirement finalizer/write fence. DoD: an exact live lease advances TASK-148 through TASK-150 in finite request-time work; D1 and in-flight R2 writes cannot repopulate the subject after deletion starts; foreign ownership references, stale attempts, uncertain outcomes, and partial failures retry or fail closed without skipping data; finalization atomically removes PII and the local user only after every owned inventory is empty; only the documented pseudonymous terminal tombstone and bounded security retention remain; and memory plus migrated-D1 interruption, convergence, race, isolation, privacy, documentation, full-validation, and CI evidence passes.
  Acceptance evidence (2026-08-08): rebased isolated commit `fb958e30b1c30246fb0f0c9a785abd80a0a8f6e9` and integrated commit `210f4bd293c36a8e2d8147cea538cdde91bef0dc` add one internal coordinator that claims one exact five-minute lease and advances bounded write-fence, credential, record, and file phases before clean-only finalization. Migration `0010` adds durable owner-bound R2 write fences and D1 triggers that reject late subject ownership; expired fences become TASK-119 repair work before removal, foreign-owner physical references remain retryable conflicts, and new R2 keys are opaque with no subject metadata. Finalization is one D1 batch that verifies the exact live attempt and empty owned inventories, removes the local user and PII, and completes only the minimal UUID tombstone; uncertain committed results are observed idempotently, stale attempts lose, explicit admin-submission purge works without foreign keys, and bounded audit/rate security retention remains. Integrated validation passed formatting, linting, type checking, the 167-file secret guard, 225 unit tests, 135 integration tests, OpenAPI parity for 41 operations, three pinned Swagger files, 20 tables across 10 Sites-packaged migrations, the 31,117-byte `AGENTS.md` guard, a zero-vulnerability audit, the reachable-history secret audit, and the production build. Exact isolated GitHub Actions runs [`31272962543`](https://github.com/aittadb/aittadb/actions/runs/31272962543) and [`31272964703`](https://github.com/aittadb/aittadb/actions/runs/31272964703), plus integrated runs [`31273087226`](https://github.com/aittadb/aittadb/actions/runs/31273087226) and [`31273084975`](https://github.com/aittadb/aittadb/actions/runs/31273084975), passed. Readiness confidence: **97/100**; real Cloudflare D1 batch and R2 failure timing remain unverified until a separately approved Sites deployment, while HTTP initiation/status, re-registration, and live deletion acceptance remain TASK-152 through TASK-155.
- **TASK-157:** Enforce public-repository secret safety. DoD: one documented repository policy and automated tracked-tree guard reject private signing material, runtime secret files, deployment credentials, and bearer-equivalent values without flagging documented placeholders or test-only fixtures; ignore/example/configuration guidance keeps local and hosted secrets out of Git; the current tree and complete reachable Git history are audited without printing credential values; any real exposure is recorded by category and location for rotation or approved history remediation; `AGENTS.md` states that this GitHub repository is public and contains no secrets while remaining below 32,000 bytes; and focused tests, full validation, exact-commit CI, documentation, and acceptance evidence pass.
  Acceptance evidence (2026-08-08): isolated commit `228b1f557ef294a2954fbc81ef4399dec173fe09` and integrated commit `dafca2da1066cef6e49e5051a7713a6014b50091` add a dependency-free repository-aware guard for forbidden tracked paths, private PEM/JWK material, common provider credentials, bearer values, and sensitive literal assignments. The guard permits only inert templates and explicitly synthetic test fixtures, prints finding category/path/commit rather than matched values, runs in `npm run validate` and CI, and is paired with expanded ignore, contributor, deployment, README, security, and public-repository agent policy. The integrated tracked-tree scan passed for 163 files. The full reachable-history audit passed across 239 commits and 1,149 unique blobs with no credential exposure; it separately noted three legacy `.openai/hosting.json` revisions as non-secret public Sites deployment metadata while still scanning their contents. Integrated validation passed formatting, linting, type checking, 196 unit tests, 135 integration tests, OpenAPI parity for 41 operations, three pinned Swagger files, 19 tables across 9 Sites-packaged migrations, the 31,694-byte `AGENTS.md` guard, a zero-vulnerability audit, and the production build. Exact isolated GitHub Actions run [`31272422068`](https://github.com/aittadb/aittadb/actions/runs/31272422068) and integrated runs [`31272564530`](https://github.com/aittadb/aittadb/actions/runs/31272564530) and [`31272562732`](https://github.com/aittadb/aittadb/actions/runs/31272562732) passed. Readiness confidence: **98/100**; detection is intentionally signature- and repository-policy-based rather than entropy-based, GitHub's secret-scanning alerts API was unavailable to the current token, and history coverage is limited to refs reachable in the complete local clone at audit time.
- **TASK-150:** Add retry-safe account file purge. DoD: deterministic batches remove only the subject's D1 file metadata and referenced R2 objects across client namespaces, use TASK-119 for persistent partial failure, never expose physical keys, and pass large-fixture/isolation/D1-R2 failure/documentation tests plus full validation and CI.
  Acceptance evidence (2026-08-08): rebased isolated commit `31f6f05868bb5e8f4ff3a29264985626963368be` and integrated commit `ff4856340ff2621c5e1c0243ad73d21c4dc4d48b` add one internal 25-item deterministic batch primitive that requires the subject's exact running deletion-job attempt and unexpired lease. It retires only that subject's R2 objects, conditionally removes their D1 metadata, and records owner-bound TASK-119 repair work in the same D1 batch when retirement is incomplete. Global physical-key reference checks prevent deleting shared bytes; completed clean tombstones retry idempotently, dirty tombstones fail closed; other subjects, clients, records, credentials, internal state, and physical keys remain undisclosed and untouched. Integrated validation passed formatting, linting, type checking, 188 unit tests, 135 integration tests, OpenAPI parity for 41 operations, three pinned Swagger files, 19 tables across 9 Sites-packaged migrations, the 31,774-byte `AGENTS.md` guard, a zero-vulnerability audit, and the production build. Exact isolated GitHub Actions run [`31268494700`](https://github.com/aittadb/aittadb/actions/runs/31268494700) and integrated runs [`31268599936`](https://github.com/aittadb/aittadb/actions/runs/31268599936) and [`31268597800`](https://github.com/aittadb/aittadb/actions/runs/31268597800) passed. Readiness confidence: **98/100**; R2 retirement remains intentionally at-least-once and repair-backed, exact hosted Cloudflare D1/R2 behavior remains unverified, and coordinator/final identity removal remains independently scoped to TASK-151.
- **TASK-149:** Add bounded account record purge. DoD: deterministic batches delete only records and usage accounting owned by the subject across its client namespaces, preserve all other users and internal tables, resume safely, and pass large-fixture/isolation/failure/documentation tests plus full validation and CI.
  Acceptance evidence (2026-08-08): rebased isolated commit `1ed63dc772f12b29db8ed634c3807d7fb08d6a0f` and integrated commit `4dffdaa26614d79bca56f2c7699a126e246403cd` add one internal repository primitive that requires the exact subject's deletion job and removes at most 100 records per call in deterministic client-and-key order across all of that subject's client namespaces. Progress contains only aggregate deletion count and completion state; repeated exact-multiple and completed-tombstone calls converge idempotently; one failed D1 statement rolls back its batch and retries safely. Large migrated-D1 and memory fixtures prove other subjects, files, credentials, clients, deletion state, audit/rate state, and unrelated internal tables remain unchanged, while derived namespace quota usage immediately releases capacity. No migration, HTTP route, UI, coordinator, file/R2 purge, or identity removal was added. Integrated validation passed formatting, linting, type checking, 173 unit tests, 135 integration tests, OpenAPI parity for 41 operations, three pinned Swagger files, 19 tables across 9 Sites-packaged migrations, the 31,935-byte `AGENTS.md` guard, a zero-vulnerability audit, and the production build. Exact isolated GitHub Actions run [`31268117723`](https://github.com/aittadb/aittadb/actions/runs/31268117723) and integrated runs [`31268239468`](https://github.com/aittadb/aittadb/actions/runs/31268239468) and [`31268237545`](https://github.com/aittadb/aittadb/actions/runs/31268237545) passed. Readiness confidence: **98/100**; a storage request already past TASK-147 at deletion start may commit afterward, so the future coordinator must repeat this phase before final removal, and live hosted D1 behavior remains unverified.
- **TASK-148:** Add bounded account credential and grant purge. DoD: one deterministic batch removes only the subject's browser sessions, authorization requests/codes, device grants, consents, refresh tokens/families, and revoked-JTI state without touching OAuth clients, signing material, configuration, or other subjects; retries are idempotent; tests/docs pass full validation and CI.
  Acceptance evidence (2026-08-08): isolated commit `30ec5c41c0bb8feca62a2932955c5f68732974ba` and integrated commit `83ff009836283a7ac001a9cffdfd7a6e1b0de665` add one internal repository primitive that requires an existing deletion job and spends a single 100-row budget in deterministic child-before-parent order across authorization codes and requests, device grants, consents, refresh tokens and families, and subject-attributed revoked JWT identifiers. Repeated calls converge idempotently; interruption resumes from committed work; cross-subject child corruption blocks its parent instead of authorizing another subject's deletion; OAuth clients, the reserved browser client, users, storage, audit/rate/administrative state, configuration, and signing material remain untouched. AittaDB browser access credentials are stateless and are invalidated by TASK-147 rather than by introducing session state. Migration `0009` adds purge indexes and nullable ownership for new revoked-JTI rows; unattributable legacy rows remain protected until ordinary expiry cleanup. Integrated validation passed formatting, linting, type checking, 165 unit tests, 135 integration tests, OpenAPI parity for 41 operations, three pinned Swagger files, 19 tables across 9 Sites-packaged migrations, the 31,960-byte `AGENTS.md` guard, a zero-vulnerability audit, and the production build. Exact isolated GitHub Actions run [`31267519347`](https://github.com/aittadb/aittadb/actions/runs/31267519347) and integrated runs [`31267648582`](https://github.com/aittadb/aittadb/actions/runs/31267648582) and [`31267646385`](https://github.com/aittadb/aittadb/actions/runs/31267646385) passed. Readiness confidence: **98/100**; D1 cannot transact all purge tables together, so the future coordinator must repeat until `done`, and no live Sites D1 migration was performed.
- **TASK-131:** Verify encrypted record pagination on one exact approved deployment. DoD: two forward pages are followed only through returned links, cursor text discloses no known position/namespace value, malformed and cross-namespace cursors fail, and evidence retains no cursor, item key, identity, or client identifier.
  Acceptance evidence (2026-08-08): approved public Sites version 36 from commit `1e79bc2522fd7da8828d6d152b4d4f1476bedc73` was exercised with three temporary records in the existing public acceptance-client namespace and the current signed-in browser namespace as a second client boundary. Three one-item versioned pages returned `200`, API version `0.1`, distinct items, and two usable `next` relations; each subsequent request used only the preceding returned link, and the traversal found every temporary item exactly once. The 118-character cursor contained none of the known logical positions or namespace values. A malformed cursor and reuse of the valid cursor through the same user's different browser-client namespace each returned versioned `400 invalid_request`. The temporary records were deleted with three `200` responses and verified with three `404` responses. Evidence retains only counts, statuses, cursor length, API version, and boolean disclosure/isolation outcomes; no cursor, record key, identity, client identifier, device code, or token is retained. Readiness confidence: **99/100**; the exact returned ciphertext was inspected without retention, while cryptographic confidentiality and tamper resistance remain additionally covered by integrated source tests.
- **TASK-147:** Add a deleting/deleted-subject authorization gate. DoD: once TASK-146 starts, new internal sessions and storage access fail immediately, existing access/refresh credentials are rejected through one shared check, administrators configured in `ADMIN_SUBJECTS` cannot start deletion, and focused positive/negative/security tests plus full validation and CI pass.
  Acceptance evidence (2026-08-08): isolated commit `0f542c783472dc1f234464110f8aaf6302666956` and integrated commit `971929e50df80290b37c5bfa428b7d339879cb54` route browser-session creation, access-token use, refresh rotation, UserInfo, introspection, administration, and storage authorization through one protocol-independent active-subject gate. Every deletion-job state, including its completed tombstone, rejects new and existing access generically; storage rejects before namespace reads, rate-limit writes, mutation, or R2 access; and the deletion-start domain guard rejects subjects configured in `ADMIN_SUBJECTS` before repository mutation. No deletion route, purge, coordinator, or status UI was added. Integrated validation passed formatting, linting, type checking, 157 unit tests, 135 integration tests, OpenAPI parity for 41 operations, pinned Swagger, 19 tables across 8 Sites-packaged migrations, the 31,952-byte `AGENTS.md` guard, a zero-vulnerability audit, and the production build. Exact isolated GitHub Actions run [`31266255673`](https://github.com/aittadb/aittadb/actions/runs/31266255673) and integrated runs [`31266693458`](https://github.com/aittadb/aittadb/actions/runs/31266693458) and [`31266690330`](https://github.com/aittadb/aittadb/actions/runs/31266690330) passed. Readiness confidence: **98/100**; a request already past its authorization check at the exact instant deletion starts may complete, every subsequent check rejects, and geographically distributed hosted D1 concurrency remains unverified.
- **TASK-132:** Verify encrypted file pagination on one exact approved deployment. DoD: two forward pages are followed only through returned links, cursor text discloses no known position/namespace value or physical key, malformed and cross-namespace cursors fail, and evidence retains no cursor, file key, identity, or client identifier.
  Acceptance evidence (2026-08-08): approved public Sites version 36 from commit `1e79bc2522fd7da8828d6d152b4d4f1476bedc73` was exercised with three temporary one-byte files in the existing public acceptance-client namespace and the current signed-in browser namespace as a second client boundary. Three one-item versioned pages returned `200`, API version `0.1`, distinct items, and two usable `next` relations; each subsequent request used only the preceding returned link, and the traversal found every temporary item exactly once. The 118-character cursor and its decoded envelope contained none of the known logical positions or namespace values, while all page representations omitted physical R2 keys. A malformed cursor and reuse of the valid cursor through the same user's different browser-client namespace each returned versioned `400 invalid_request`. All temporary files were deleted with `200` and verified `404`. Evidence retains only counts, statuses, cursor length, API version, and boolean disclosure/isolation outcomes; no cursor, logical or physical file key, identity, client identifier, token, or bytes are retained. Readiness confidence: **99/100**; inspection covers the exact returned ciphertext and decoded envelope without retaining either, while cryptographic confidentiality and tamper resistance remain additionally covered by integrated source tests.
- **TASK-140:** Verify non-multipart streamed body bounds on one exact approved deployment. DoD: one oversized JSON/form request and one oversized raw-file request are rejected before persistence by AittaDB's own response, ordinary bounded requests still work, and evidence stores no request body, token, file bytes, or identity.
  Acceptance evidence (2026-08-08): approved public Sites version 36 from commit `1e79bc2522fd7da8828d6d152b4d4f1476bedc73` was exercised through a temporary Device Grant access token retained only in browser memory. A 65,550-byte JSON record request returned AittaDB API version `0.1`, status `413`, and generic `invalid_request`; the item remained `404`. A 10,485,761-byte request streamed from an undeclared-length browser `ReadableStream` to the raw file endpoint returned the same AittaDB-versioned `413 invalid_request`; the file remained `404`, proving the provider edge did not substitute the rejection and no D1/R2 resource persisted. Ordinary bounded record and raw-file writes returned `200`, the seven-byte file read returned `200` with length seven, and immediate synthetic cleanup returned `200` followed by `404` for both resources. Evidence retains only byte counts, statuses, error class, API version, and mutation booleans; no request body, access/device code, token, file bytes, cursor, identity, client identifier, or resource key is retained. Readiness confidence: **99/100**; exact provider chunk boundaries may vary, but the tested response was demonstrably AittaDB's own bounded-reader response.
- **TASK-134:** Verify storage read/write rate-limit exhaustion and recovery on one exact approved deployment. DoD: one namespace reaches each configured limit, receives generic throttling without mutation or cross-tenant data, recovers after the window, and only coarse request counts/statuses are retained.
  Acceptance evidence (2026-08-08): approved public Sites version 36 from commit `1e79bc2522fd7da8828d6d152b4d4f1476bedc73`, environment revision 11, and the configured default limits were exercised through one signed-in namespace without retaining request bodies, credentials, identity, resource identifiers, or response content. Exactly 120 read requests succeeded and the next 10 returned generic `429` responses with `Retry-After: 60`; a read after the 60-second window returned `200`. Exactly 30 idempotent writes succeeded and the next 10 returned the same generic `429` contract; a write after the window returned `200`. In a separate reset window, 30 unchanged-value writes returned `200`, a distinct 31st sentinel returned `429` with `Retry-After: 60`, and an authenticated read returned `200` with the stable value present and sentinel absent, directly proving the throttled write did not mutate. No other namespace data appeared. Readiness confidence: **98/100**; provider-wide load balancing across geographically distinct edge locations was not exercised, while D1 atomicity under concurrent callers remains covered by the integrated source tests.
- **TASK-146:** Add an account-deletion job state and repository contract. DoD: one migration models pending/running/retryable/completed terminal state for one immutable local subject, supports idempotent start and bounded claims, exposes no deployment-internal identifier, and passes migration/race/isolation/documentation tests plus full validation and CI.
  Acceptance evidence (2026-08-08): isolated commit `21236d9a432edfb04afdd5a404b1e54e47fa0fe0` and integrated commit `a154230534a7ab860e066d42f06bab482b3c8e0e` add one `WITHOUT ROWID` subject-keyed migration plus matching D1 and memory repository contracts. Start is idempotent for an existing immutable local subject; claim uses one finite, deterministic, single-statement D1 transition capped at 25 jobs and a five-minute lease; retry and completion compare subject, attempt, state, and unexpired lease; stale, concurrent, and cross-subject transitions lose; completed state is terminal. The row contains no deployment, project, client, email, credential, storage key, or separate public job identifier, and no route or deletion behavior is exposed yet. Integrated validation passed formatting, linting, type checking, 152 unit tests, 133 integration tests, OpenAPI parity for 41 operations, pinned Swagger, 19 tables across 8 Sites-packaged migrations, the 31,951-byte `AGENTS.md` guard, a zero-vulnerability audit, and the production build. Exact isolated GitHub Actions run [`31265090388`](https://github.com/aittadb/aittadb/actions/runs/31265090388) and integrated runs [`31265525937`](https://github.com/aittadb/aittadb/actions/runs/31265525937) and [`31265523332`](https://github.com/aittadb/aittadb/actions/runs/31265523332) passed. Readiness confidence: **98/100**; concurrency is proven with migrated SQLite-backed D1 behavior rather than geographically distributed hosted D1, completed-tombstone retention remains documented but unautomated, and authorization/purge/coordinator/UI behavior remains independently scoped to TASK-147 through TASK-154.
- **TASK-123:** Verify browser-origin enforcement on one exact approved Sites deployment. DoD: canonical and legitimate Sites-dispatch submissions succeed while missing, malformed, foreign, and cross-site-null probes fail before mutation; evidence contains only route family, status, mutation boolean, deployment version, and confidence.
  Acceptance evidence (2026-08-08): approved public Sites version 36 from commit `1e79bc2522fd7da8828d6d152b4d4f1476bedc73` was exercised at the canonical custom domain through the signed-in Chrome session. Missing, malformed, foreign, and opaque cross-site origin probes each returned `403` at the origin gate with mutation false. Canonical issuer-origin and opaque same-origin Sites-dispatch probes advanced past origin enforcement to the independent CSRF gate when deliberately given an invalid CSRF token. With the page-issued CSRF state, the canonical form and a browser-computed opaque-origin same-site form each completed the record update transition; a subsequent authenticated item read confirmed mutation true while retaining the same synthetic value. No identity, cookie, CSRF value, record identifier, request body, or credential was retained in evidence. Readiness confidence: **96/100**; the provider does not expose the internal Sites dispatch request headers as a separate operator artifact, so the opaque case is proven by Chrome sandbox semantics, the accepted state transition, and the server's exclusive origin policy rather than by preserving raw headers.
- **TASK-124:** Connect quota-rejected file reservations and dual compensation failure to TASK-119 repair state. DoD: quota rejection cannot strand an R2 object or stale D1 metadata, persistent compensation failure creates one bounded repair item, retry converges through TASK-120, and focused quota/failure/isolation/documentation tests pass full validation and CI.
  Acceptance evidence (2026-08-08): isolated commit `70e1a4a6d0d79387781ebc5e94ee4dae7ccdec20` and integrated commit `c7b37f02989cb33d1b61da4e4da7e9ad331b23ab` prove a quota loser commits no D1 metadata, failed R2 retirement creates exactly one owner-bound private repair, a bounded first pass may defer the work, and a later pass converges without exposing logical identifiers or the physical key. Integrated `npm run validate` passed 142 unit and 133 integration tests, OpenAPI parity for 41 operations, pinned Swagger, 18 tables in 7 Sites-packaged migrations, the 31,845-byte `AGENTS.md` guard, a zero-vulnerability audit, and the production build. Exact isolated GitHub Actions run [`31264366690`](https://github.com/aittadb/aittadb/actions/runs/31264366690) and integrated runs [`31264479561`](https://github.com/aittadb/aittadb/actions/runs/31264479561) and [`31264477454`](https://github.com/aittadb/aittadb/actions/runs/31264477454) passed. Readiness confidence: **98/100**; a total D1 outage can still prevent durable repair recording, and live hosted quota behavior remains separately scoped to TASK-125 through TASK-127.
- **TASK-142:** Prove production-shaped D1 compare-and-set behavior for concurrent file replace/delete races. DoD: stale mutations return deterministic `409`, no loser overwrites or deletes a newer winner, retained metadata and bytes agree, cleanup uses TASK-119 when necessary, and focused concurrency/failure tests plus full validation and CI pass.
  Acceptance evidence (2026-08-08): isolated commit `a9c30fb2114d6dd965122d7a75493585d82785b9` and integrated commit `a2d7c35a5cc67d8d1320dd35f62bcc43c1016d9a` exercise migrated SQLite-backed D1 through canonical bearer file routes. Eight concurrent replacements produce one `200` winner and seven identical, redacted `409` losers; a stale delete cannot remove a newer replacement; replacement/delete and rollback races preserve the newest metadata/bytes pair; and failed loser retirement creates one private TASK-119 repair that TASK-120 resolves without key disclosure. Integrated `npm run validate` passed 141 unit and 133 integration tests, OpenAPI parity for 41 operations, pinned Swagger, 18 tables in 7 Sites-packaged migrations, the 31,845-byte `AGENTS.md` guard, a zero-vulnerability audit, and the production build. Exact isolated GitHub Actions run [`31264093027`](https://github.com/aittadb/aittadb/actions/runs/31264093027) and integrated runs [`31264257450`](https://github.com/aittadb/aittadb/actions/runs/31264257450) and [`31264254952`](https://github.com/aittadb/aittadb/actions/runs/31264254952) passed. Readiness confidence: **98/100**; geographically distributed hosted D1 conflict evidence remains TASK-143, and a total D1 outage can still prevent repair recording.
- **TASK-112:** Gate downstream OIDC UserInfo and discovery with the OAuth Apps flag while retaining only verification metadata required by AittaDB's internal session. DoD: disabled deployments do not advertise unavailable grants or UserInfo, `/userinfo` performs no bearer lookup, JWKS behavior is explicitly documented, and focused discovery/UserInfo/OpenAPI tests, full validation, and CI pass.
  Acceptance evidence (2026-08-08): isolated commit `8614790b5e62be4e25908b768b6e7d38549835e2` and integrated commit `144d9974f1844a9d9cb0a114ace887bbb44a95cd` gate every `/userinfo` method at the outer boundary. Disabled requests pull no form stream and perform no bearer parsing, Sites identity lookup, CORS client lookup, rate limiting, repository operation, cleanup scheduling, or durable work; signed-in HTML and hypermedia omit UserInfo controls. Disabled discovery contains only `issuer`, `jwks_uri`, and ES256 verification-algorithm metadata, while JWKS remains D1-free and publishes only the configured public P-256 key. Enabled OAuth/OIDC behavior remains intact. Integrated `npm run validate` passed 136 unit and 133 integration tests, OpenAPI parity for 41 operations, pinned Swagger, 18 tables in 7 Sites-packaged migrations, the 31,845-byte `AGENTS.md` guard, a zero-vulnerability audit, and the production build. Exact isolated GitHub Actions run [`31264050975`](https://github.com/aittadb/aittadb/actions/runs/31264050975) and integrated runs [`31264129552`](https://github.com/aittadb/aittadb/actions/runs/31264129552) and [`31264127591`](https://github.com/aittadb/aittadb/actions/runs/31264127591) passed. Readiness confidence: **98/100**; no hosted behavior is claimed.
- **TASK-141:** Prove production-shaped D1 compare-and-set behavior for concurrent first file writes. DoD: exactly one writer wins, losers return deterministic `409`, every retained metadata row references existing bytes, losing R2 objects are retired or queued through TASK-119, and focused concurrency/failure tests plus full validation and CI pass.
  Acceptance evidence (2026-08-08): isolated commit `7ccb1b6f45e1b8e0b58ec49d0209d3a528546948` and integrated commit `168c5836703460c5f14686464410690e21ab4734` exercise the canonical bearer file `PUT` path against migrated SQLite-backed D1 and coordinated R2. Eight concurrent first writes produce exactly one `200` winner and seven deterministic `409` losers, with one metadata row naming the sole retained object. An injected losing-object deletion failure returns `409`, records one private owner-scoped TASK-119 repair without exposing the physical key, and converges after repair. Integrated `npm run validate` passed 136 unit and 128 integration tests, OpenAPI parity for 41 operations, pinned Swagger, 18 tables in 7 Sites-packaged migrations, the 31,845-byte `AGENTS.md` guard, a zero-vulnerability audit, and the production build. Exact isolated GitHub Actions run [`31262955795`](https://github.com/aittadb/aittadb/actions/runs/31262955795) and integrated runs [`31263478324`](https://github.com/aittadb/aittadb/actions/runs/31263478324) and [`31263476910`](https://github.com/aittadb/aittadb/actions/runs/31263476910) passed. Readiness confidence: **98/100**; hosted replace/delete race evidence remains independently scoped to TASK-142 and TASK-143.
- **TASK-111:** Gate only `/oauth/revoke` and `/oauth/introspect` with the OAuth Apps flag. DoD: disabled deployments perform no client authentication, token lookup, revocation, or audit mutation; omit both actions from discovery; return documented protocol-safe responses; and pass non-mutation/OpenAPI tests plus full validation and CI.
  Acceptance evidence (2026-08-08): isolated commit `2328d6cdc93ed19dfae3be3657ea5e5c0ed743f9` and integrated commit `28ee26c078a84999ea2bdbf344a21b4c5c85585f` gate revocation and introspection GET and POST requests at the outer request boundary. Disabled requests return negotiated, no-store feature-unavailable responses before body pulls, client authentication, token lookup, audit, cleanup, identity, rate-limit, or repository work; discovery omits both endpoints while enabled behavior remains unchanged. Integrated `npm run validate` passed 136 unit and 128 integration tests, OpenAPI parity for 41 operations, pinned Swagger, 18 tables in 7 Sites-packaged migrations, the 31,845-byte `AGENTS.md` guard, a zero-vulnerability audit, and the production build. Exact isolated GitHub Actions run [`31263371290`](https://github.com/aittadb/aittadb/actions/runs/31263371290) and integrated runs [`31263478324`](https://github.com/aittadb/aittadb/actions/runs/31263478324) and [`31263476910`](https://github.com/aittadb/aittadb/actions/runs/31263476910) passed. Readiness confidence: **98/100**; downstream OIDC discovery and UserInfo remain independently scoped to TASK-112, and no hosted behavior is claimed.
- **TASK-110:** Gate only `/oauth/token` with the OAuth Apps flag. DoD: disabled deployments reject every grant before client authentication, credential lookup, consumption, or durable write; advertise no token action; preserve internal Sites-session token issuance; and pass grant-family non-consumption/OpenAPI tests plus full validation and CI.
  Acceptance evidence (2026-08-08): isolated commit `fa1e40143c2651fc7d1e1a37d6166dcfd69a449b` and integrated commit `19eaaf2c48bbb678fdeba63fc51c2681f7fb83dd` gate token GET and POST at the outer boundary. Disabled Device Grant, Authorization Code, and Refresh Token requests return no-store `503 temporarily_unavailable` without pulling their streams or performing identity, CORS, rate, client, credential, repository, cleanup, or durable work; HTML receives the equivalent error page. The root omits its token control, unsupported hypermedia versions return `406`, enabled behavior remains intact, and the private signed-in session remains authenticated. Integrated `npm run validate` passed 134 unit and 124 integration tests, OpenAPI parity for 41 operations, pinned Swagger, 18 tables in 7 Sites-packaged migrations, the 31,845-byte `AGENTS.md` guard, a zero-vulnerability audit, and the production build. GitHub Actions runs [`31263158740`](https://github.com/aittadb/aittadb/actions/runs/31263158740) and [`31263156740`](https://github.com/aittadb/aittadb/actions/runs/31263156740) passed for the exact integrated SHA. Readiness confidence: **98/100**; revocation/introspection and OIDC UserInfo/discovery remain independently scoped to TASK-111 and TASK-112, and no hosted behavior is claimed.
- **TASK-108:** Gate OAuth authorization and Device Grant initiation with the OAuth Apps flag. DoD: `/authorize`, `/oauth/device_authorization`, and their external-client browser continuations fail closed before durable writes when disabled, disappear from advertised actions, preserve AittaDB's internal Sites session, and pass exact endpoint/error/OpenAPI tests plus full validation and CI.
  Acceptance evidence (2026-08-08): isolated implementation commit `c383d9cf28d114d055e4e339c8f0df24df7c95b2` and integrated commit `cd5d1d4f54b76652b0600a0683fb12e0e77d7cc5` apply the default-off gate at the outer request boundary to `/authorize`, `/oauth/device_authorization`, `/device`, `/device/decision`, and `/consent`. Disabled HTML, compatible JSON, and versioned hypermedia requests return documented local errors before body pulls, CORS/client/rate/identity/repository/cleanup work or durable writes; unsupported vendor versions return `406`. Root metadata omits authorization and device controls while the private signed-in session and storage remain available. Integrated `npm run validate` passed 134 unit and 120 integration tests, OpenAPI parity for 41 operations, pinned Swagger, 18 tables in 7 Sites-packaged migrations, the 31,845-byte `AGENTS.md` guard, a zero-vulnerability audit, and the production build. GitHub Actions runs [`31262715265`](https://github.com/aittadb/aittadb/actions/runs/31262715265) and [`31262712055`](https://github.com/aittadb/aittadb/actions/runs/31262712055) passed on the integrated branch; isolated implementation run `31262103840` also passed. Readiness confidence: **98/100**; external token, revocation/introspection, and OIDC discovery/UserInfo gates remain independently scoped to TASK-110 through TASK-112, and no hosted behavior is claimed.
- **TASK-156:** Restore the enforced `AGENTS.md` instruction budget after the browser-origin guidance update. DoD: redundant wording is removed without weakening origin, CSRF, workflow, or architecture rules; `AGENTS.md` remains below 32,000 bytes; repository-policy tests and full validation pass; and acceptance evidence is archived in `CHANGELOG.md`.
  Acceptance evidence (2026-08-08): implementation commit `c8080459875e9deabcb8714d46f4a5b05fd688a7` compacted only redundant introductory and browser-origin wording while preserving pre-body rejection, rate/repository/R2/maintenance ordering, Sites-dispatch handling, CSRF, concurrent-tab cookies, fail-closed behavior, architecture, and workflow rules. Integrated `npm run validate` passed 134 unit and 116 integration tests, OpenAPI parity for 41 operations, pinned Swagger validation, 18 tables in 7 Sites-packaged migrations, the 31,845-byte `AGENTS.md` guard, a zero-vulnerability audit, and the production build. GitHub Actions runs [`31262601365`](https://github.com/aittadb/aittadb/actions/runs/31262601365) and [`31262599313`](https://github.com/aittadb/aittadb/actions/runs/31262599313) passed for the exact implementation SHA. Readiness confidence: **99/100**; the only residual uncertainty is future instruction growth, which remains mechanically guarded in local validation and CI.
- **TASK-122:** Apply TASK-121 to every enabled state-changing HTML adapter. DoD: a table-driven route test proves invalid origin rejection occurs before body parsing or mutation for session, storage, account, privacy-relevant, and enabled administration forms, while valid origin plus valid CSRF reaches each operation; OpenAPI/security guidance, full validation, and CI pass.
  Acceptance evidence (2026-08-08): implementation commit `b3311dae518aa1b54ae68aa5f1a004cd3e5ce439` applies one pre-body route classifier to all 15 enabled browser operations across session, storage, account, privacy-relevant credential, and administration families. Five invalid-origin classes across every operation produced 75 `403` results with zero request-body pulls, repository calls, R2 calls, or maintenance scheduling. A canonical issuer origin presented through a distinct Sites-dispatch request origin plus a valid CSRF pair reached every expected operation; standards-defined machine OAuth and raw bearer storage POSTs remained usable without browser-origin state. Local `npm run validate` passed 131 unit and 105 integration tests, OpenAPI parity for 41 operations, Swagger, seven packaged migrations covering 18 tables, the 31,887-byte AGENTS limit, zero audit findings, and the production build. GitHub Actions run `31262026901` passed every CI step. Readiness confidence: **98/100**; the source and CI contract are complete, while exact hosted canonical/opaque dispatch behavior remains intentionally unclaimed and separately scoped as TASK-123.
- **TASK-136:** Prove pre-buffer bounds for accepted URL-encoded and JSON request bodies. DoD: declared, undeclared, undersized-header, malformed, and streamed oversized bodies stop at the shared bound before parsing or repository calls, return the documented error, and pass focused performance/security/OpenAPI tests plus full validation and CI.
  Acceptance evidence (2026-08-08): implementation commit `d1da9440a61e90307d92c61b1790b50c413082a5` passed `npm run validate` with 134 unit and 100 integration tests, 41 OpenAPI operations, Swagger parity, 18 tables in 7 Sites-packaged migrations, a 31,965-byte `AGENTS.md`, zero dependency vulnerabilities, and the production build. Ten declared, undeclared, undersized-header, malformed-header, and chunked overflow cases across accepted URL-encoded and JSON bodies returned the documented `413 invalid_request`, canceled at the shared bound, and made zero repository calls; malformed streams returned documented `400 invalid_request` with the same zero-call property. GitHub Actions run [`31261455426`](https://github.com/aittadb/aittadb/actions/runs/31261455426) passed every CI step for the exact implementation SHA. Readiness confidence: **98/100**; residual uncertainty is limited to live ChatGPT Sites/Cloudflare request-stream chunking and header normalization, which remain separately scoped to TASK-140, and no deployment was performed for this task.
- **TASK-137:** Prove pre-buffer bounds for canonical raw file uploads. DoD: declared, undeclared, undersized-header, malformed, and streamed oversized bodies stop before R2 writes or D1 metadata changes, return the documented error, and pass focused storage/performance/OpenAPI tests plus full validation and CI.
  Acceptance evidence (2026-08-08): implementation commit `fbb1ee69639e4daa4c7cc45e469e453630a3ca96` passed `npm run validate` with 134 unit and 113 integration tests, 41 OpenAPI operations, Swagger parity, 18 tables in 7 Sites-packaged migrations, a 31,965-byte `AGENTS.md`, zero dependency vulnerabilities, and the production build. Ten declared, absent, malformed-header, deliberately undersized-header, and chunked overflow cases across canonical collection `POST` and item `PUT` returned documented `413 invalid_request`, canceled at the shared observed-byte bound, made zero R2 puts and zero D1 file-metadata upserts, and retained the existing metadata and bytes. Two failed-stream cases returned documented `400 invalid_request` with the same non-mutation property; ordinary raw create/replace and the browser multipart regression suite remained green. GitHub Actions run [`31262203942`](https://github.com/aittadb/aittadb/actions/runs/31262203942) passed every CI step for that exact implementation SHA. Readiness confidence: **98/100**; provider-edge buffering, request-stream chunking, and header normalization on ChatGPT Sites/Cloudflare remain outside this local proof and are separately scoped to TASK-140. No deployment was performed.
- **TASK-107:** Gate only OAuth-client administration with the OAuth Apps flag. DoD: disabled deployments expose no client list/create/mutation controls, perform no client-administration write, keep the reserved internal browser client hidden and usable only by AittaDB, and pass authorization/non-persistence/discovery/OpenAPI/documentation tests plus full validation and CI.
  Acceptance evidence (2026-08-08): implementation commit `4d11578d137d001e390c2899d8389d37c65a4a50` evaluates the default-off gate at the outer request boundary, before Sites identity, repository access, rate limiting, body parsing, cleanup, or mutation. HTML, compatible JSON, and versioned hypermedia return `503 feature_unavailable`; root and session representations omit administration controls. Explicitly enabled allowlisted administration retains its existing behavior, while the migration-seeded browser-session client remains hidden from administration and continues to power signed-in record storage. Rebased `npm run validate` passed 131 unit and 106 integration tests, OpenAPI/Swagger/migration/AGENTS checks, zero audit findings, and the production build. GitHub Actions run `31261381180` passed every CI step. Readiness confidence: **98/100**; live deployment behavior is not part of this implementation task, and the separately scoped authorization, token, revocation, introspection, UserInfo, and discovery gates remain in TASK-108 and TASK-110 through TASK-112.
- **TASK-120:** Add a bounded orphan-repair processor for TASK-119 state. DoD: each invocation handles a deterministic finite batch, deletes only unreferenced physical objects, never deletes bytes referenced by current D1 metadata, retries safely, converges under injected failures, exposes no keys, and passes positive/race/failure/cleanup/documentation tests plus full validation and CI.
  Acceptance: implementation commit `6bdc6ea9e44ad7902d2530233a937432469ae0e2` passed `npm run validate` with 127 unit and 89 integration tests, OpenAPI/Swagger/migration/AGENTS checks, zero high-severity audit findings, and the production build. GitHub Actions run `31260408678` passed every CI step. The processor uses a 25-row indexed deterministic batch, an explicit missing/referenced/orphan D1 classification, repair-row metadata exclusion, global reference retention, owner-bound completion, fair retry rotation, idempotent R2 deletion, `waitUntil` scheduling, and no HTTP/OpenAPI surface. Readiness confidence: 97/100; residual uncertainty is limited to live D1/R2 timing and provider failure modes that cannot be reproduced without a deployed fault-injection facility, and no deployment was performed for this task.
- **TASK-118:** Verify the existing aggregate multipart bound on one exact approved Sites deployment. DoD: a lengthless streamed body larger than the documented wrapper limit receives AittaDB's `413`, produces no D1/R2 object or `Location`, reflects no credential, and only byte count and status evidence are retained.
  Acceptance evidence (2026-08-08): Sites version 36, source `1e79bc2522fd7da8828d6d152b4d4f1476bedc73`. The signed-in browser submitted a `10,747,905`-byte synthetic file through the canonical multipart collection form. DevTools recorded a multipart `POST /storage/files` with no `Content-Length`; AittaDB returned document status `413` with no `Location`. The browser rendered `Storage browser form is too large`, and the signed-in collection contained four rows both before and after the rejection, proving no D1 metadata or R2 object became reachable. No credential, cookie, file bytes, header value, or user identity was retained. Readiness confidence: **99/100**; exact physical-bucket inspection is intentionally unavailable to the caller, while the response and unchanged tenant collection prove the public contract.
- **TASK-145:** Verify the trusted Sites edge rejects forged upstream identity headers on one exact approved deployment. DoD: anonymous requests carrying caller-supplied `oai-authenticated-user-*` headers obtain no session, storage, statistics-private, account, or administration access; the real signed-in path still works; and no supplied or real identity value is retained.
  Acceptance evidence (2026-08-08): Sites version 36, source `1e79bc2522fd7da8828d6d152b4d4f1476bedc73`. Anonymous requests carrying synthetic `oai-authenticated-user-*` headers received `401` from session, record, file, and administration resources. The public UserInfo entry resource remained a protocol form containing only `title` and `protocol_response`, and public Statistics contained only `identity_count`; neither returned identity claims or private dimensions. The existing genuine signed-in browser session still rendered the File object storage resource with `Current signed-in session` selected and an upload control. Thirty-eight focused identity, current-session, isolation, and repository-policy tests passed. No supplied or real identity value, cookie, credential, count, or stored content was retained. Readiness confidence: **98/100**; this proves caller headers are not accepted at this exact edge revision, while the upstream platform remains part of the deployment trust boundary.
- **TASK-139:** Verify production security headers on one exact approved deployment. DoD: HTTPS responses include HSTS and the documented CSP, frame, MIME, referrer, permissions, cache, and cookie protections; local HTTP tests omit only HSTS as documented; statuses and header names, not sensitive values, are recorded.
  Acceptance evidence (2026-08-08): Sites version 36, source `1e79bc2522fd7da8828d6d152b4d4f1476bedc73`. Anonymous root and health requests returned `200`, the protected file route returned `401`, and the `aittadb.com` root returned `200`. Every response included `strict-transport-security`, `content-security-policy`, `x-frame-options`, `x-content-type-options`, `referrer-policy`, `permissions-policy`, and `cache-control`. The Sites edge cookie exposed only attribute-category evidence: `Secure`, `HttpOnly`, `SameSite`, path, domain, and expiry; no name or value was retained. Forty-five focused tests passed for HTTPS-only HSTS, local-HTTP omission, AittaDB's secure `SameSite=Lax` CSRF cookie continuity/rejection, and secure `SameSite=Strict` one-time administration result state. Readiness confidence: **97/100**; browser JavaScript correctly cannot read `Set-Cookie`, so AittaDB cookie attributes are proven by the exact source and tests rather than by retaining a live header value.
- **TASK-129:** Prove bounded forward record pagination with a large automated fixture. DoD: deterministic pages never exceed the configured maximum, following returned next links yields every authorized item exactly once with no omissions/duplicates, malformed and cross-namespace cursors fail, and HTML/hypermedia/OpenAPI/documentation tests pass full validation and CI.
  Acceptance evidence (2026-08-08): a 237-record bearer fixture traversed 14 server-provided hypermedia pages at the configured 17-item maximum with every authorized key returned exactly once; a 41-record signed-in fixture traversed three rendered HTML next-page controls with the same bounds and exact-once result. Malformed and oversized inputs and cursors reused across another client or user returned `400 invalid_request`; each control namespace listed only its own record. No runtime defect was found. Local `npm run validate` passed 121 unit and 94 integration tests, zero high-severity audit findings, OpenAPI/Swagger/migration/AGENTS checks, and the production build. Implementation commit `27569735e36f72d574672bba9c83f6395e345a64` passed GitHub CI run `31260235665`. Readiness confidence: **98/100**; concurrent collection mutation semantics and hosted pagination remain separately scoped.
- **TASK-130:** Prove bounded forward file pagination with a large automated fixture. DoD: deterministic pages never exceed the configured maximum, following returned next links yields every authorized item exactly once with no omissions/duplicates or physical keys, malformed and cross-namespace cursors fail, and HTML/hypermedia/OpenAPI/documentation tests pass full validation and CI.
  Acceptance evidence (2026-08-08): a 211-file bearer fixture traversed 12 server-provided hypermedia pages at the configured 19-item maximum with every authorized logical file returned exactly once; a 41-file signed-in fixture traversed three rendered HTML next-page controls with the same bounds and exact-once result. Every listed D1 metadata row matched its fake R2 bytes, content type, size, and SHA-256 digest. Responses exposed no physical R2 key, local-user/client identifier, or other tenant's file; malformed and oversized inputs and cursors reused across another client or user returned `400 invalid_request`, while each control namespace listed only its own file. No runtime defect was found. Local `npm run validate` passed 121 unit and 96 integration tests, zero audit findings, OpenAPI/Swagger/migration/AGENTS checks, and the production build. Implementation commit `96d60bf616fa6dc702076035c7721232dc8d7079` passed GitHub CI run `31260867107`. Readiness confidence: **98/100**; concurrent collection mutation semantics and hosted file-pagination verification remain separately scoped, with the hosted proof retained as TASK-132.
- **TASK-121:** Prove the shared browser-origin classifier independently of route handlers. DoD: canonical origin and documented Sites dispatch are accepted; missing, malformed, foreign, opaque cross-site, and unauthorized `null` origins are rejected; CSRF remains a separate control; focused unit tests and threat-model documentation pass full validation and CI.
- **TASK-119:** Add durable orphan-repair state for file mutations whose D1 and R2 compensation both fail. DoD: one migration and narrow repository contract record only non-secret physical repair metadata, deduplicate retries, exclude caller access, preserve tenant isolation, and pass migration, disclosure, dual-failure, documentation, full-validation, commit, and CI checks.
- **TASK-102:** Decompose every remaining umbrella entry in `PLAN.md` into the smallest practical flat, dependency-ordered implementation or acceptance tasks, each owning one explicit server primitive, protocol behavior, security control, or narrowly bounded live proof with a clear pass/fail definition of done; preserve retired umbrella text and its replacement-task mapping in `CHANGELOG.md` without claiming the underlying product work is complete; move genuinely future enhancements to `ROADMAP.md`; replace duplicated or umbrella release-validation clauses with separate narrow hosted proof tasks; update the compact planning guidance in `AGENTS.md` without exceeding 32,000 bytes; synchronize the Codex plan; validate repository-policy checks; and commit and push the planning-only change.
- **TASK-103:** Reclassify downstream third-party OAuth Apps as an unfinished opt-in capability rather than a current default AittaDB feature: add focused stable entries to `BACKLOG.md` for its remaining client-management, grant, token-lifecycle, and hosted-acceptance work; remove OAuth-App-only residuals from the current implementation queue after a fail-closed feature gate is planned; keep AittaDB's internal ChatGPT Sites browser session distinct; align current-capability planning language; and pass repository-policy checks in a focused planning commit.
- **TASK-104:** Add a typed environment-backed feature-availability contract with independent booleans for JSON Records, File Storage, Service Statistics, and downstream OAuth Apps. DoD: Records, Files, and Statistics default on; OAuth Apps defaults off; malformed values fail closed; effective state appears in versioned HTML/hypermedia metadata; `.env.example`, configuration, architecture, and OpenAPI agree; and focused tests, full validation, a focused commit, and CI pass.
- **TASK-105:** Gate only the JSON Records route family with the Records flag. DoD: disabled deployments execute no record repository call, omit record links/actions from HTML and hypermedia, return the documented content-negotiated unavailable response, leave other features unchanged, and pass route/non-persistence/discovery/OpenAPI/documentation tests plus full validation and CI.
- **TASK-106:** Gate only the File Storage route family with the Files flag. DoD: disabled deployments execute no D1 metadata or R2 operation, omit file links/actions from HTML and hypermedia, return the documented content-negotiated unavailable response, leave other features unchanged, and pass route/non-persistence/discovery/OpenAPI/documentation tests plus full validation and CI.
- **TASK-113:** Restore one concise home-page sentence explaining that ChatGPT provides browser sign-in inside ChatGPT Sites while AittaDB creates a separate local identity and its own credentials and never receives or forwards ChatGPT credentials. DoD: HTML fallback and runtime rendering, focused copy tests, style guidance, formatting, types, relevant tests, build, focused commit, and CI pass without reintroducing repetitive licensing prose or implying official ChatGPT OAuth.
- **TASK-109:** Gate only `/statistics` with the Statistics flag. DoD: disabled deployments perform no aggregate identity query, omit the statistics link/action, return the documented content-negotiated unavailable response, preserve health and other metadata, and pass no-query/discovery/OpenAPI/documentation tests plus full validation and CI.
- **TASK-114:** Verify the TASK-113 sentence on one exact approved Sites deployment. DoD: signed-out and signed-in root HTML both contain the sentence and compact FSL note, JSON metadata remains accurate, no credentials or identity values are recorded, and the source SHA, deployment version, statuses, and confidence are documented.
  Acceptance evidence (2026-08-08): source `42379895eae324944f18e08f34f85885ae90ca93`, Sites version 35, and deployment status `succeeded`. Signed-out HTML returned `200` with the concise ChatGPT sign-in sentence, compact FSL note, and sign-in action on both the Sites hostname and `aittadb.com`; the existing signed-in root returned the same sentence and a sign-out action. Versioned JSON returned `200` with AittaDB service metadata, the documented upstream-sign-in boundary, and effective Records/Files/Statistics-on plus OAuth-Apps-off state. Evidence retained no credential or identity value. The first anonymous request observed the previous body during propagation; cache-busted checks converged on both hosts. Readiness confidence: **98/100**, with ordinary post-deployment edge propagation as the residual uncertainty.
- **TASK-115:** Prove browser-upload CSRF continuity as one token/cookie primitive. DoD: automated fresh, concurrent-tab, stale, sign-in-return, multipart, and retry cases show matching fresh forms succeed, stale forms fail, retries recover, and rejected submissions perform zero D1/R2 mutation; any defect is fixed with synchronized security documentation; full validation and CI pass.
- **TASK-138:** Prove trusted Sites identity is established before a browser multipart stream is pulled. DoD: anonymous or invalid-identity uploads cause zero stream reads and zero D1/R2 calls, valid same-origin signed-in uploads still parse within TASK-115 bounds, and focused tests, threat-model guidance, full validation, and CI pass.
  Acceptance evidence (2026-08-08): pull-counted collection-create and item-replacement streams with absent or malformed Sites identity returned the same-origin sign-in redirect with zero body pulls, zero repository calls, zero R2 calls, and no scheduled cleanup. A valid same-origin request using the production Sites-header parser, a server-issued CSRF token, and a two-chunk multipart stream returned `201`, consumed the bounded body, and persisted exactly one metadata row and one R2 object. The proof exposed and fixed a runtime defect where two D1-backed storage rate-limit writes and occasional cleanup scheduling occurred before the existing inner identity guard. The outer lifecycle now rejects first while the inner storage guard remains defense in depth; raw bearer uploads are unchanged. Local `npm run validate` passed 122 unit and 100 integration tests, OpenAPI/Swagger/migration/AGENTS checks, zero audit findings, and the production build. Implementation commit `cb3240af4738540fbea1b95f301d2f172c4de4b4` passed GitHub CI run `31261062550`. Readiness confidence: **98/100**; AittaDB's Worker request handling is proven locally and in CI, while provider-edge buffering before the Worker receives a request remains outside the application contract and was not live-tested in this task.
- **TASK-116:** Verify browser-upload CSRF continuity on one exact approved Sites deployment. DoD: a fresh and two concurrent forms upload tiny non-sensitive files, a stale form returns `403` without persistence, a fresh retry succeeds, secret-safe status evidence is recorded, and temporary files are retained only pending separately approved cleanup.
  Acceptance evidence (2026-08-08): source `1e79bc2522fd7da8828d6d152b4d4f1476bedc73`, Sites version 36, deployment `succeeded`. One fresh form and two concurrently opened forms created three distinct tiny text files. After a deliberate CSRF-cookie rotation, the already-open form returned `HTTP 403` with `CSRF validation failed`, and the collection remained at three files; a newly rendered form retried the same fixture successfully and raised the collection to four. The temporary test cookie was removed and a fresh server-issued CSRF session rendered the four-file collection. Evidence contains no credential or identity value. The four logical fixture keys are retained only in TASK-117 for narrowly confirmed cleanup. Readiness confidence: **98/100**; broader hosted browser-origin behavior remains separately tracked.

- **TASK-001:** Establish the Sites-compatible TypeScript project scaffold, package metadata for Jaakko Heusala, FSL-1.1-MIT release posture, `.openai/hosting.json` with logical `DB`, `.env.example`, strict compiler settings, formatting/lint/test/build scripts, CI workflow, starter health metadata contract, scaffold tests, README setup notes, and acceptance evidence from install, formatting, type checking, tests, and build.
- **TASK-002:** Define the D1 schema and checked-in migrations for users, OAuth clients, redirect URIs, allowed scopes, authorization requests, authorization codes, device grants, refresh-token families, refresh tokens, remembered consents, revoked JWT IDs, audit events, and rate-limit counters, including repository interfaces, migration tests, schema documentation, cleanup indexes, and migration consistency validation.
- **TASK-003:** Implement configuration loading and production/test environment separation, including typed public configuration, required secret validation, secure defaults, `.env.example` documentation, tests for missing and malformed config, and `AGENTS.md` updates if commands or required variables change.
- **TASK-004:** Implement security-focused HTTP foundations, including routing conventions, strict security headers, no-store helpers, request-size limits, error response helpers, CORS allowlist behavior, credential redaction, audit logging interface, root metadata response, tests, OpenAPI documentation, and validation evidence.
- **TASK-005:** Implement the ChatGPT Sites upstream identity provider, including trusted header parsing, percent-encoded UTF-8 display-name decoding, email fallback, production rejection of test-only identity adapters, user/developer documentation of the trust boundary, and unit tests for valid, missing, malformed, and unsupported header cases.
- **TASK-006:** Implement local user creation and lookup, including immutable UUID subject generation from upstream email lookup, email reassignment risk documentation, repository tests, integration tests for repeat lookup, OpenAPI/userinfo schema implications, and acceptance evidence.
- **TASK-007:** Implement cryptographic primitives, including secure random values, SHA-256 hashing, constant-time comparison, PKCE `S256`, ES256 JWT signing and validation with configured JWK and `kid`, JWKS generation, local key-generation script, key-rotation documentation, incorrect-algorithm and unknown-key tests, and validation evidence.
- **TASK-008:** Implement OAuth client administration, including allowlisted ChatGPT-sign-in-protected admin API and minimal HTML form, list/create/disable/secret-rotate/revoke-grants operations, public versus confidential clients, exact redirect URIs, allowed scopes, allowed origins, one-time secret display, hashed confidential secrets, CSRF protection, tests, OpenAPI paths, and admin documentation.
- **TASK-009:** Implement client authentication and scope validation shared by OAuth endpoints, including public-client handling, confidential-client constant-time secret checks, disabled-client rejection, allowed-scope enforcement for `openid`, `email`, `profile`, and `offline_access`, generic errors, tests, OpenAPI schemas, and documentation.
- **TASK-010:** Implement OAuth 2.0 Device Authorization Grant issuance, including high-entropy device code hashing, short user code generation, verification URI and complete URI, expiry, polling interval, rate limits, tests for valid and invalid clients/scopes, OpenAPI examples, CLI documentation, and acceptance evidence.
- **TASK-011:** Implement device-code browser verification and consent, including Sites sign-in continuation, code entry, consent display with client name and local scopes, approve/deny actions, one-time approval, CSRF and same-origin checks, denial and expiry handling, minimal accessible HTML, tests, OpenAPI/browser-route documentation, and validation evidence.
- **TASK-012:** Implement device-flow token polling, including `authorization_pending`, `slow_down`, `access_denied`, `expired_token`, success token issuance, optional ID token for `openid`, optional refresh token for approved `offline_access`, polling rate limits, integration tests for success and failure paths, OpenAPI examples, and acceptance evidence.
- **TASK-013:** Implement Authorization Code with PKCE authorize request intake, including request validation, exact redirect URI matching, `state` and OIDC `nonce` preservation, authorization request persistence, internal same-origin continuation, standard OAuth errors, no open redirects, tests, OpenAPI documentation, browser/native PKCE example, and validation evidence.
- **TASK-014:** Implement Authorization Code consent and code issuance, including required explicit consent unless an exact remembered consent exists, one-time high-entropy authorization code hashing, short expiry, CSRF and same-origin checks, error display, minimal accessible HTML, tests, OpenAPI/browser-route documentation, and acceptance evidence.
- **TASK-015:** Implement Authorization Code token exchange, including PKCE verification, one-time code consumption, client authentication where required, access token issuance, ID token issuance with nonce when `openid` was requested, optional refresh token issuance, failure-path tests, OpenAPI examples, and acceptance evidence.
- **TASK-016:** Implement refresh-token rotation and reuse detection, including opaque token hashing, family tracking, rotation on every use, reuse-triggered family revocation, expiry behavior, tests, OpenAPI token examples, security documentation, and validation evidence.
- **TASK-017:** Implement OpenID Connect discovery, JWKS, JWT bearer access tokens, ID tokens, service-side token validation, external JWKS verification example, tests for issuer/audience/expiration/`nbf`/`iat`/`jti`/`sub`, OpenAPI documentation, and acceptance evidence.
- **TASK-018:** Implement UserInfo, token introspection for authorized confidential clients, and token revocation, including bearer-token validation, revoked-JTI tracking, refresh-token revocation, inactive-token responses, client authorization rules, tests, OpenAPI documentation, curl examples, and validation evidence.
- **TASK-019:** Implement rate limiting and bounded cleanup, including D1-backed counters for device authorization, token polling, client authentication, and admin operations, expiration cleanup for short-lived credentials, tests for limit and cleanup behavior, operational documentation, and validation evidence.
- **TASK-020:** Complete the canonical OpenAPI 3.1 specification and minimal `/docs` viewer, including all REST endpoints, OAuth errors, schemas, examples for Device Authorization and PKCE, route/spec divergence tests, OpenAPI validation in CI, and documentation of the Sites identity boundary.
- **TASK-021:** Complete repository documentation, including `README.md`, `LICENSE.md`, `SECURITY.md`, `CONTRIBUTING.md`, `CHANGELOG.md`, architecture documentation, threat model, deployment guide, self-hosting limitations, Sites setup-from-fork instructions, CLI example, browser/native PKCE example, curl examples, key generation and rotation instructions, downstream JWT verification example, tests for documented commands where applicable, and validation evidence.
- **TASK-022:** Complete end-to-end integration tests for Device Authorization and Authorization Code with PKCE using the test-only identity adapter, including local UUID subjects, consent enforcement, token validation, refresh rotation, revocation, introspection, CORS, CSRF, rate limits, OpenAPI validation, migrations, production build output, and full-suite validation evidence.
- **TASK-023:** Review and harden protocol and security behavior, including no token values in URLs or logs, no ChatGPT credential forwarding, no user-controlled issuer/audience, exact origin policy, generic authentication errors, cache controls, accessibility of mandatory HTML, threat-model coverage, `AGENTS.md` updates, and acceptance evidence from targeted regression tests.
- **TASK-024:** Finalize the auditable branch, including all completed task checkboxes, final formatting/linting/type checking/unit/integration/OpenAPI/migration/build validation, focused commit, optional authenticated GitHub push and draft pull request without deploying, and final report evidence.
- **TASK-025:** Implement content-negotiated browser response views and hypermedia JSON error affordances, including the shared HTML page shell, route-specific metadata/health/device/consent/admin/error pages, JSON `_links` and `actions` for discoverable API errors and device authorization, tests for browser and API response variants, README/OpenAPI/AGENTS.md updates, and acceptance evidence from formatting, linting, type checking, tests, OpenAPI validation, migration checks, and production build validation.
- **TASK-026:** Upgrade the browser response visual design to a modern authentication-service experience, including the refined shared HTML shell, identity-to-token visual treatment, responsive form/table styling, tests that assert the browser visual shell is present on success and error pages, README documentation of the no-external-asset boundary, and acceptance evidence from formatting, linting, type checking, tests, OpenAPI validation, migration checks, and production build validation.
- **TASK-027:** Add the public GitHub repository affordance to browser and repository documentation, including the shared HTML shell badge/link to `https://github.com/sendanor/sites-auth-broker`, README repository link, integration test coverage for the visible browser link, and acceptance evidence from formatting, linting, type checking, tests, OpenAPI validation, migration checks, and production build validation.
- **TASK-028:** Fix browser UI stylesheet delivery under strict CSP, including the same-origin `/auth-ui.css` route, removal of inline page styles from generated HTML, explicit `style-src 'self'` CSP, tests for linked CSS and stylesheet content, README/AGENTS.md updates, and acceptance evidence from formatting, linting, type checking, tests, OpenAPI validation, migration checks, and production build validation.
- **TASK-029:** Implement broker-local storage for third-party clients, including `storage.read`, `storage.write`, and `storage.delete` scopes; D1 JSON record and file-metadata schema; R2 file-byte binding `BUCKET`; per-local-user and per-client authorization; logical-key validation and generated physical R2 keys; hypermedia JSON storage responses; OpenAPI, README, architecture, deployment, examples, threat-model, style-guide, and `AGENTS.md` documentation; integration tests for record/file success and insufficient-scope failure; and acceptance evidence from formatting, linting, type checking, tests, OpenAPI validation, migration checks, production build validation, local commit, and approved Sites preview deployment.
- **TASK-030:** Replace the browser shell's schematic CSS artwork with an original locally hosted identity-broker visual system, including a responsive image-led composition shared by every HTML response, page- and tone-aware visual copy, accessible decorative-image handling, strict-CSP-compatible asset delivery, browser-shell and asset-reference tests, README/style-guide/AGENTS.md documentation, desktop and mobile visual QA, and acceptance evidence from formatting, linting, type checking, tests, OpenAPI validation, migration checks, production build validation, focused commit, and approved Sites preview deployment.
- **TASK-031:** Replace ambiguous standalone "Sites identity" product wording with an explicit ChatGPT sign-in boundary, including user-facing HTML copy, root JSON metadata contract, OpenAPI and application metadata, terminology rules in README/style-guide/AGENTS.md, regression tests proving the upstream ChatGPT sign-in and independent local-token distinction, and acceptance evidence from formatting, linting, type checking, tests, OpenAPI validation, migration checks, production build validation, focused commit, and approved Sites preview deployment.
- **TASK-032:** Rebrand the complete project and public contract as AittaDB, including package and repository identity, `Aitta`/`DB` wordmark typography and colors, the supplied local SVG mark with teal accent, self-hosted Inter typography with declared fallbacks, every content-negotiated HTML response, hypermedia JSON metadata, OpenAPI, compatibility-conscious internal names and headers, automated regression tests, README/style-guide/architecture/AGENTS documentation, and acceptance evidence from formatting, linting, type checking, tests, OpenAPI validation, migration checks, production build validation, browser visual QA, focused commit, approved Sites preview deployment, and GitHub publication when write access is available.
- **TASK-033:** Make clean-checkout validation independent of ignored deployment metadata, including a typed build-time hosting-config loader that prefers local `.openai/hosting.json` and safely falls back to `.openai/hosting.example.json`, malformed-config failure behavior, automated loader tests, contributor/deployment/AGENTS documentation, and acceptance evidence from the full validation suite and passing GitHub Actions CI.
- **TASK-034:** Add a public-to-authenticated browser entry path into the real AittaDB service, including an unauthenticated home that clearly explains when sign-in is required, links to every production protocol and storage surface rather than a mock demo, a ChatGPT-sign-in-protected local session view backed by the production Sites identity adapter and durable local user record, immutable AittaDB subject details, equivalent root JSON hypermedia, tests, OpenAPI/internal-route documentation, style-guide and AGENTS updates, and acceptance evidence from formatting, linting, type checking, tests, and production build validation.
- **TASK-035:** Add content-negotiated browser representations to the production OAuth, OpenID Connect, and schema endpoints, including readable discovery/JWKS/OpenAPI views and a self-hosted, CSP-compatible Swagger UI at `/docs` backed only by the canonical `/openapi.json`; functional authorization, device authorization, token, revocation, introspection, and UserInfo forms that invoke the same route and domain logic as API clients; HTML success and failure results; strict preservation of canonical JSON and OAuth response contracts; CSRF and same-origin protection for browser-only form operations; no credentials in URLs; no mock users, grants, or tokens; automated success and failure tests; OpenAPI/examples/threat-model/AGENTS documentation; and acceptance evidence from the full validation suite.
- **TASK-036:** Add functional browser representations to the production AittaDB record and file storage endpoints, including bearer-token entry without URL exposure, list/read/write/delete record operations, list/read/upload/download/delete file operations using the same scoped D1/R2 service logic as REST clients, bounded multipart handling, CSRF and same-origin checks, content-aware HTML results, unchanged D1/R2 REST contracts, no mock or browser-only storage state, automated scope/security/failure tests, OpenAPI/examples/threat-model/AGENTS documentation, and acceptance evidence from the full validation suite.
- **TASK-038:** Resolve the pre-release dependency and tooling audit before hosted acceptance, including compatible patched React/RSC, Vinext, Vite, Cloudflare, and Wrangler versions; correct runtime dependency classification; removal of unused Drizzle Kit, incomplete generation commands/configuration, and redundant Fontsource package; explicit local and CI high-severity `npm audit` enforcement; lockfile, command, migration-workflow, contributor, AGENTS, and Dependabot documentation/configuration updates; Vite configuration compatibility cleanup; and acceptance evidence from a clean lockfile install, zero high-severity findings, formatting, linting, type checking, all tests, OpenAPI/Swagger/migration checks, and production build validation.
- **TASK-039:** Harden and prove the application-storage isolation boundary before hosted acceptance, including a public contract that storage APIs expose only application records/files owned by the access token's immutable local user UUID and OAuth client ID; no generic D1 query, internal-auth-table, R2-key, binding, environment, signing-key, or secret exposure; attachment-safe file retrieval; automated cross-user and cross-client read/list/write/delete denial tests for D1 records and R2 files; private-JWK and physical-object-key non-disclosure tests across success and failure responses; architecture, threat-model, OpenAPI, SECURITY, and `AGENTS.md` updates; and acceptance evidence from formatting, linting, type checking, all tests, OpenAPI/Swagger/migration checks, audit, and production build validation. This task was discovered during live acceptance and is intentionally completed before TASK-037 resumes.
- **TASK-040:** Isolate the self-hosted Swagger UI from AittaDB shell styling before hosted acceptance, including removal of unscoped AittaDB heading, form-control, button, preformatted-output, and table selectors that override Swagger components; preserved branded framing around the viewer; regression tests for selector isolation and Swagger assets; style-guide and `AGENTS.md` updates; Chrome verification of endpoint and Schemas controls including `Expand all`; and acceptance evidence from formatting, linting, type checking, all tests, OpenAPI/Swagger checks, and production build validation. This task was discovered during live acceptance and is intentionally completed before TASK-037 resumes.
- **TASK-041:** Remove avoidable request-path latency before hosted acceptance, including deployment-owned D1 migration application instead of per-request DDL; direct pass-through for Vinext/static assets; no D1 work for public metadata routes; bounded non-authoritative cleanup scheduling only on durable-state requests; parallel OAuth-client metadata hydration; focused regression tests proving public requests issue no D1 statements and runtime requests issue no schema DDL; architecture, deployment, performance, and `AGENTS.md` documentation; live before/after TTFB measurements; and acceptance evidence from formatting, linting, type checking, all tests, migration checks, audit, production build validation, Worker logs, and Chrome QA. This task was discovered during live acceptance and is intentionally completed before TASK-037 resumes.
- **TASK-042:** Make the signed-in session useful across the browser operation surface without weakening API protocols, including a reserved non-administrable AittaDB browser client and migration; current-session and explicit access-token modes for record/file forms, both invoking the canonical scoped storage endpoint; local UUID plus browser-client isolation; current-session affordances and accurate protocol-boundary copy for Device Authorization, Authorization Code with PKCE, UserInfo, and client administration; no implicit replacement for client credentials, grants, PKCE, consent, revocation, or introspection; automated signed-in, signed-out, isolation, secret-non-disclosure, and token-mode tests; OpenAPI, architecture, threat-model, examples, style-guide, and `AGENTS.md` updates; Chrome QA; and acceptance evidence from the full validation suite. This task was discovered during live acceptance and is intentionally completed before TASK-037 resumes.
- **TASK-043:** Make CSRF-protected browser operations reliable across concurrent tabs, including a validated host-only browser-session CSRF token reused across operation pages without weakening same-origin enforcement; rejection of missing, malformed, mismatched, and cross-origin submissions; regression tests that open multiple forms before submitting the first; architecture, threat-model, OpenAPI, and `AGENTS.md` documentation; live signed-in multipart upload verification; and acceptance evidence from the full validation suite. This task was discovered during live R2 acceptance and is intentionally completed before TASK-037 resumes.
- **TASK-037:** Complete public-domain route parity and deployment acceptance, including canonical `https://aittadb.com` issuer configuration, absolute hypermedia and social metadata verification, browser/API content-negotiation checks for every endpoint, desktop and mobile visual QA, real Sites authentication and representative protocol/storage E2E checks, GitHub CI, focused commits, approved Sites deployment without access-policy changes, and an auditable final PLAN state.
- **TASK-044:** Align every public AittaDB HTML, hypermedia JSON, OpenAPI, package, and repository description with the implemented hosted application-backend scope, including primary positioning for third-party applications, ChatGPT-based sign-in, AittaDB-issued OAuth/OIDC sessions, isolated D1 JSON records, and R2 file storage; removal of sign-in-flow internals from normal user operation maps while preserving every production endpoint, directly opened HTML representation, API documentation, and hypermedia contract; preservation of the independent issuer, no-ChatGPT-credential, no-ChatGPT-data, and source-available boundaries; explicit treatment of persistent events as planned rather than implemented; regression tests, README/style-guide/`AGENTS.md`/changelog updates, full validation, focused commit, GitHub publication, and approved Sites deployment with live acceptance evidence.
- **TASK-046:** Keep the authoritative root `AGENTS.md` within Codex's default instruction budget, including a compacted but complete project contract below a strict 32,000-byte ceiling; an explicit maintenance rule that favors concise edits and moves explanatory detail to linked project documentation without weakening mandatory boundaries; an automated byte-size check wired into local validation and CI; command, contributor, and `AGENTS.md` documentation updates; failure-path coverage proving oversized instructions fail validation; and acceptance evidence from formatting, linting, type checking, tests, documentation-size validation, audit, and production build.
- **TASK-047:** Establish the versioned Hypermedia JSON contract foundation, including the canonical `docs/hypermedia-json-rest-api.md` definition; preview media type and version signaling; typed resource, link, action, authorization, field, option, and condition interfaces; shared constructors and lookup helpers; same-URI `Accept` negotiation without User-Agent detection; unsupported-version rejection; standard OAuth payload exception rules; automated document-shape, media-negotiation, browser/CLI User-Agent-independence, and version failure-path tests; README, architecture, examples, changelog, OpenAPI media-type, and compact `AGENTS.md` updates; and acceptance evidence from complete validation, focused commit, GitHub CI, approved Sites deployment, and live negotiation verification.
- **TASK-048:** Replace storage-operation JSON dumps in negotiated HTML with resource-aware storage views, including file and record collection tables with item links and explicit empty states; readable item metadata and record values; upload, write, read, list, and delete outcomes with only context-valid next operations; safe escaping, accessible tables and status text, responsive layout, and no exposure of physical R2 keys, user IDs, client internals, credentials, or deployment secrets; shared use of the same authorized storage resource model as hypermedia JSON; automated populated, empty, item, deletion, escaping, isolation, and failure-path coverage; OpenAPI, style-guide, architecture, changelog, and compact `AGENTS.md` updates; and acceptance evidence from complete validation, focused commits, GitHub CI, approved Sites deployment, and live browser verification.
- **TASK-049:** Make `PLAN.md` the mandatory first capture point for every repository-affecting user request, including follow-up requirements received while another task is in progress; require agents to add or amend an unchecked flat task before implementation, preserve the request through implementation and review, distinguish repository work from conversational questions, keep partially completed work unchecked, run independent reads, validation commands, and non-overlapping implementation units in parallel whenever practical while serializing dependent, overlapping, and security-sensitive changes and using isolated Git worktrees only when they reduce conflict without fragmenting the canonical checkout; commit every intended source change before handoff; update the compact `AGENTS.md` and contributor workflow, add focused policy regression coverage within the instruction-size budget, and record acceptance evidence from documentation checks and the full validation suite.
- **TASK-050:** Tighten the public service-entry trust-boundary copy by replacing the two long informational notices with one concise, accurate statement and a clear CTA-style link to maintained documentation explaining that this public instance runs on ChatGPT Sites and that AittaDB is reusable software developers can independently deploy on their own ChatGPT Sites project as the database, file-storage, and authentication backend for their solutions; also explain upstream sign-in, separate AittaDB identity and credentials, D1/R2 capabilities, independence from OpenAI, credential non-forwarding, self-hosted deployment ownership, and planned-event status; present the shared product-domain label as `Identity / Data / Files / Events` while every capability list and operation map continues to identify Events as planned and unavailable in the current MVP; preserve all legally and security-relevant boundaries without creating a marketing or duplicate documentation page; keep the CTA accessible, responsive, and consistent with the AittaDB visual system; add focused content and link tests; update README/style guidance/changelog and compact `AGENTS.md` only where the interface contract changes; and record acceptance evidence from complete validation, focused commit, GitHub CI, approved Sites deployment, and live browser review.
- **TASK-051:** Make the public entry resource's primary session control and operation copy reflect the actual trusted Sites authentication state, showing `Sign in to AittaDB` and sign-in guidance only when no upstream identity is present and `Sign out` plus direct private-resource guidance only when already signed in; use the Sites-owned sign-in/sign-out routes with same-origin continuation, keep HTML and hypermedia action names/targets equivalent, never infer state from browser JavaScript, cookies controlled by AittaDB, or user-supplied headers outside the trusted adapter, and add signed-in, signed-out, label, copy, target, exclusivity, and failure-path tests plus compact interface documentation and full acceptance evidence.
- **TASK-052:** Add a public, privacy-preserving service statistics resource at a conventional same-URI content-negotiated endpoint, including only the aggregate number of durable local identities; a readable HTML count and equivalent versioned hypermedia JSON state with navigation; prepared repository count access without retrieving identity rows; no email addresses, names, local UUIDs, per-client counts, activity data, secrets, or other identifying/deployment information; explicit non-authoritative cache behavior; automated empty and populated count, HTML/JSON negotiation, privacy, database-failure, OpenAPI route-parity, and public-access tests; OpenAPI, README, architecture, privacy/security, changelog, and compact `AGENTS.md` updates; and acceptance evidence from complete validation, focused commit, GitHub CI, approved Sites deployment, and live browser/API verification.
- **TASK-053:** Make the self-hosted Swagger UI execute simple public `GET` operations reliably, including correct canonical server resolution, JSON `Accept` negotiation for dual HTML/JSON resources, CSP-compatible viewer behavior, functional `Try it out` and response rendering for `/health` and `/statistics`, preserved OpenAPI 3.1 schemas and route descriptions, visible failure feedback rather than silent controls, no credential persistence or external assets, automated Swagger bootstrap/configuration and public-GET request tests, OpenAPI/style-guide/architecture/changelog/compact `AGENTS.md` updates where needed, and acceptance evidence from complete validation, focused commit, GitHub CI, approved Sites deployment, and live Chrome verification.
- **TASK-054:** Convert the service root, health, local-session, and generic error resources to the canonical hypermedia model while preserving equivalent content-negotiated HTML, including absolute semantic links, currently valid actions only, trusted Sites-session authorization state, anonymous sign-in controls, signed-in storage/session controls, administrative-action omission for non-admins, generic failure details without private diagnostics, automated HTML/JSON capability-equivalence and authorization-state tests, route-specific OpenAPI plus README/architecture/style-guide/changelog/compact `AGENTS.md` updates, and acceptance evidence from complete validation, focused commit, GitHub CI, approved Sites deployment, and live traversal verification.
- **TASK-058:** Complete the canonical OpenAPI 3.1 hypermedia contract and route-divergence guard after all resource units are represented, including reusable link, field, authorization, action, document, error, statistics, session, storage, administration, and protocol schemas; versioned vendor and compatible JSON response media types; HTML alternatives; every implemented public/internal REST path and method; examples for dynamic controls and standards-defined exceptions; validation that implemented and documented route/method sets match; Swagger schema rendering regression coverage; README/architecture/examples/changelog/compact `AGENTS.md` updates; and acceptance evidence from OpenAPI validation, full validation, focused commit, GitHub CI, approved Sites deployment, and live `/openapi.json` plus `/docs` verification.
- **TASK-059:** Document ChatGPT Sites storage-limit uncertainty without inventing capacity claims, including verification against the current official Sites and ChatGPT Library limit documentation; README wording that D1 database and R2 object storage are subject to plan-specific aggregate public-beta limits displayed by ChatGPT; an explicit statement that no public numerical D1/R2 capacity, allocation, object-size, row/query, bandwidth/operation, or additional-pricing figures are currently documented; a clear distinction from the unrelated ChatGPT Library quota; source links and an as-of date that does not imply permanence; tests or documentation checks preventing unsupported fixed-capacity marketing claims; any compact `AGENTS.md` operational guidance needed within its byte ceiling; and acceptance evidence from formatting, link/content review, the full validation suite, focused commit, GitHub CI, approved Sites deployment, and live documentation verification.
- **TASK-060:** Create and maintain a root `ROADMAP.md` for future AittaDB capabilities as a flat list of stable unchecked items distinct from the auditable implementation work in `PLAN.md`; inventory the current storage and protocol surface before listing only genuinely unimplemented work; represent each missing record/file CRUD or metadata operation as its own item; decompose Events into independently reviewable persistence, publication, retrieval, delivery, cursor, retention, and authorization capabilities; separately cover administrator-controlled storage ACLs, per-user and per-client quotas, record/file count and byte limits, content-type policy, JSON Schema validation, usage visibility, enforcement behavior, and safe policy migration; keep roadmap claims compatible with unknown ChatGPT Sites D1/R2 platform quotas; link the roadmap from README and contributor/agent guidance without implying planned work is available; add documentation regression checks for flat unchecked stable items and capability labeling; and record acceptance evidence from formatting, linting, type checking, tests, documentation checks, full validation, focused commit, GitHub CI, approved Sites deployment, and live README-link verification.
- **TASK-061:** Remove the obsolete unused Vinext starter identity helper that trusts or models the undocumented `oai-authenticated-user-id` header, leaving `src/identity.ts` as the sole production upstream Sites identity adapter; retain only documented server-side email, optional full name, and encoding inputs; keep the immutable subject locally generated; add a repository regression scan rejecting the undocumented header and duplicate production adapters; update compact architecture/agent guidance only if ownership changes; and record acceptance evidence from formatting, linting, type checking, identity/unit/integration tests, full validation, focused commit, GitHub CI, approved Sites deployment, and live sign-in verification without claiming an upstream stable subject.
- **TASK-062:** Make the upstream ChatGPT Sites identity boundary impossible to bypass through production configuration, including an explicit `UpstreamIdentityProvider` interface; a production adapter that reads only the documented trusted Sites request headers; dependency injection into browser-session, transaction, administration, and storage entry points; a mock adapter located only in test sources; removal of deployable `TEST_AUTH_*` bindings and runtime branches; retained production/test environment-mode behavior only where it is unrelated to identity; automated production-source scans, signed-in/signed-out integration coverage, malformed-header and sign-in-continuation failure tests; compact architecture, threat-model, security, configuration, and `AGENTS.md` updates; and acceptance evidence from formatting, linting, type checking, all tests, OpenAPI/migration checks, audit, production build, focused commit, GitHub CI, approved Sites deployment, and live trusted-header sign-in verification.
- **TASK-063:** Resolve the newly published high-severity `image-size` denial-of-service advisories in Vinext's transitive dependency graph without downgrading or breaking the Sites runtime, including verification of upstream advisory and package compatibility; the narrowest supported direct upgrade or package-manager override; an updated lockfile; retained runtime/development dependency classification; documentation and changelog updates where the release dependency set changes; regression evidence from a clean dependency tree, zero high-severity audit findings, formatting, linting, type checking, all tests, OpenAPI/Swagger/migration checks, production build, focused commit, GitHub CI, approved Sites deployment, and live smoke verification.
- **TASK-068:** Implement RFC-compatible `Accept` negotiation for HTML, compatible JSON, and versioned hypermedia, including media-range quality ordering, `q=0` exclusion, wildcard and supported-fallback behavior, `406` when no representation is acceptable, request-negotiated hypermedia error media types, unchanged OAuth-standard payload exceptions, focused parser/route/error tests, OpenAPI/hypermedia/architecture documentation updates, and complete validation, commit, CI, approved deployment, and live acceptance evidence.
- **TASK-073:** Strengthen route and Swagger acceptance evidence so delegated storage methods are derived from executable runtime routing rather than a duplicated hard-coded list and a real rendered Swagger `Try it out` request/response is exercised in browser automation; retain exact OpenAPI 3.1 method parity, same-origin request rewriting, JSON negotiation, schema styling, CSP, and no persisted credentials; add failure fixtures, tooling/style/architecture documentation updates, and complete validation, commit, CI, approved deployment, and live browser evidence.
- **TASK-074:** Create a root `BACKLOG.md` for uncommitted future implementation ideas and add separate stable unchecked backup/restore and incremental live-synchronization capabilities, including complete user/client namespace isolation, internal-table and secret exclusion, D1/R2 snapshot consistency, encrypted export ownership, resumable/idempotent transfer, conflict and deletion semantics, rate and quota controls, provider-limit uncertainty, recovery verification, and explicit non-availability language; distinguish backlog ideas from the product-direction `ROADMAP.md` and auditable implementation `PLAN.md`; link and document the relationship in README, contributor guidance, and compact `AGENTS.md`; add flat-list/stable-ID documentation checks; and record acceptance evidence from formatting, linting, type checking, tests, documentation checks, full validation, focused commit, GitHub CI, approved Sites deployment, and live README-link verification.
- **TASK-079:** Replace email-only and independent-key administrator authorization with a stable session-bound AittaDB administrator-principal contract: allow only a currently authenticated trusted ChatGPT Sites identity whose immutable local UUID is present in `ADMIN_SUBJECTS`; require no administrator password/key, key header, unlock form, or separate unlock cookie; remove `ADMIN_EMAILS`, `ADMIN_ACCESS_KEY_HASH`, generated-admin-key tooling, and their hosted values; document the manual fresh-deployment bootstrap through `/session` plus Sites secret configuration; preserve generic denial, same-origin and CSRF enforcement for mutations, durable redacted audit events for every administrative mutation, no display-name authorization, and no administrative data/actions for non-admin users; update configuration, OpenAPI, hypermedia, HTML, security, architecture, threat model, deployment, examples, README, changelog, and compact `AGENTS.md`; add authenticated allowlisted-subject success, anonymous, unlisted-subject, reassigned-email, malformed-list, audit-redaction, and existing-deployment migration tests; and record acceptance evidence from full validation, focused commit, CI, approved deployment, and live allowlisted/non-allowlisted administrator checks.
- **TASK-081:** Add a compact evidence-based readiness-confidence practice for repository work, including a `0`-to-`100` confidence score whenever an agent decides that a feature, task, deployment, or release is ready; a short record of evidence and residual uncertainty; an explicit rule that confidence never replaces security gates or the definition of done and should rarely be `100`; preference for the smallest risk-reducing implementation over speculative over-engineering; mandatory capture of material residual findings in `PLAN.md`, `ROADMAP.md`, or `BACKLOG.md`; concise `AGENTS.md` and contributor guidance within the instruction-size ceiling; policy tests; and acceptance evidence from formatting, checks, full validation, focused commit, CI, approved deployment where applicable, and the final report.
- **TASK-087:** Verify that the active `aittadb.com` custom domain advances to the exact successfully deployed Sites revision, including comparison with the Sites hostname and production worker-version evidence; bounded propagation retries without changing domain DNS, validation records, or access settings; escalation documentation if Sites keeps the custom domain pinned to an older worker; and acceptance evidence from the canonical signed-in home copy, public JSON/HTML smoke checks, error-only logs, committed PLAN state, and the existing approved deployment workflow.
- **TASK-088:** Complete live signed-in R2 browser acceptance using only synthetic or public test content, including collection upload, item download/read, replacement, list visibility, deletion, and cleanup through the canonical content-negotiated HTML controls; use Chrome's supported file-chooser flow after its file-URL permission is available, never inspect browser credentials or upload private user files, retain local integration/API evidence when the browser permission is unavailable, and record exact platform-limited evidence without weakening the automated gate.
- **TASK-089:** After the merged `codex/initial-implementation` pull request, advance all remaining repository work onto a fresh `codex/` feature branch created from the fetched canonical `origin/main`; preserve the merged history and current validated PLAN evidence without rewriting or pushing `main`; verify clean ancestry, branch tracking, remote state, and intended working-tree changes; update workflow documentation only if the established branch contract changes; and record acceptance evidence with a focused commit, push, and new draft pull request for subsequent changes.
- **TASK-090:** Add a basic AittaDB Privacy Policy adapted in English from the maintainer-provided `hg.fi/privacy.html` reference and constrained to implemented behavior, including one canonical content-negotiated `GET /privacy` resource with accessible HTML and versioned hypermedia JSON; shared footer/root/session links; configurable controller/contact name, email, postal address, and any other displayed contact data supplied only through documented environment values with no committed personal defaults; when explicit privacy contact configuration is absent, resolve the first canonical UUID in `ADMIN_SUBJECTS` through the local-user repository and use that administrator's current email and optional name as the contact, while returning a generic unavailable representation if neither source resolves; clear treatment of ChatGPT Sites identity signals, local UUIDs, OAuth/OIDC credentials, D1 records, R2 files, audit/security logs, retention, recipients/processors, user rights, international/platform processing, cookies, and deployment-specific operator responsibility; safe rendering, validation, omission, and escaping for incomplete or malformed contact configuration; complete OpenAPI/interface/configuration/README/architecture/threat-model/deployment/`AGENTS.md` updates; route/content-negotiation, explicit-config, administrator-fallback, missing/malformed-config, no-secret/PII-leak, hypermedia-parity, footer-link, OpenAPI-parity, and production-build tests; and acceptance evidence from full validation, focused commits, GitHub CI, approved Sites deployment, and live desktop/mobile HTML plus JSON checks.
- **TASK-091:** Configure the hosted AittaDB Privacy Policy contact email as `jheusala@iki.fi` through the existing `PRIVACY_CONTACT_EMAIL` deployment environment contract without changing administrator allowlists, identity mappings, source defaults, access settings, or secrets; preserve the existing contact name through explicit configuration where required by the fail-closed privacy contract; verify the resulting Sites environment revision and live HTML plus versioned hypermedia JSON representations expose the requested contact address and no deployment configuration; and record acceptance evidence with a focused PLAN commit, branch push, and green CI.
- **TASK-092:** Make `PLAN.md` an unfinished-work queue by moving every completed task entry into `CHANGELOG.md` with its stable task identifier and full auditable description; preserve all unchecked tasks and their dependency order; update `AGENTS.md` and repository workflow tests to require new work to enter PLAN first and completed work to move atomically into the changelog only after its definition of done passes; keep both files readable and within repository constraints; verify exact before/after completed and open counts, formatting, workflow tests, and instruction-size checks; and record acceptance evidence with a focused commit, branch push, and green CI.
- **TASK-094:** Review every open Dependabot pull request in `aittadb/aittadb`, including the dependency and advisory rationale, semver and runtime compatibility, manifest and lockfile scope, transitive changes, deprecations, release notes where material, CI status, and interaction with the current feature branch; reject or request correction for unsafe, unnecessary, stale, duplicate, or failing updates; approve and otherwise process valid updates without merging under the standing no-merge rule; reproduce or repair failures in focused branches only when needed; keep dependency audit and production build gates intact; document decisions and residual risk; and record acceptance evidence in the repository workflow with focused commits, branch pushes, and green CI where local changes are required.
- **TASK-095:** Keep the primary Git worktree continuously checkpointed by staging and committing intended changes promptly and pushing them when validated and authenticated access is available; permit `PLAN.md`, `ROADMAP.md`, `BACKLOG.md`, `CHANGELOG.md`, and equivalent coordination records to be edited and committed directly there; require implementation subagents to work only in their isolated Git worktrees; integrate only reviewed, complete, validated agent commits into the primary worktree by rebase or an equivalent commit-preserving operation; never integrate partial agent work; preserve unrelated user changes; update compact `AGENTS.md` guidance and repository-policy tests; and record focused commit, push, and green CI evidence.
- **TASK-055:** Give OAuth 2.0 and OpenID Connect operation entry resources canonical hypermedia controls and equivalent HTML forms where protocol standards permit, including authorization-code PKCE, device authorization, token exchange grant-specific conditional fields, revocation, introspection, and UserInfo; keep standards-defined successful and error wire payloads compatible; advertise encodings, required fields, secrets, constraints, authorization schemes, and currently valid operations; add action/form equivalence, hidden-field, success, and failure tests; update route OpenAPI, examples, architecture, style guide, changelog, and compact `AGENTS.md`; and record complete validation, focused commit, GitHub CI, approved Sites deployment, and live form/API evidence.
- **TASK-096:** Correct front-page and public copy so it accurately distinguishes organizational status from technical platform dependency: AittaDB is a third-party, non-official project, while the current hosted implementation depends on OpenAI-hosted ChatGPT Sites for runtime, ChatGPT sign-in, D1, R2, configuration, and secrets; keep the no-forwarded-credentials boundary clear; remove the out-of-place "Persistent events are planned" sentence from deployment/trust explanations while retaining honest planned-events status in capability or roadmap contexts; explicitly cover `app/page.tsx` deployment copy, the token-success heading, the issuer-discovery summary, and deployment-scoped ownership guidance; update every affected HTML/JSON/OpenAPI/documentation representation and focused copy tests; and record formatting, relevant tests, build, focused commit, push, CI, approved Sites deployment, and live verification evidence.
- **TASK-065:** Enforce JWT credential purpose at every bearer-token consumer, including rejection of ID tokens by UserInfo, introspection, storage, and any access-token verifier; `openid` scope enforcement for UserInfo; access-token-only introspection state; retained ID-token audience/nonce behavior; algorithm/key/issuer/audience validation; focused positive and negative tests, OAuth/OIDC/OpenAPI/security documentation updates, and complete validation, commit, CI, approved deployment, and live acceptance evidence.
- **TASK-097:** Refine the root `AGENTS.md` product boundary and engineering decision rules so AittaDB remains a general-purpose hosted database/application-backend server built from small vendor-neutral primitives; keep client SDKs, application integrations, provider adapters, and application-specific workflows in separate repositories; integrate primitive-first delivery, restrained architecture, explicit boundary-conflict handling, and planning/OpenAPI/schema implications into existing sections without duplicate policy; remove conflicting or obsolete guidance; keep the file below 32,000 bytes; add focused repository-policy tests and acceptance evidence from formatting, linting, type checking, relevant tests, the AGENTS size check, commit, push, and CI; then preserve the completed task in `CHANGELOG.md`.
- **TASK-098:** Replace defensive lead copy that calls AittaDB a "third-party, non-official project" with concise positive licensing and product language: describe AittaDB as a source-available hosted application-backend project, state that current public releases use FSL-1.1-MIT and convert to MIT two years after publication, and accurately disclose that a commercial MIT license is available now without calling the current FSL release open source or weakening the separate no-affiliation/no-endorsement boundary; update every affected HTML, hypermedia, OpenAPI, README/style/licensing representation and focused copy tests in the same task; validate formatting, linting, types, tests, OpenAPI, build, commit, push, CI, approved Sites deployment, and live HTML/JSON copy; then preserve the completed task in `CHANGELOG.md`.
- **TASK-100:** Remove repetitive front-page licensing, platform, and credential-boundary prose while preserving equivalent human and machine facts: keep the versioned hypermedia root's detailed self-description; give the HTML root one concise capability-focused summary instead of rendering that machine description verbatim; reduce the trust note to one non-duplicative source-available FSL-1.1-MIT, ChatGPT Sites dependency, separate-AittaDB-credentials/no-forwarding statement with the existing licensing/platform details link; retain secondary no-affiliation/no-endorsement information on the linked documentation rather than repeating it in the first viewport; update focused HTML/JSON copy and repository-policy tests plus style guidance where needed; and record acceptance evidence from formatting, linting, type checking, relevant tests, build, focused commit, GitHub CI, approved Sites deployment, and live HTML/JSON verification.
