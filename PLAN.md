# AittaDB Implementation Plan

- [ ] TASK-220: Provision one production service client restricted to Events publication and deliver its credential only to the AittaSocial Hub Sites runtime. DoD: after TASK-219, use the real allowlisted AittaDB administrator session to create one dedicated service client with exactly `events.publish`; retain its one-time client identifier and secret only in protected transient memory or mode-0600 storage until setting separate Hub Sites runtime variables reserved for Events publication; preserve every unrelated Hub environment entry, secret, binding, source version, access setting, and domain; apply configuration through the Sites runtime without committing or logging credentials; verify the client obtains only a short-lived client-credentials access token whose response and signed claims contain exactly `events.publish`, receives no user claims, ID token, or refresh token, and cannot request read, subscribe, storage, identity, or additional scopes; verify the deployed publication contract from discovery and automated integration evidence without creating a synthetic production event, destroy transient credentials, record only coarse evidence, and leave the client active for Hub `TASK-157`.
