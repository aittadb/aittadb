# AittaDB Implementation Plan

- [ ] TASK-242: Add the acceptance-only proof-safety assertion for disposable bounded-storage contract proofs. Depends on TASK-241. DoD: an explicit default-off deployment setting can enable only a recognized non-production logical issuer to serve `GET /.well-known/aittadb-proof-safety?challenge=<canonical UUIDv4>` as the exact no-store AittaDB 0.1 hypermedia assertion bound to the request URL, issuer, fixed `disposable-acceptance` environment, and fixed allowed proof purpose with no links or actions; malformed/absent challenges, unsupported representations or methods, disabled configuration, and a production issuer never expose an allowing assertion or touch D1, R2, identity, CORS, rate, or cleanup state; issuer, deployment, and agent instructions distinguish each disposable acceptance deployment's own canonical HTTPS issuer from production `https://aittadb.com`; configuration, canonical OpenAPI, operations/security documentation, focused config and HTTP tests, full relevant validation, commit, push, and CI pass.
- [ ] TASK-243: Admit bounded-record transaction traffic to a finite pre-body rate gate. Depends on TASK-241. DoD: after feature and browser-origin gates but before a JSON or form transaction body is pulled, a non-disclosing IP/deployment admission family rejects excess requests with the documented fixed response; successful traffic reaches the existing subject/client write admission exactly once without double-counting the new gate; missing D1, malformed credentials, CORS, feature-disabled, and body-bound outcomes retain their existing precedence; stream-pull, rate-boundary, and Memory/D1 parity tests plus performance/security documentation, full relevant validation, commit, push, and CI pass.
- [ ] TASK-244: Correct the bounded transaction non-disclosure contract to match namespace-scoped compare-and-set behavior. Depends on TASK-241. DoD: discovery, OpenAPI, protocol/security documentation, and focused integration assertions state that authenticated transaction preconditions operate only in the caller's namespace, so a foreign-existing and own-absent key receive the same mutation-specific fixed outcome while record reads remain fixed `404`; no contract claims transactions detect or distinguish foreign records, and versioned payloads/tests stay behaviorally compatible; full relevant validation, commit, push, and CI pass.
- [ ] TASK-232: Close the bounded-storage review gate on the final combined diff. Depends on TASK-228, TASK-230, TASK-231, TASK-239, TASK-240, TASK-241, TASK-242, TASK-243, TASK-244, and every additional P0/P1/P2 remediation task created by TASK-228. DoD: the primary agent verifies every review finding against the combined diff, obtains reviewer confirmation where needed, records all residual lower-severity risk, and the exact pushed head passes `npm run validate`, tracked-tree and reachable-history secret checks, and exact-head GitHub CI before any deployment.
- [ ] TASK-233: Deploy the exact reviewed bounded-storage build to the acceptance Site. Depends on TASK-232. DoD: capture secret-free acceptance and production baselines, record the current acceptance version as rollback, package the exact pushed TASK-232 source with migrations 0018 through 0021, deploy it only to `test.aittadb.com`, and prove source revision, migration success, public access, custom domain, bindings, environment values, feature flags, administrators, unrelated clients/data, and `aittadb.com` remain unchanged except for the accepted acceptance source/version.
- [ ] TASK-234: Prove the hosted bounded-record discovery and operational envelope. Depends on TASK-233. DoD: `https://test.aittadb.com/storage/record-protocol` returns protocol 1.1, exactly the three actions, eight capabilities, accepted finite limits, exact scopes, and same-origin HTTPS targets; health reports D1/R2 available; security and no-store headers are present; error-only Worker logs contain no failure or private value; and credential-free evidence plus confidence are archived.
- [ ] TASK-235: Prove hosted atomic transaction and durable idempotency behavior. Depends on TASK-233. DoD: one disposable least-privilege service namespace proves ordered mixed put/check/delete, exact replay, changed-operation conflict, stale-revision rollback, concurrent one-winner behavior, and exact replay after fresh token/runtime reconstruction and simulated response loss; no credential or private stored value enters source, logs, URLs, or retained evidence; and credential-free outcomes plus confidence are archived.
- [ ] TASK-236: Prove hosted quota preflight and rollback. Depends on TASK-233. DoD: bounded synthetic records bring only the disposable namespace to its existing configured item ceiling, one additional transaction returns fixed `quota_exceeded` while committing neither records nor receipt, deletes in one transaction demonstrably fund puts, and all created application records are deleted afterward without changing hosted limits or any environment value; retain only coarse credential-free counts and confidence.
- [ ] TASK-237: Prove hosted namespace isolation and non-disclosing authorization. Depends on TASK-233. DoD: two disposable service namespaces show that one cannot read, list, replace, or delete the other's records; foreign-existing and own-absent observations are equivalent for the same valid operation shape; missing/insufficient credentials remain only the documented OAuth boundary; all fixed protocol errors omit keys, values, operation IDs, cursors, quota state, identities, credentials, paths, and backend text; and credential-free outcomes plus confidence are archived.
- [ ] TASK-238: Revoke acceptance credentials and close the hosted proof. Depends on TASK-234, TASK-235, TASK-236, and TASK-237. DoD: delete every disposable application record, revoke or disable every temporary service credential/client, destroy protected transient secret material and archives, confirm no private value was retained as evidence, compare acceptance and production settings/domains/access/bindings/unrelated state to their baselines, verify `aittadb.com` source and data were untouched, record acceptance source/version and rollback revision, archive all completed proof tasks, leave `PLAN.md` with zero unchecked tasks, commit and push the final history, and report hosted readiness confidence plus exact residual uncertainty.
