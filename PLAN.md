# AittaDB Implementation Plan

- [ ] TASK-192: Deploy the exact Events candidate to `test.aittadb.com`. DoD: push one completely validated feature-branch commit to the sole draft PR, package that exact source and checked-in migrations, save and deploy it only to the preapproved acceptance Site, enable Events there with finite test limits, and prove deployment success plus D1/R2 health while byte-for-byte preserving unrelated environment entries, secrets, feature flags, bindings, access policy, and custom domains; record only non-sensitive commit/version/configuration evidence and confirm `aittadb.com` source, environment revision, data, access, DNS, and domains were untouched.
- [ ] TASK-193: Prove hosted event publication and idempotency. DoD: on the exact TASK-192 acceptance deployment, use one temporary signed-in or service namespace to prove first publication, exact idempotent replay without another row, conflicting key reuse rejection, payload/type/body limits, and disabled publication behavior; record statuses and opaque counts only, with no identity, client secret, token, key, payload, event identifier, or environment value; remove every event fixture created by this proof through bounded retention or isolated acceptance cleanup; and leave unrelated deployment state unchanged.
- [ ] TASK-205: Prove hosted event collection paging and filtering (split from TASK-194). DoD: on the exact TASK-192 deployment, use bounded synthetic fixtures in one temporary namespace to prove deterministic oldest-first pages, finite page size, opaque cursor resume without duplicates, exact type filtering, and malformed/expired cursor rejection; record only coarse statuses and counts, remove every fixture, and leave unrelated deployment state unchanged.
- [ ] TASK-206: Prove hosted immutable event item retrieval (split from TASK-194). DoD: on the exact TASK-192 deployment, prove one owned event item is readable and immutable while malformed, absent, other-user, and other-client identifiers return the same generic result; record no identity, client, event ID, payload, token, or credential, remove every fixture, and leave unrelated deployment state unchanged.
- [ ] TASK-195: Prove hosted bounded long polling. DoD: on the exact TASK-192 deployment, prove one valid cursor waits and returns promptly after a later same-namespace publication, one wait reaches its documented empty timeout bound, ordinary polling remains immediate, and invalid cursor, missing subscribe scope, rate-limit, cancellation where observable, and foreign-namespace publication do not disclose or unblock data; record only timings/statuses within coarse safe ranges, remove all fixtures, and leave unrelated deployment state unchanged.
- [ ] TASK-207: Prove hosted Events scope and client-state enforcement (split from TASK-196). DoD: on the exact TASK-192 deployment, prove publish/read/subscribe scope separation, anonymous denial, service-token non-human claims, current-session policy, and disabled-client denial using bounded temporary clients; record no identity, secret, token, event ID, payload, or complete request, disable temporary clients, remove grants and fixtures, and leave unrelated deployment state unchanged.
- [ ] TASK-208: Prove hosted cross-client event isolation (split from TASK-196). DoD: on the exact TASK-192 deployment, use two temporary clients for one authorized identity to prove neither client can list, retrieve, resume, or unblock on the other's events; record only generic statuses and counts, disable both clients, remove grants and fixtures, and leave unrelated deployment state unchanged.
- [ ] TASK-209: Prove hosted cross-user event isolation (split from TASK-196). DoD: on the exact TASK-192 deployment, use the two preapproved test identities in one temporary client to prove neither identity can list, retrieve, resume, or unblock on the other's events and that current-session namespaces remain separate; record no identity, subject, credential, event ID, or payload, remove grants and fixtures, and leave unrelated deployment state unchanged.
- [ ] TASK-210: Prove hosted event admission quota behavior (split from TASK-197). DoD: on the exact TASK-192 deployment with one bounded temporary namespace limit, prove the boundary-crossing append is rejected atomically, an exact replay consumes no additional quota, and authorized reads/deletes or retention remain usable; record coarse counts/statuses, restore the temporary limit, remove fixtures, and leave unrelated deployment state unchanged.
- [ ] TASK-211: Prove hosted bounded event retention (split from TASK-197). DoD: on the exact TASK-192 deployment with one bounded temporary retention setting, prove expired events are removed in finite batches while unexpired and foreign-namespace events survive; record coarse counts/statuses, restore the temporary setting, remove fixtures, and leave unrelated deployment state unchanged.
- [ ] TASK-212: Prove hosted account-deletion event safety (split from TASK-197). DoD: on the exact TASK-192 deployment using a disposable acceptance identity suitable for deletion, prove deletion start blocks new event writes, owned event residue blocks finalization, bounded purge removes only that subject's events, and finalization then succeeds without affecting another identity or service principal; record only coarse states/counts, remove residual fixtures, and leave unrelated deployment state unchanged.
- [ ] TASK-213: Restore the Events acceptance deployment after hosted proofs (split from TASK-198). DoD: verify all temporary Events fixtures, clients, grants, credentials, deleted identities, and local artifacts from hosted proofs are removed or irreversibly disabled; restore every temporary acceptance environment setting while retaining only the explicitly intended Events feature state; redeploy the same reviewed source only if applying configuration requires it; confirm health and public/private authorization baselines; and prove `aittadb.com` source, data, secrets, access, DNS, and domains remained untouched.
- [ ] TASK-214: Complete the Events branch and pull-request handoff (split from TASK-198). DoD: archive every completed PLAN description unchanged with evidence in `CHANGELOG.md`, leave `PLAN.md` with zero unchecked items, run complete validation on the exact final commit, push the feature branch, leave one ready-for-review PR to `develop` without creating a direct Events PR to `main`, report evidence and residual hosted uncertainty with a readiness-confidence score, and do not merge or change `aittadb.com`.
