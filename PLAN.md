# AittaDB Implementation Plan

- [ ] TASK-265: Remove the latest failed disposable StorageAdapter fixture from `test.aittadb.com`. Depends on the redacted proof result supplying its exact collection and operation prefixes. DoD: enable only the accepted maintenance control on acceptance for the bounded repair window, remove only that service namespace's named synthetic records and durable receipt, prove zero matching rows with redacted count-only evidence, immediately disable the control, preserve every other acceptance namespace, client, setting, binding, proof endpoint, and all production resources, and archive after a repeatable hosted check.
- [ ] TASK-267: Repair D1 parity for a nonzero acceptance-maintenance service-namespace purge. Depends on the observed hosted nonzero maintenance `not_found` behavior. DoD: reproduce the active service-client record-and-receipt purge through a real D1-compatible adapter, identify and correct only the bounded maintenance SQL/batch parity defect, add focused D1 regression coverage for one record and its associated receipt plus zero-row, isolation, and fixed-failure behavior, update the maintenance runbook if operational behavior changes, pass relevant formatting/lint/types/tests/OpenAPI/migration/build validation and independent correctness review, deploy only the accepted source to `test.aittadb.com`, and retain credential-free hosted count-only evidence; preserve production, DNS, unrelated acceptance settings, bindings, access, data, and credentials.
- [ ] TASK-266: Verify the disposable proof owner service client's initial bounded-record transaction contract on `test.aittadb.com`. Depends on TASK-265 and TASK-267. DoD: use only the external mode-0600 handoff and a newly generated disposable fixture to prove confidential `client_credentials`, discovery, exact storage scopes, the advertised atomic transaction capability, an idempotent receipt, and non-disclosing fixed failures; remove that fixture and receipt through the bounded acceptance repair path, retain only redacted evidence, preserve production and unrelated acceptance state, issue one precise retry handoff, and archive after an independent retry can begin.
- [ ] TASK-261: Use the accepted maintenance primitive to remove the current failed disposable proof fixture on `test.aittadb.com`. Depends on TASK-260. DoD: deploy only the validated source commit and one explicitly approved acceptance feature setting, use the existing administrator session and the existing isolated proof service namespace to remove only the failed proof collection prefix and associated receipts, prove zero matching records and receipts with redacted count-only evidence, preserve every other acceptance namespace, client, setting, binding, proof endpoint, and all production resources, then archive after a repeatable credential-free hosted check.
- [ ] TASK-257: Clean up the disposable Investor App StorageAdapter proof handoff after its redacted `status: "passed"` confirmation. Depends on TASK-259, TASK-261, and that confirmation. DoD: remove only the named synthetic fixture prefixes, revoke both temporary service-type confidential clients, remove the external mode-0600 JSON handoff file, prove neither credential remains usable and no handoff artifact remains, preserve production and unrelated acceptance state, retain only redacted evidence, and archive the task.
- [ ] TASK-253: Close the disposable proof-safety window after the external acceptance consumer confirms its proof. Depends on TASK-252, TASK-257, and that consumer confirmation. DoD: disable only the acceptance proof-safety setting through the same patch-style configuration path, preserve source, bindings, access, data, other settings, and production, prove an otherwise valid assertion request is again unavailable, retain credential-free before/after setting-difference evidence; then archive this final operational task, leave `PLAN.md` with zero unchecked tasks, commit and push the final history, and report hosted readiness confidence plus exact residual uncertainty.
