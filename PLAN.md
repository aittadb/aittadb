# AittaDB Implementation Plan

- [ ] TASK-256: Provision the disposable Investor App acceptance StorageAdapter proof handoff. Depends on the Investor App agent's redacted handoff-file contract. DoD: create only on `test.aittadb.com` one isolated service-type confidential owner client with exactly `storage.read`, `storage.write`, and `storage.delete`, and one distinct service-type confidential read-only outsider client; use a bounded expiry; create one mode-0600 JSON handoff file outside every repository and accessible to the shared local user; disclose only its non-secret path and expiry; preserve production, DNS, source, committed configuration, normal acceptance settings, data outside named disposable proof fixtures, bindings, access, and all secrets; retain only redacted evidence and archive the task after the Investor agent acknowledges receipt.
- [ ] TASK-257: Clean up the disposable Investor App StorageAdapter proof handoff after its redacted `status: "passed"` confirmation. Depends on TASK-256 and that confirmation. DoD: remove only the named synthetic fixture prefixes, revoke both temporary service-type confidential clients, remove the external mode-0600 JSON handoff file, prove neither credential remains usable and no handoff artifact remains, preserve production and unrelated acceptance state, retain only redacted evidence, and archive the task.
- [ ] TASK-253: Close the disposable proof-safety window after the external acceptance consumer confirms its proof. Depends on TASK-252, TASK-257, and that consumer confirmation. DoD: disable only the acceptance proof-safety setting through the same patch-style configuration path, preserve source, bindings, access, data, other settings, and production, prove an otherwise valid assertion request is again unavailable, retain credential-free before/after setting-difference evidence; then archive this final operational task, leave `PLAN.md` with zero unchecked tasks, commit and push the final history, and report hosted readiness confidence plus exact residual uncertainty.
