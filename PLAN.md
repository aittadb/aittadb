# AittaDB Implementation Plan

- [ ] TASK-268: Remove the latest failed Investor App StorageAdapter fixture from `test.aittadb.com`. Depends on the redacted failure result supplying one exact disposable collection and operation prefix. DoD: enable only the accepted maintenance control for the bounded repair window, remove only the selected owner service namespace's matching synthetic records and durable receipt, prove zero matching counts with redacted evidence, immediately disable the control, preserve every other acceptance namespace, client, setting, binding, proof endpoint, and all production resources, and archive after a repeatable hosted check.
- [ ] TASK-269: Diagnose the latest failed owner-client first bounded-storage fixture on `test.aittadb.com`. Depends on the redacted failure result and may run independently of TASK-268. DoD: inspect only bounded recent acceptance Worker error evidence and the existing private owner-client handoff against confidential `client_credentials`, discovery, and one isolated atomic transaction; identify any AittaDB discrepancy and add its smallest focused corrective task before changing source, otherwise issue one precise redacted retry handoff; retain no credentials, identifiers, stored values, or query values in source, logs, plan history, or evidence; preserve production, DNS, acceptance settings, bindings, access, and unrelated data, and archive after the diagnosis and next action are clear.
- [ ] TASK-270: Align bounded-record transaction discovery with its successful response contract. Depends on TASK-269. DoD: make the versioned discovery resource, canonical TypeScript contract, OpenAPI, protocol documentation, and `transact-records` success/replay response use the same ordered `records` field; add focused consumer-style parity coverage that fails for discovery/response field drift and validates the strict response shape; preserve atomic, idempotency, authorization, limits, and all existing wire semantics; pass relevant formatting, linting, types, tests, OpenAPI checks, and build validation; obtain independent correctness review, deploy only the accepted source to `test.aittadb.com`, retain redacted hosted evidence, and archive after one external retry can begin.
- [ ] TASK-257: Clean up the disposable Investor App StorageAdapter proof handoff after its redacted `status: "passed"` confirmation. Depends on TASK-259, TASK-261, and that confirmation. DoD: remove only the named synthetic fixture prefixes, revoke both temporary service-type confidential clients, remove the external mode-0600 JSON handoff file, prove neither credential remains usable and no handoff artifact remains, preserve production and unrelated acceptance state, retain only redacted evidence, and archive the task.
- [ ] TASK-253: Close the disposable proof-safety window after the external acceptance consumer confirms its proof. Depends on TASK-252, TASK-257, and that consumer confirmation. DoD: disable only the acceptance proof-safety setting through the same patch-style configuration path, preserve source, bindings, access, data, other settings, and production, prove an otherwise valid assertion request is again unavailable, retain credential-free before/after setting-difference evidence; then archive this final operational task, leave `PLAN.md` with zero unchecked tasks, commit and push the final history, and report hosted readiness confidence plus exact residual uncertainty.
