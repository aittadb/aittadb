# AittaDB Implementation Plan

- [ ] TASK-273: Decompose the feature-flagged deployment-operator D1 administration exception into the smallest secure AittaDB primitives. DoD: reconcile the requested administrator-only database console with the current isolation and administration boundary; define the exact Sites-session identity, deployment flags, read/write/query capability split, audit/redaction model, HTML/hypermedia contract, persistence/migration needs, and acceptance-versus-production deployment controls; create dependency-ordered replacement tasks without implementing runtime behavior, changing hosted configuration, or deploying; then archive this unchanged decomposition task with the approved architecture decision.
- [ ] TASK-271: Remove the latest failed disposable Investor App StorageAdapter fixture from `test.aittadb.com`. Depends on the redacted failure result supplying one exact collection and operation prefix. DoD: enable only the accepted bounded acceptance-maintenance control, remove only the selected owner service namespace's matching synthetic records and durable receipt, prove zero remaining counts with redacted evidence, immediately disable the control, preserve production and all unrelated acceptance state, and archive after a repeatable hosted check.
- [ ] TASK-272: Diagnose the subsequent failed owner-client first bounded-storage fixture on `test.aittadb.com`. Depends on the redacted failure result and may run independently of TASK-271. DoD: inspect bounded recent acceptance Worker evidence and the protected owner-client handoff against confidential `client_credentials`, discovery, and one isolated initial atomic transaction; identify a precise AittaDB discrepancy and add its smallest corrective task before source changes, or issue one redacted retry handoff if none exists; retain no credentials, identifiers, stored values, query values, or private paths, preserve production and unrelated acceptance state, and archive after the next action is clear.
- [ ] TASK-257: Clean up the disposable Investor App StorageAdapter proof handoff after its redacted `status: "passed"` confirmation. Depends on TASK-259, TASK-261, and that confirmation. DoD: remove only the named synthetic fixture prefixes, revoke both temporary service-type confidential clients, remove the external mode-0600 JSON handoff file, prove neither credential remains usable and no handoff artifact remains, preserve production and unrelated acceptance state, retain only redacted evidence, and archive the task.
- [ ] TASK-253: Close the disposable proof-safety window after the external acceptance consumer confirms its proof. Depends on TASK-252, TASK-257, and that consumer confirmation. DoD: disable only the acceptance proof-safety setting through the same patch-style configuration path, preserve source, bindings, access, data, other settings, and production, prove an otherwise valid assertion request is again unavailable, retain credential-free before/after setting-difference evidence; then archive this final operational task, leave `PLAN.md` with zero unchecked tasks, commit and push the final history, and report hosted readiness confidence plus exact residual uncertainty.
