# AittaDB Implementation Plan

- [ ] **TASK-223:** Promote one exact validated `develop` revision through the ChatGPT Sites acceptance-to-production release path. DoD: document the invariant that `test.aittadb.com` receives and passes bounded hosted smoke checks before `aittadb.com`; build, save, and deploy one pushed `develop` commit to acceptance without changing environment values, secrets, bindings, domains, or access policy; verify public metadata, health, protected-route denial, discovery, and enabled Events behavior; deploy the identical commit and compiled build output to production only after acceptance succeeds, allowing archives to differ solely in the Sites-required target `project_id`; verify both custom domains resolve to that revision with equivalent public behavior and error-only logs; update deployment guidance and compact `AGENTS.md`; expose no deployment identifiers or configuration; pass full validation and exact-head CI; archive the unchanged task with evidence in `CHANGELOG.md`; and report readiness confidence plus residual hosted uncertainty.
